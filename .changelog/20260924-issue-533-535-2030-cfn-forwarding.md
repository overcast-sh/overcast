* [cloudformation/pipes] forward SourceParameters, TargetParameters, Enrichment, EnrichmentParameters, Description, DesiredState and Tags to CreatePipe/UpdatePipe
  Update now also sends RoleArn (UpdatePipe requires it every call) and uses PUT, not PATCH — the route only answers PUT, so updating a forwarded property used to 405 and roll back
  a Tags-only change, including a stack-tag change, reconciles via TagResource/UntagResource instead of forcing replacement
*! [cloudformation/pipes] a pipe naming an unsupported source/target/filter/enrichment now fails the stack instead of silently deploying without it
  migration: drop FilterCriteria, an ActiveMQ/RabbitMQ/MSK/Kafka source, or an HttpParameters enrichment from the template, or accept the named ValidationException
* [cloudformation/firehose] forward every destination configuration, KinesisStreamSourceConfiguration, encryption config and Tags to CreateDeliveryStream
  DescribeDeliveryStream echoes each one back, renamed to its Description counterpart, instead of always reporting an empty Destinations list
  KinesisStreamSourceConfiguration with no DeliveryStreamType now implies KinesisStreamAsSource rather than DirectPut; a Tags-only update reconciles instead of replacing
*! [mcp/acm] runtime_acm_request_certificate dispatches through RequestCertificate instead of writing acm:certs directly, gaining domain validation
  migration: pass a well-formed domain name — a malformed one this tool used to accept now answers RequestCertificate's ValidationException
* [acm] RequestCertificate answers ValidationException for a missing DomainName, matching AWS's required-member wording, not its own InvalidParameterException
