+ [web] Athena, Glue and S3 Tables appear in the console's sidebar, dashboard and global search, with ARN links into them
  Their console pages come later; until then each route shows the service's docs and raw state.
+ [athena/glue/s3tables] Athena query states, Glue table and partition changes and S3 Tables creates, renames, deletes and commits are published as events
  They reach the Events page and the console's live updates, so nothing polls for them.
