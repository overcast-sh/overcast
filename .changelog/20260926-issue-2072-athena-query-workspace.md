+ [web] Athena's console page is a query workspace: a SQL editor with completion from the catalog and every Trino function, and results in the data grid
  Query tabs, parameters, statistics, errors underlined in the SQL, and the engine's start-up shown while the first query waits.
  History, saved queries, workgroups with their prepared statements, and data catalogs are its other tabs.
+ [web] other pages open SQL in the Athena editor, S3's *Query with Athena* among them
