+ [web] The Glue table page's Iceberg tab shows the table's snapshots and metadata history
  It embeds the S3 Tables console's Iceberg viewer, read from the table's `metadata_location`, with *Query as of this snapshot* in Athena.
* [web] Moving from one S3 Tables table to another no longer shows the first table's metadata while the second loads
