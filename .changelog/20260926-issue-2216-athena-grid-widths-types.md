+ [web] the Athena result grid remembers its column widths per query tab, and a large result keeps its column types
  a result read from its CSV right-aligns numbers and shows NULLs as a small, paged result does.
