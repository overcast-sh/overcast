// Code generated by cmd/compatgen; DO NOT EDIT.

using Amazon.ECR;
using Amazon.ECR.Model;
using OvercastCompat.Clients;
using OvercastCompat.Harness;
using OvercastCompat.Scenario;

namespace OvercastCompat.Groups;

/// <summary>The authored ecr-registry groups.</summary>
/// <remarks>
/// Generated from compat/model/authored/ecr-registry.json by cmd/compatgen. The semantics live in
/// OvercastCompat.Scenario; this file is the data and the typed SDK calls.
/// </remarks>
internal sealed class ScenariosAuthoredEcrRegistry : IServiceGroup
{
    private static readonly ScenarioGroup GroupEcrRegistry = new("ecr-registry", "compat/model/authored/ecr-registry.json");

    private readonly Lazy<AmazonECRClient> _client;

    internal ScenariosAuthoredEcrRegistry(AwsClients clients)
    {
        // A generated group builds its own client from the configuration the
        // suite's hand-written groups share, rather than adding an accessor to
        // AwsClients for every service the generator learns to cover. Nothing
        // else about the client differs, and Lazy<T> builds it once however
        // many of the group's tests run concurrently.
        _client = new Lazy<AmazonECRClient>(() => clients.CreateClient(
            (credentials, configuration) => new AmazonECRClient(credentials, (AmazonECRConfig)configuration),
            new AmazonECRConfig()));
    }

    public string SourceName => "ScenariosAuthoredEcrRegistry";

    public IReadOnlyDictionary<string, TestFn> Impls() => new Dictionary<string, TestFn>(StringComparer.Ordinal)
    {
        ["ecr-registry:GetAuthorizationToken"] = TestEcrRegistryGetAuthorizationToken,
        ["ecr-registry:DescribeRegistry"] = TestEcrRegistryDescribeRegistry,
    };

    public IReadOnlyDictionary<string, SetupFn> Setups() => new Dictionary<string, SetupFn>(StringComparer.Ordinal)
    {
        ["ecr-registry"] = SetupEcrRegistry,
    };

    public IReadOnlyDictionary<string, SetupFn> Teardowns() => new Dictionary<string, SetupFn>(StringComparer.Ordinal)
    {
        ["ecr-registry"] = TeardownEcrRegistry,
    };

    private AmazonECRClient Cl() => _client.Value;

    private Task SetupEcrRegistry(TestContext t) =>
        // No setup steps: an empty phase is a no-op, not a missing one.
        GroupEcrRegistry.RunSetupAsync(t);

    private Task TeardownEcrRegistry(TestContext t) =>
        // No teardown steps: an empty phase is a no-op, not a missing one.
        GroupEcrRegistry.RunTeardownAsync(t);

    private Task TestEcrRegistryGetAuthorizationToken(TestContext t) => GroupEcrRegistry.RunTestAsync(t, "GetAuthorizationToken", new ScenarioTest
    {
        Call = new ScenarioCall
        {
            Op = "GetAuthorizationToken",
            Params = "{}",
            Build = b =>
            {
                var request = new GetAuthorizationTokenRequest();
                return request;
            },
            SendAsync = async request =>
                await Cl().GetAuthorizationTokenAsync((GetAuthorizationTokenRequest)request),
        },
        Assert =
        [
            Clause.ResponseField(
                Check.Matches("$.authorizationData[0].authorizationToken", "^QVdTO[g-v](?:[A-Za-z0-9+/]=|[A-Za-z0-9+/]{2}(?:[A-Za-z0-9+/]{4})*(?:[A-Za-z0-9+/]{2}==|[A-Za-z0-9+/]{3}=)?)$"),
                Check.NonEmpty("$.authorizationData[0].expiresAt"),
                Check.Matches("$.authorizationData[0].proxyEndpoint", "^http")
            )
        ],
    });

    private Task TestEcrRegistryDescribeRegistry(TestContext t) => GroupEcrRegistry.RunTestAsync(t, "DescribeRegistry", new ScenarioTest
    {
        Call = new ScenarioCall
        {
            Op = "DescribeRegistry",
            Params = "{}",
            Build = b =>
            {
                var request = new DescribeRegistryRequest();
                return request;
            },
            SendAsync = async request =>
                await Cl().DescribeRegistryAsync((DescribeRegistryRequest)request),
        },
        Assert =
        [
            Clause.ResponseField(
                Check.NonEmpty("$.registryId"),
                Check.NonEmpty("$.replicationConfiguration"),
                Check.IsList("$.replicationConfiguration.rules")
            )
        ],
    });
}
