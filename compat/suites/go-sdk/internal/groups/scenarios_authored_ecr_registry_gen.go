// Code generated by cmd/compatgen; DO NOT EDIT.

package groups

import (
	"context"
	"sync"

	"github.com/aws/aws-sdk-go-v2/service/ecr"
	"github.com/overcast-sh/overcast-compat-go-sdk/internal/clients"
	"github.com/overcast-sh/overcast-compat-go-sdk/internal/harness"
	"github.com/overcast-sh/overcast-compat-go-sdk/internal/scenario"
)

// ScenariosAuthoredEcrRegistry returns the authored ecr-registry groups.
//
// Generated from compat/model/authored/ecr-registry.json by cmd/compatgen.
// The semantics live in internal/scenario; this file is the data and the
// typed SDK calls.
func ScenariosAuthoredEcrRegistry(c *clients.Clients) ServiceGroup {
	g := &authoredEcrRegistryScenarios{c: c}
	return ServiceGroup{
		Name: "scenarios/authored-ecr-registry",
		Impls: map[string]harness.TestFn{
			"ecr-registry:GetAuthorizationToken": g.testEcrRegistryGetAuthorizationToken,
			"ecr-registry:DescribeRegistry":      g.testEcrRegistryDescribeRegistry,
		},
		Setup: map[string]func(context.Context, *harness.TestContext) error{
			"ecr-registry": g.setupEcrRegistry,
		},
		Teardown: map[string]func(context.Context, *harness.TestContext) error{
			"ecr-registry": g.teardownEcrRegistry,
		},
	}
}

type authoredEcrRegistryScenarios struct {
	c      *clients.Clients
	once   sync.Once
	client *ecr.Client
}

// cl builds this service's client once, from the config the suite's
// hand-written groups share. A generated group builds its own rather than
// adding an accessor to internal/clients for every service the generator
// learns to cover; nothing else about the client differs.
func (g *authoredEcrRegistryScenarios) cl() *ecr.Client {
	g.once.Do(func() { g.client = ecr.NewFromConfig(g.c.Config()) })
	return g.client
}

var groupEcrRegistry = scenario.Group{Name: "ecr-registry", File: "compat/model/authored/ecr-registry.json"}

func (g *authoredEcrRegistryScenarios) setupEcrRegistry(ctx context.Context, t *harness.TestContext) error {
	// No setup steps: an empty phase is a no-op, not a missing one.
	return groupEcrRegistry.RunSetup(ctx, t)
}

func (g *authoredEcrRegistryScenarios) teardownEcrRegistry(ctx context.Context, t *harness.TestContext) error {
	// No teardown steps: an empty phase is a no-op, not a missing one.
	return groupEcrRegistry.RunTeardown(ctx, t)
}

func (g *authoredEcrRegistryScenarios) testEcrRegistryGetAuthorizationToken(ctx context.Context, t *harness.TestContext) error {
	return groupEcrRegistry.RunTest(ctx, t, "GetAuthorizationToken", scenario.Test{
		Call: scenario.Call{
			Op:     "GetAuthorizationToken",
			Params: `{}`,
			Build: func(b *scenario.Binder) any {
				in := &ecr.GetAuthorizationTokenInput{}
				return in
			},
			Send: func(ctx context.Context, in any) (any, error) {
				return g.cl().GetAuthorizationToken(ctx, in.(*ecr.GetAuthorizationTokenInput))
			},
		},
		Assert: []scenario.Clause{
			scenario.ResponseField(
				scenario.Matches("$.authorizationData[0].authorizationToken", "^QVdTO[g-v](?:[A-Za-z0-9+/]=|[A-Za-z0-9+/]{2}(?:[A-Za-z0-9+/]{4})*(?:[A-Za-z0-9+/]{2}==|[A-Za-z0-9+/]{3}=)?)$"),
				scenario.NonEmpty("$.authorizationData[0].expiresAt"),
				scenario.Matches("$.authorizationData[0].proxyEndpoint", "^http"),
			),
		},
	})
}

func (g *authoredEcrRegistryScenarios) testEcrRegistryDescribeRegistry(ctx context.Context, t *harness.TestContext) error {
	return groupEcrRegistry.RunTest(ctx, t, "DescribeRegistry", scenario.Test{
		Call: scenario.Call{
			Op:     "DescribeRegistry",
			Params: `{}`,
			Build: func(b *scenario.Binder) any {
				in := &ecr.DescribeRegistryInput{}
				return in
			},
			Send: func(ctx context.Context, in any) (any, error) {
				return g.cl().DescribeRegistry(ctx, in.(*ecr.DescribeRegistryInput))
			},
		},
		Assert: []scenario.Clause{
			scenario.ResponseField(
				scenario.NonEmpty("$.registryId"),
				scenario.NonEmpty("$.replicationConfiguration"),
				scenario.IsList("$.replicationConfiguration.rules"),
			),
		},
	})
}
