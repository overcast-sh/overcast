// Code generated by cmd/compatgen; DO NOT EDIT.
//
// Generated from compat/model/authored/appconfig-configuration-profiles.json by cmd/compatgen.
// The semantics live in crate::scenario; this file is the data and the typed
// SDK calls.

use std::collections::HashMap;
use std::sync::Arc;

use crate::clients::AwsClients;
use crate::groups::ServiceGroup;
use crate::harness::{TestContext, TestFn};
use crate::scenario::{self, Call, Group, Test};

/// The scenario file every group in this file was generated from.
const SCENARIO_FILE: &str = "compat/model/authored/appconfig-configuration-profiles.json";

const GROUP_APPCONFIG_CONFIGURATION_PROFILES_SHADOW: Group = Group {
    name: "appconfig-configuration-profiles-shadow",
    file: SCENARIO_FILE,
};

/// The authored appconfig-configuration-profiles groups.
///
/// The client is built once, from the config the suite's hand-written groups
/// share, and cloned into every test — a generated group builds its own rather
/// than adding an accessor to AwsClients for every service the generator learns
/// to cover; nothing else about the client differs.
pub struct ScenariosAuthoredAppconfigConfigurationProfiles {
    client: aws_sdk_appconfig::Client,
}

impl ScenariosAuthoredAppconfigConfigurationProfiles {
    pub fn new(clients: &Arc<AwsClients>) -> Self {
        let config = aws_sdk_appconfig::config::Builder::from(clients.config())
            .endpoint_url(clients.endpoint())
            .build();
        Self {
            client: aws_sdk_appconfig::Client::from_conf(config),
        }
    }
}

impl ServiceGroup for ScenariosAuthoredAppconfigConfigurationProfiles {
    fn name(&self) -> &'static str {
        "scenarios/authored-appconfig-configuration-profiles"
    }

    fn impls(&self) -> HashMap<String, TestFn> {
        let mut impls: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            impls.insert(
                "appconfig-configuration-profiles-shadow:CreateConfigurationProfile".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_APPCONFIG_CONFIGURATION_PROFILES_SHADOW
                            .run_test(&ctx, "CreateConfigurationProfile", test_appconfig_configuration_profiles_shadow_create_configuration_profile(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "appconfig-configuration-profiles-shadow:GetConfigurationProfile".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_APPCONFIG_CONFIGURATION_PROFILES_SHADOW
                            .run_test(&ctx, "GetConfigurationProfile", test_appconfig_configuration_profiles_shadow_get_configuration_profile(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "appconfig-configuration-profiles-shadow:ListConfigurationProfiles".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_APPCONFIG_CONFIGURATION_PROFILES_SHADOW
                            .run_test(&ctx, "ListConfigurationProfiles", test_appconfig_configuration_profiles_shadow_list_configuration_profiles(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "appconfig-configuration-profiles-shadow:ListConfigurationProfilesByType".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_APPCONFIG_CONFIGURATION_PROFILES_SHADOW
                            .run_test(&ctx, "ListConfigurationProfilesByType", test_appconfig_configuration_profiles_shadow_list_configuration_profiles_by_type(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "appconfig-configuration-profiles-shadow:UpdateConfigurationProfile".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_APPCONFIG_CONFIGURATION_PROFILES_SHADOW
                            .run_test(&ctx, "UpdateConfigurationProfile", test_appconfig_configuration_profiles_shadow_update_configuration_profile(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "appconfig-configuration-profiles-shadow:DeleteConfigurationProfile".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_APPCONFIG_CONFIGURATION_PROFILES_SHADOW
                            .run_test(&ctx, "DeleteConfigurationProfile", test_appconfig_configuration_profiles_shadow_delete_configuration_profile(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "appconfig-configuration-profiles-shadow:GetConfigurationProfileNotFound".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_APPCONFIG_CONFIGURATION_PROFILES_SHADOW
                            .run_test(&ctx, "GetConfigurationProfileNotFound", test_appconfig_configuration_profiles_shadow_get_configuration_profile_not_found(&client))
                            .await
                    })
                }),
            );
        }
        impls
    }

    fn setups(&self) -> HashMap<String, TestFn> {
        let mut setups: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            setups.insert(
                "appconfig-configuration-profiles-shadow".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_APPCONFIG_CONFIGURATION_PROFILES_SHADOW.run_setup(&ctx, setup_appconfig_configuration_profiles_shadow(&client)).await
                    })
                }),
            );
        }
        setups
    }

    fn teardowns(&self) -> HashMap<String, TestFn> {
        let mut teardowns: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            teardowns.insert(
                "appconfig-configuration-profiles-shadow".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_APPCONFIG_CONFIGURATION_PROFILES_SHADOW.run_teardown(&ctx, teardown_appconfig_configuration_profiles_shadow(&client)).await
                    })
                }),
            );
        }
        teardowns
    }
}

fn setup_appconfig_configuration_profiles_shadow(client: &aws_sdk_appconfig::Client) -> Vec<Call> {
    vec![
        Call {
            op: "CreateApplication",
            params: scenario::map(vec![("Name", scenario::name("app"))]),
            export: vec![
                ("app.id", "$.Id"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_application()
                            .name(b.string("Name")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "CreateConfigurationProfile",
            params: scenario::map(vec![
                ("ApplicationId", scenario::context("app.id")),
                ("LocationUri", scenario::lit(::serde_json::json!("hosted"))),
                ("Name", scenario::name("flags")),
                ("Type", scenario::lit(::serde_json::json!("AWS.AppConfig.FeatureFlags"))),
            ]),
            export: vec![
                ("flags.id", "$.Id"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_configuration_profile()
                            .application_id(b.string("ApplicationId")?)
                            .location_uri("hosted")
                            .name(b.string("Name")?)
                            .r#type("AWS.AppConfig.FeatureFlags")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn teardown_appconfig_configuration_profiles_shadow(client: &aws_sdk_appconfig::Client) -> Vec<Call> {
    vec![
        Call {
            op: "DeleteConfigurationProfile",
            params: scenario::map(vec![
                ("ApplicationId", scenario::context("app.id")),
                ("ConfigurationProfileId", scenario::context("prof.id")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_configuration_profile()
                            .application_id(b.string("ApplicationId")?)
                            .configuration_profile_id(b.string("ConfigurationProfileId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "DeleteConfigurationProfile",
            params: scenario::map(vec![
                ("ApplicationId", scenario::context("app.id")),
                ("ConfigurationProfileId", scenario::context("flags.id")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_configuration_profile()
                            .application_id(b.string("ApplicationId")?)
                            .configuration_profile_id(b.string("ConfigurationProfileId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "DeleteApplication",
            params: scenario::map(vec![
                ("ApplicationId", scenario::context("app.id")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_application()
                            .application_id(b.string("ApplicationId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn test_appconfig_configuration_profiles_shadow_create_configuration_profile(client: &aws_sdk_appconfig::Client) -> Test {
    Test {
        call: Call {
            op: "CreateConfigurationProfile",
            params: scenario::map(vec![
                ("ApplicationId", scenario::context("app.id")),
                ("Description", scenario::lit(::serde_json::json!("compat configuration profiles group"))),
                ("LocationUri", scenario::lit(::serde_json::json!("hosted"))),
                ("Name", scenario::name("prof")),
                ("Type", scenario::lit(::serde_json::json!("AWS.Freeform"))),
            ]),
            export: vec![
                ("prof.id", "$.Id"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_configuration_profile()
                            .application_id(b.string("ApplicationId")?)
                            .description("compat configuration profiles group")
                            .location_uri("hosted")
                            .name(b.string("Name")?)
                            .r#type("AWS.Freeform")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.Id"),
                scenario::equals("$.LocationUri", scenario::lit(::serde_json::json!("hosted"))),
                scenario::equals("$.Name", scenario::name("prof")),
            ]),
            scenario::readback(
                Call {
                    op: "GetConfigurationProfile",
                    params: scenario::map(vec![
                        ("ApplicationId", scenario::context("app.id")),
                        ("ConfigurationProfileId", scenario::context("prof.id")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .get_configuration_profile()
                                    .application_id(b.string("ApplicationId")?)
                                    .configuration_profile_id(b.string("ConfigurationProfileId")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                vec![
                    scenario::equals("$.Name", scenario::name("prof")),
                    scenario::equals("$.Type", scenario::lit(::serde_json::json!("AWS.Freeform"))),
                ],
            ),
        ],
    }
}

fn test_appconfig_configuration_profiles_shadow_get_configuration_profile(client: &aws_sdk_appconfig::Client) -> Test {
    Test {
        call: Call {
            op: "GetConfigurationProfile",
            params: scenario::map(vec![
                ("ApplicationId", scenario::context("app.id")),
                ("ConfigurationProfileId", scenario::context("prof.id")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_configuration_profile()
                            .application_id(b.string("ApplicationId")?)
                            .configuration_profile_id(b.string("ConfigurationProfileId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.Id", scenario::context("prof.id")),
                scenario::equals("$.Name", scenario::name("prof")),
            ]),
        ],
    }
}

fn test_appconfig_configuration_profiles_shadow_list_configuration_profiles(client: &aws_sdk_appconfig::Client) -> Test {
    Test {
        call: Call {
            op: "ListConfigurationProfiles",
            params: scenario::map(vec![
                ("ApplicationId", scenario::context("app.id")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_configuration_profiles()
                            .application_id(b.string("ApplicationId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.Items",
                vec![
                    scenario::where_entry("$.Id", scenario::context("prof.id")),
                    scenario::where_entry("$.Name", scenario::name("prof")),
                ],
            ),
        ],
    }
}

fn test_appconfig_configuration_profiles_shadow_list_configuration_profiles_by_type(client: &aws_sdk_appconfig::Client) -> Test {
    Test {
        call: Call {
            op: "ListConfigurationProfiles",
            params: scenario::map(vec![
                ("ApplicationId", scenario::context("app.id")),
                ("Type", scenario::lit(::serde_json::json!("AWS.AppConfig.FeatureFlags"))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_configuration_profiles()
                            .application_id(b.string("ApplicationId")?)
                            .r#type("AWS.AppConfig.FeatureFlags")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.Items",
                vec![
                    scenario::where_entry("$.Id", scenario::context("flags.id")),
                ],
            ),
            scenario::absent_from_list(
                None,
                "$.Items",
                vec![
                    scenario::where_entry("$.Id", scenario::context("prof.id")),
                ],
            ),
        ],
    }
}

fn test_appconfig_configuration_profiles_shadow_update_configuration_profile(client: &aws_sdk_appconfig::Client) -> Test {
    Test {
        call: Call {
            op: "UpdateConfigurationProfile",
            params: scenario::map(vec![
                ("ApplicationId", scenario::context("app.id")),
                ("ConfigurationProfileId", scenario::context("prof.id")),
                ("Description", scenario::lit(::serde_json::json!("updated by compat"))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .update_configuration_profile()
                            .application_id(b.string("ApplicationId")?)
                            .configuration_profile_id(b.string("ConfigurationProfileId")?)
                            .description("updated by compat")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.Description", scenario::lit(::serde_json::json!("updated by compat"))),
            ]),
            scenario::readback(
                Call {
                    op: "GetConfigurationProfile",
                    params: scenario::map(vec![
                        ("ApplicationId", scenario::context("app.id")),
                        ("ConfigurationProfileId", scenario::context("prof.id")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .get_configuration_profile()
                                    .application_id(b.string("ApplicationId")?)
                                    .configuration_profile_id(b.string("ConfigurationProfileId")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                vec![
                    scenario::equals("$.Description", scenario::lit(::serde_json::json!("updated by compat"))),
                    scenario::equals("$.Name", scenario::name("prof")),
                ],
            ),
        ],
    }
}

fn test_appconfig_configuration_profiles_shadow_delete_configuration_profile(client: &aws_sdk_appconfig::Client) -> Test {
    Test {
        call: Call {
            op: "DeleteConfigurationProfile",
            params: scenario::map(vec![
                ("ApplicationId", scenario::context("app.id")),
                ("ConfigurationProfileId", scenario::context("prof.id")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_configuration_profile()
                            .application_id(b.string("ApplicationId")?)
                            .configuration_profile_id(b.string("ConfigurationProfileId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::absent_by_error(
                Call {
                    op: "GetConfigurationProfile",
                    params: scenario::map(vec![
                        ("ApplicationId", scenario::context("app.id")),
                        ("ConfigurationProfileId", scenario::context("prof.id")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .get_configuration_profile()
                                    .application_id(b.string("ApplicationId")?)
                                    .configuration_profile_id(b.string("ConfigurationProfileId")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                scenario::error("ResourceNotFoundException", "ResourceNotFoundException"),
            ),
            scenario::absent_from_list(
                Some(Call {
                    op: "ListConfigurationProfiles",
                    params: scenario::map(vec![
                        ("ApplicationId", scenario::context("app.id")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_configuration_profiles()
                                    .application_id(b.string("ApplicationId")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                }),
                "$.Items",
                vec![
                    scenario::where_entry("$.Id", scenario::context("prof.id")),
                ],
            ),
        ],
    }
}

fn test_appconfig_configuration_profiles_shadow_get_configuration_profile_not_found(client: &aws_sdk_appconfig::Client) -> Test {
    Test {
        call: Call {
            op: "GetConfigurationProfile",
            params: scenario::map(vec![
                ("ApplicationId", scenario::context("app.id")),
                ("ConfigurationProfileId", scenario::lit(::serde_json::json!("zzzzzzz"))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_configuration_profile()
                            .application_id(b.string("ApplicationId")?)
                            .configuration_profile_id("zzzzzzz")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::error_code(scenario::error("ResourceNotFoundException", "ResourceNotFoundException")),
        ],
    }
}
