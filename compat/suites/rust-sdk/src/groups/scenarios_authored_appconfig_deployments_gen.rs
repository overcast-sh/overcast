// Code generated by cmd/compatgen; DO NOT EDIT.
//
// Generated from compat/model/authored/appconfig-deployments.json by cmd/compatgen.
// The semantics live in crate::scenario; this file is the data and the typed
// SDK calls.

use std::collections::HashMap;
use std::sync::Arc;

use crate::clients::AwsClients;
use crate::groups::ServiceGroup;
use crate::harness::{TestContext, TestFn};
use crate::scenario::{self, Call, Group, Test};

/// The scenario file every group in this file was generated from.
const SCENARIO_FILE: &str = "compat/model/authored/appconfig-deployments.json";

const GROUP_APPCONFIG_DEPLOYMENTS: Group = Group {
    name: "appconfig-deployments",
    file: SCENARIO_FILE,
};

/// The authored appconfig-deployments groups.
///
/// The client is built once, from the config the suite's hand-written groups
/// share, and cloned into every test — a generated group builds its own rather
/// than adding an accessor to AwsClients for every service the generator learns
/// to cover; nothing else about the client differs.
pub struct ScenariosAuthoredAppconfigDeployments {
    client: aws_sdk_appconfig::Client,
}

impl ScenariosAuthoredAppconfigDeployments {
    pub fn new(clients: &Arc<AwsClients>) -> Self {
        let config = aws_sdk_appconfig::config::Builder::from(clients.config())
            .endpoint_url(clients.endpoint())
            .build();
        Self {
            client: aws_sdk_appconfig::Client::from_conf(config),
        }
    }
}

impl ServiceGroup for ScenariosAuthoredAppconfigDeployments {
    fn name(&self) -> &'static str {
        "scenarios/authored-appconfig-deployments"
    }

    fn impls(&self) -> HashMap<String, TestFn> {
        let mut impls: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            impls.insert(
                "appconfig-deployments:StartDeployment".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_APPCONFIG_DEPLOYMENTS
                            .run_test(&ctx, "StartDeployment", test_appconfig_deployments_start_deployment(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "appconfig-deployments:GetDeployment".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_APPCONFIG_DEPLOYMENTS
                            .run_test(&ctx, "GetDeployment", test_appconfig_deployments_get_deployment(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "appconfig-deployments:ListDeployments".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_APPCONFIG_DEPLOYMENTS
                            .run_test(&ctx, "ListDeployments", test_appconfig_deployments_list_deployments(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "appconfig-deployments:StopDeployment".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_APPCONFIG_DEPLOYMENTS
                            .run_test(&ctx, "StopDeployment", test_appconfig_deployments_stop_deployment(&client))
                            .await
                    })
                }),
            );
        }
        impls
    }

    fn setups(&self) -> HashMap<String, TestFn> {
        let mut setups: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            setups.insert(
                "appconfig-deployments".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_APPCONFIG_DEPLOYMENTS.run_setup(&ctx, setup_appconfig_deployments(&client)).await
                    })
                }),
            );
        }
        setups
    }

    fn teardowns(&self) -> HashMap<String, TestFn> {
        let mut teardowns: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            teardowns.insert(
                "appconfig-deployments".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_APPCONFIG_DEPLOYMENTS.run_teardown(&ctx, teardown_appconfig_deployments(&client)).await
                    })
                }),
            );
        }
        teardowns
    }
}

fn setup_appconfig_deployments(client: &aws_sdk_appconfig::Client) -> Vec<Call> {
    vec![
        Call {
            op: "CreateApplication",
            params: scenario::map(vec![("Name", scenario::name("app"))]),
            export: vec![
                ("app.id", "$.Id"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_application()
                            .name(b.string("Name")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "CreateEnvironment",
            params: scenario::map(vec![
                ("ApplicationId", scenario::context("app.id")),
                ("Name", scenario::name("env")),
            ]),
            export: vec![
                ("env.id", "$.Id"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_environment()
                            .application_id(b.string("ApplicationId")?)
                            .name(b.string("Name")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "CreateConfigurationProfile",
            params: scenario::map(vec![
                ("ApplicationId", scenario::context("app.id")),
                ("LocationUri", scenario::lit(::serde_json::json!("hosted"))),
                ("Name", scenario::name("prof")),
            ]),
            export: vec![
                ("prof.id", "$.Id"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_configuration_profile()
                            .application_id(b.string("ApplicationId")?)
                            .location_uri("hosted")
                            .name(b.string("Name")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn teardown_appconfig_deployments(client: &aws_sdk_appconfig::Client) -> Vec<Call> {
    vec![
        Call {
            op: "DeleteConfigurationProfile",
            params: scenario::map(vec![
                ("ApplicationId", scenario::context("app.id")),
                ("ConfigurationProfileId", scenario::context("prof.id")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_configuration_profile()
                            .application_id(b.string("ApplicationId")?)
                            .configuration_profile_id(b.string("ConfigurationProfileId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "DeleteEnvironment",
            params: scenario::map(vec![
                ("ApplicationId", scenario::context("app.id")),
                ("EnvironmentId", scenario::context("env.id")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_environment()
                            .application_id(b.string("ApplicationId")?)
                            .environment_id(b.string("EnvironmentId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "DeleteApplication",
            params: scenario::map(vec![
                ("ApplicationId", scenario::context("app.id")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_application()
                            .application_id(b.string("ApplicationId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn test_appconfig_deployments_start_deployment(client: &aws_sdk_appconfig::Client) -> Test {
    Test {
        call: Call {
            op: "StartDeployment",
            params: scenario::map(vec![
                ("ApplicationId", scenario::context("app.id")),
                ("ConfigurationProfileId", scenario::context("prof.id")),
                ("ConfigurationVersion", scenario::lit(::serde_json::json!("1"))),
                ("DeploymentStrategyId", scenario::lit(::serde_json::json!("AppConfig.AllAtOnce"))),
                ("EnvironmentId", scenario::context("env.id")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .start_deployment()
                            .application_id(b.string("ApplicationId")?)
                            .configuration_profile_id(b.string("ConfigurationProfileId")?)
                            .configuration_version("1")
                            .deployment_strategy_id("AppConfig.AllAtOnce")
                            .environment_id(b.string("EnvironmentId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.DeploymentNumber"),
            ]),
        ],
    }
}

fn test_appconfig_deployments_get_deployment(client: &aws_sdk_appconfig::Client) -> Test {
    Test {
        call: Call {
            op: "GetDeployment",
            params: scenario::map(vec![
                ("ApplicationId", scenario::context("app.id")),
                ("DeploymentNumber", scenario::lit(::serde_json::json!(1))),
                ("EnvironmentId", scenario::context("env.id")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_deployment()
                            .application_id(b.string("ApplicationId")?)
                            .deployment_number(1)
                            .environment_id(b.string("EnvironmentId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.DeploymentNumber", scenario::lit(::serde_json::json!(1))),
            ]),
        ],
    }
}

fn test_appconfig_deployments_list_deployments(client: &aws_sdk_appconfig::Client) -> Test {
    Test {
        call: Call {
            op: "ListDeployments",
            params: scenario::map(vec![
                ("ApplicationId", scenario::context("app.id")),
                ("EnvironmentId", scenario::context("env.id")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_deployments()
                            .application_id(b.string("ApplicationId")?)
                            .environment_id(b.string("EnvironmentId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::is_list("$.Items"),
            ]),
        ],
    }
}

fn test_appconfig_deployments_stop_deployment(client: &aws_sdk_appconfig::Client) -> Test {
    Test {
        call: Call {
            op: "StopDeployment",
            params: scenario::map(vec![
                ("ApplicationId", scenario::context("app.id")),
                ("DeploymentNumber", scenario::lit(::serde_json::json!(1))),
                ("EnvironmentId", scenario::context("env.id")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .stop_deployment()
                            .application_id(b.string("ApplicationId")?)
                            .deployment_number(1)
                            .environment_id(b.string("EnvironmentId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.State"),
            ]),
        ],
    }
}
