// Code generated by cmd/compatgen; DO NOT EDIT.
//
// Generated from compat/model/authored/appconfig-environments.json by cmd/compatgen.
// The semantics live in crate::scenario; this file is the data and the typed
// SDK calls.

use std::collections::HashMap;
use std::sync::Arc;

use crate::clients::AwsClients;
use crate::groups::ServiceGroup;
use crate::harness::{TestContext, TestFn};
use crate::scenario::{self, Call, Group, Test};

/// The scenario file every group in this file was generated from.
const SCENARIO_FILE: &str = "compat/model/authored/appconfig-environments.json";

const GROUP_APPCONFIG_ENVIRONMENTS_SHADOW: Group = Group {
    name: "appconfig-environments-shadow",
    file: SCENARIO_FILE,
};

/// The authored appconfig-environments groups.
///
/// The client is built once, from the config the suite's hand-written groups
/// share, and cloned into every test — a generated group builds its own rather
/// than adding an accessor to AwsClients for every service the generator learns
/// to cover; nothing else about the client differs.
pub struct ScenariosAuthoredAppconfigEnvironments {
    client: aws_sdk_appconfig::Client,
}

impl ScenariosAuthoredAppconfigEnvironments {
    pub fn new(clients: &Arc<AwsClients>) -> Self {
        let config = aws_sdk_appconfig::config::Builder::from(clients.config())
            .endpoint_url(clients.endpoint())
            .build();
        Self {
            client: aws_sdk_appconfig::Client::from_conf(config),
        }
    }
}

impl ServiceGroup for ScenariosAuthoredAppconfigEnvironments {
    fn name(&self) -> &'static str {
        "scenarios/authored-appconfig-environments"
    }

    fn impls(&self) -> HashMap<String, TestFn> {
        let mut impls: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            impls.insert(
                "appconfig-environments-shadow:CreateEnvironment".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_APPCONFIG_ENVIRONMENTS_SHADOW
                            .run_test(&ctx, "CreateEnvironment", test_appconfig_environments_shadow_create_environment(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "appconfig-environments-shadow:GetEnvironment".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_APPCONFIG_ENVIRONMENTS_SHADOW
                            .run_test(&ctx, "GetEnvironment", test_appconfig_environments_shadow_get_environment(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "appconfig-environments-shadow:ListEnvironments".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_APPCONFIG_ENVIRONMENTS_SHADOW
                            .run_test(&ctx, "ListEnvironments", test_appconfig_environments_shadow_list_environments(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "appconfig-environments-shadow:UpdateEnvironment".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_APPCONFIG_ENVIRONMENTS_SHADOW
                            .run_test(&ctx, "UpdateEnvironment", test_appconfig_environments_shadow_update_environment(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "appconfig-environments-shadow:DeleteEnvironment".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_APPCONFIG_ENVIRONMENTS_SHADOW
                            .run_test(&ctx, "DeleteEnvironment", test_appconfig_environments_shadow_delete_environment(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "appconfig-environments-shadow:GetEnvironmentNotFound".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_APPCONFIG_ENVIRONMENTS_SHADOW
                            .run_test(&ctx, "GetEnvironmentNotFound", test_appconfig_environments_shadow_get_environment_not_found(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "appconfig-environments-shadow:ListEnvironmentsApplicationNotFound".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_APPCONFIG_ENVIRONMENTS_SHADOW
                            .run_test(&ctx, "ListEnvironmentsApplicationNotFound", test_appconfig_environments_shadow_list_environments_application_not_found(&client))
                            .await
                    })
                }),
            );
        }
        impls
    }

    fn setups(&self) -> HashMap<String, TestFn> {
        let mut setups: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            setups.insert(
                "appconfig-environments-shadow".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_APPCONFIG_ENVIRONMENTS_SHADOW.run_setup(&ctx, setup_appconfig_environments_shadow(&client)).await
                    })
                }),
            );
        }
        setups
    }

    fn teardowns(&self) -> HashMap<String, TestFn> {
        let mut teardowns: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            teardowns.insert(
                "appconfig-environments-shadow".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_APPCONFIG_ENVIRONMENTS_SHADOW.run_teardown(&ctx, teardown_appconfig_environments_shadow(&client)).await
                    })
                }),
            );
        }
        teardowns
    }
}

fn setup_appconfig_environments_shadow(client: &aws_sdk_appconfig::Client) -> Vec<Call> {
    vec![
        Call {
            op: "CreateApplication",
            params: scenario::map(vec![("Name", scenario::name("app"))]),
            export: vec![
                ("app.id", "$.Id"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_application()
                            .name(b.string("Name")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn teardown_appconfig_environments_shadow(client: &aws_sdk_appconfig::Client) -> Vec<Call> {
    vec![
        Call {
            op: "DeleteEnvironment",
            params: scenario::map(vec![
                ("ApplicationId", scenario::context("app.id")),
                ("EnvironmentId", scenario::context("env.id")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_environment()
                            .application_id(b.string("ApplicationId")?)
                            .environment_id(b.string("EnvironmentId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "DeleteApplication",
            params: scenario::map(vec![
                ("ApplicationId", scenario::context("app.id")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_application()
                            .application_id(b.string("ApplicationId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn test_appconfig_environments_shadow_create_environment(client: &aws_sdk_appconfig::Client) -> Test {
    Test {
        call: Call {
            op: "CreateEnvironment",
            params: scenario::map(vec![
                ("ApplicationId", scenario::context("app.id")),
                ("Description", scenario::lit(::serde_json::json!("compat environments group"))),
                ("Name", scenario::name("env")),
            ]),
            export: vec![
                ("env.id", "$.Id"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_environment()
                            .application_id(b.string("ApplicationId")?)
                            .description("compat environments group")
                            .name(b.string("Name")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.ApplicationId", scenario::context("app.id")),
                scenario::non_empty("$.Id"),
                scenario::equals("$.Name", scenario::name("env")),
                scenario::equals("$.State", scenario::lit(::serde_json::json!("READY_FOR_DEPLOYMENT"))),
            ]),
            scenario::readback(
                Call {
                    op: "GetEnvironment",
                    params: scenario::map(vec![
                        ("ApplicationId", scenario::context("app.id")),
                        ("EnvironmentId", scenario::context("env.id")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .get_environment()
                                    .application_id(b.string("ApplicationId")?)
                                    .environment_id(b.string("EnvironmentId")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                vec![
                    scenario::equals("$.Name", scenario::name("env")),
                ],
            ),
        ],
    }
}

fn test_appconfig_environments_shadow_get_environment(client: &aws_sdk_appconfig::Client) -> Test {
    Test {
        call: Call {
            op: "GetEnvironment",
            params: scenario::map(vec![
                ("ApplicationId", scenario::context("app.id")),
                ("EnvironmentId", scenario::context("env.id")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_environment()
                            .application_id(b.string("ApplicationId")?)
                            .environment_id(b.string("EnvironmentId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.Id", scenario::context("env.id")),
                scenario::equals("$.Name", scenario::name("env")),
            ]),
        ],
    }
}

fn test_appconfig_environments_shadow_list_environments(client: &aws_sdk_appconfig::Client) -> Test {
    Test {
        call: Call {
            op: "ListEnvironments",
            params: scenario::map(vec![
                ("ApplicationId", scenario::context("app.id")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_environments()
                            .application_id(b.string("ApplicationId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.Items",
                vec![
                    scenario::where_entry("$.Id", scenario::context("env.id")),
                    scenario::where_entry("$.Name", scenario::name("env")),
                ],
            ),
        ],
    }
}

fn test_appconfig_environments_shadow_update_environment(client: &aws_sdk_appconfig::Client) -> Test {
    Test {
        call: Call {
            op: "UpdateEnvironment",
            params: scenario::map(vec![
                ("ApplicationId", scenario::context("app.id")),
                ("Description", scenario::lit(::serde_json::json!("updated by compat"))),
                ("EnvironmentId", scenario::context("env.id")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .update_environment()
                            .application_id(b.string("ApplicationId")?)
                            .description("updated by compat")
                            .environment_id(b.string("EnvironmentId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.Description", scenario::lit(::serde_json::json!("updated by compat"))),
            ]),
            scenario::readback(
                Call {
                    op: "GetEnvironment",
                    params: scenario::map(vec![
                        ("ApplicationId", scenario::context("app.id")),
                        ("EnvironmentId", scenario::context("env.id")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .get_environment()
                                    .application_id(b.string("ApplicationId")?)
                                    .environment_id(b.string("EnvironmentId")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                vec![
                    scenario::equals("$.Description", scenario::lit(::serde_json::json!("updated by compat"))),
                    scenario::equals("$.Name", scenario::name("env")),
                ],
            ),
        ],
    }
}

fn test_appconfig_environments_shadow_delete_environment(client: &aws_sdk_appconfig::Client) -> Test {
    Test {
        call: Call {
            op: "DeleteEnvironment",
            params: scenario::map(vec![
                ("ApplicationId", scenario::context("app.id")),
                ("EnvironmentId", scenario::context("env.id")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_environment()
                            .application_id(b.string("ApplicationId")?)
                            .environment_id(b.string("EnvironmentId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::absent_by_error(
                Call {
                    op: "GetEnvironment",
                    params: scenario::map(vec![
                        ("ApplicationId", scenario::context("app.id")),
                        ("EnvironmentId", scenario::context("env.id")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .get_environment()
                                    .application_id(b.string("ApplicationId")?)
                                    .environment_id(b.string("EnvironmentId")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                scenario::error("ResourceNotFoundException", "ResourceNotFoundException"),
            ),
            scenario::absent_from_list(
                Some(Call {
                    op: "ListEnvironments",
                    params: scenario::map(vec![
                        ("ApplicationId", scenario::context("app.id")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_environments()
                                    .application_id(b.string("ApplicationId")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                }),
                "$.Items",
                vec![
                    scenario::where_entry("$.Id", scenario::context("env.id")),
                ],
            ),
        ],
    }
}

fn test_appconfig_environments_shadow_get_environment_not_found(client: &aws_sdk_appconfig::Client) -> Test {
    Test {
        call: Call {
            op: "GetEnvironment",
            params: scenario::map(vec![
                ("ApplicationId", scenario::context("app.id")),
                ("EnvironmentId", scenario::lit(::serde_json::json!("zzzzzzz"))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_environment()
                            .application_id(b.string("ApplicationId")?)
                            .environment_id("zzzzzzz")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::error_code(scenario::error("ResourceNotFoundException", "ResourceNotFoundException")),
        ],
    }
}

fn test_appconfig_environments_shadow_list_environments_application_not_found(client: &aws_sdk_appconfig::Client) -> Test {
    Test {
        call: Call {
            op: "ListEnvironments",
            params: scenario::lit(::serde_json::json!({"ApplicationId": "zzzzzzz"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_environments()
                            .application_id("zzzzzzz")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::error_code(scenario::error("ResourceNotFoundException", "ResourceNotFoundException")),
        ],
    }
}
