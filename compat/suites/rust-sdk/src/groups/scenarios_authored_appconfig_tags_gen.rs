// Code generated by cmd/compatgen; DO NOT EDIT.
//
// Generated from compat/model/authored/appconfig-tags.json by cmd/compatgen.
// The semantics live in crate::scenario; this file is the data and the typed
// SDK calls.

use std::collections::HashMap;
use std::sync::Arc;

use crate::clients::AwsClients;
use crate::groups::ServiceGroup;
use crate::harness::{TestContext, TestFn};
use crate::scenario::{self, Call, Group, Test};

/// The scenario file every group in this file was generated from.
const SCENARIO_FILE: &str = "compat/model/authored/appconfig-tags.json";

const GROUP_APPCONFIG_TAGS: Group = Group {
    name: "appconfig-tags",
    file: SCENARIO_FILE,
};

/// The authored appconfig-tags groups.
///
/// The client is built once, from the config the suite's hand-written groups
/// share, and cloned into every test — a generated group builds its own rather
/// than adding an accessor to AwsClients for every service the generator learns
/// to cover; nothing else about the client differs.
pub struct ScenariosAuthoredAppconfigTags {
    client: aws_sdk_appconfig::Client,
}

impl ScenariosAuthoredAppconfigTags {
    pub fn new(clients: &Arc<AwsClients>) -> Self {
        let config = aws_sdk_appconfig::config::Builder::from(clients.config())
            .endpoint_url(clients.endpoint())
            .build();
        Self {
            client: aws_sdk_appconfig::Client::from_conf(config),
        }
    }
}

impl ServiceGroup for ScenariosAuthoredAppconfigTags {
    fn name(&self) -> &'static str {
        "scenarios/authored-appconfig-tags"
    }

    fn impls(&self) -> HashMap<String, TestFn> {
        let mut impls: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            impls.insert(
                "appconfig-tags:CreateApplicationWithTags".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_APPCONFIG_TAGS
                            .run_test(&ctx, "CreateApplicationWithTags", test_appconfig_tags_create_application_with_tags(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "appconfig-tags:TagResource".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_APPCONFIG_TAGS
                            .run_test(&ctx, "TagResource", test_appconfig_tags_tag_resource(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "appconfig-tags:ListTagsForResource".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_APPCONFIG_TAGS
                            .run_test(&ctx, "ListTagsForResource", test_appconfig_tags_list_tags_for_resource(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "appconfig-tags:UntagResource".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_APPCONFIG_TAGS
                            .run_test(&ctx, "UntagResource", test_appconfig_tags_untag_resource(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "appconfig-tags:ListTagsForResourceNotFound".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_APPCONFIG_TAGS
                            .run_test(&ctx, "ListTagsForResourceNotFound", test_appconfig_tags_list_tags_for_resource_not_found(&client))
                            .await
                    })
                }),
            );
        }
        impls
    }

    fn setups(&self) -> HashMap<String, TestFn> {
        let mut setups: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            setups.insert(
                "appconfig-tags".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_APPCONFIG_TAGS.run_setup(&ctx, setup_appconfig_tags(&client)).await
                    })
                }),
            );
        }
        setups
    }

    fn teardowns(&self) -> HashMap<String, TestFn> {
        let mut teardowns: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            teardowns.insert(
                "appconfig-tags".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_APPCONFIG_TAGS.run_teardown(&ctx, teardown_appconfig_tags(&client)).await
                    })
                }),
            );
        }
        teardowns
    }
}

fn setup_appconfig_tags(_client: &aws_sdk_appconfig::Client) -> Vec<Call> {
    // An empty phase is a no-op, not a missing one.
    Vec::new()
}

fn teardown_appconfig_tags(client: &aws_sdk_appconfig::Client) -> Vec<Call> {
    vec![
        Call {
            op: "DeleteApplication",
            params: scenario::map(vec![
                ("ApplicationId", scenario::context("app.id")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_application()
                            .application_id(b.string("ApplicationId")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn test_appconfig_tags_create_application_with_tags(client: &aws_sdk_appconfig::Client) -> Test {
    Test {
        call: Call {
            op: "CreateApplication",
            params: scenario::map(vec![
                ("Name", scenario::name("app")),
                ("Tags", scenario::lit(::serde_json::json!({"Owner": "compat", "Team": "platform"}))),
            ]),
            export: vec![
                ("app.id", "$.Id"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_application()
                            .name(b.string("Name")?)
                            .tags("Owner", "compat")
                            .tags("Team", "platform")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.Id"),
            ]),
            scenario::readback(
                Call {
                    op: "ListTagsForResource",
                    params: scenario::map(vec![
                        ("ResourceArn", scenario::concat(vec![
                            scenario::lit(::serde_json::json!("arn:aws:appconfig:us-east-1:000000000000:application/")),
                            scenario::context("app.id"),
                        ])),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_tags_for_resource()
                                    .resource_arn(b.string("ResourceArn")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                vec![
                    scenario::equals("$.Tags.Owner", scenario::lit(::serde_json::json!("compat"))),
                    scenario::equals("$.Tags.Team", scenario::lit(::serde_json::json!("platform"))),
                ],
            ),
        ],
    }
}

fn test_appconfig_tags_tag_resource(client: &aws_sdk_appconfig::Client) -> Test {
    Test {
        call: Call {
            op: "TagResource",
            params: scenario::map(vec![
                ("ResourceArn", scenario::concat(vec![
                    scenario::lit(::serde_json::json!("arn:aws:appconfig:us-east-1:000000000000:application/")),
                    scenario::context("app.id"),
                ])),
                ("Tags", scenario::lit(::serde_json::json!({"Owner": "compat-updated", "Stage": "beta"}))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .tag_resource()
                            .resource_arn(b.string("ResourceArn")?)
                            .tags("Owner", "compat-updated")
                            .tags("Stage", "beta")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::readback(
                Call {
                    op: "ListTagsForResource",
                    params: scenario::map(vec![
                        ("ResourceArn", scenario::concat(vec![
                            scenario::lit(::serde_json::json!("arn:aws:appconfig:us-east-1:000000000000:application/")),
                            scenario::context("app.id"),
                        ])),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_tags_for_resource()
                                    .resource_arn(b.string("ResourceArn")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                vec![
                    scenario::equals("$.Tags.Owner", scenario::lit(::serde_json::json!("compat-updated"))),
                    scenario::equals("$.Tags.Stage", scenario::lit(::serde_json::json!("beta"))),
                    scenario::equals("$.Tags.Team", scenario::lit(::serde_json::json!("platform"))),
                ],
            ),
        ],
    }
}

fn test_appconfig_tags_list_tags_for_resource(client: &aws_sdk_appconfig::Client) -> Test {
    Test {
        call: Call {
            op: "ListTagsForResource",
            params: scenario::map(vec![
                ("ResourceArn", scenario::concat(vec![
                    scenario::lit(::serde_json::json!("arn:aws:appconfig:us-east-1:000000000000:application/")),
                    scenario::context("app.id"),
                ])),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_tags_for_resource()
                            .resource_arn(b.string("ResourceArn")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.Tags.Owner", scenario::lit(::serde_json::json!("compat-updated"))),
                scenario::equals("$.Tags.Stage", scenario::lit(::serde_json::json!("beta"))),
                scenario::equals("$.Tags.Team", scenario::lit(::serde_json::json!("platform"))),
            ]),
        ],
    }
}

fn test_appconfig_tags_untag_resource(client: &aws_sdk_appconfig::Client) -> Test {
    Test {
        call: Call {
            op: "UntagResource",
            params: scenario::map(vec![
                ("ResourceArn", scenario::concat(vec![
                    scenario::lit(::serde_json::json!("arn:aws:appconfig:us-east-1:000000000000:application/")),
                    scenario::context("app.id"),
                ])),
                ("TagKeys", scenario::lit(::serde_json::json!(["Team"]))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .untag_resource()
                            .resource_arn(b.string("ResourceArn")?)
                            .tag_keys("Team")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::readback(
                Call {
                    op: "ListTagsForResource",
                    params: scenario::map(vec![
                        ("ResourceArn", scenario::concat(vec![
                            scenario::lit(::serde_json::json!("arn:aws:appconfig:us-east-1:000000000000:application/")),
                            scenario::context("app.id"),
                        ])),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_tags_for_resource()
                                    .resource_arn(b.string("ResourceArn")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                vec![
                    scenario::equals("$.Tags.Owner", scenario::lit(::serde_json::json!("compat-updated"))),
                    scenario::missing("$.Tags.Team"),
                ],
            ),
        ],
    }
}

fn test_appconfig_tags_list_tags_for_resource_not_found(client: &aws_sdk_appconfig::Client) -> Test {
    Test {
        call: Call {
            op: "ListTagsForResource",
            params: scenario::lit(::serde_json::json!({"ResourceArn": "arn:aws:appconfig:us-east-1:000000000000:application/zzzzzzz"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_tags_for_resource()
                            .resource_arn("arn:aws:appconfig:us-east-1:000000000000:application/zzzzzzz")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::error_code(scenario::error("ResourceNotFoundException", "ResourceNotFoundException")),
        ],
    }
}
