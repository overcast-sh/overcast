// Code generated by cmd/compatgen; DO NOT EDIT.
//
// Generated from compat/model/authored/backup-tags.json by cmd/compatgen.
// The semantics live in crate::scenario; this file is the data and the typed
// SDK calls.

use std::collections::HashMap;
use std::sync::Arc;

use crate::clients::AwsClients;
use crate::groups::ServiceGroup;
use crate::harness::{TestContext, TestFn};
use crate::scenario::{self, Call, Group, Test};

/// The scenario file every group in this file was generated from.
const SCENARIO_FILE: &str = "compat/model/authored/backup-tags.json";

const GROUP_BACKUP_TAGS: Group = Group {
    name: "backup-tags",
    file: SCENARIO_FILE,
};

/// The authored backup-tags groups.
///
/// The client is built once, from the config the suite's hand-written groups
/// share, and cloned into every test — a generated group builds its own rather
/// than adding an accessor to AwsClients for every service the generator learns
/// to cover; nothing else about the client differs.
pub struct ScenariosAuthoredBackupTags {
    client: aws_sdk_backup::Client,
}

impl ScenariosAuthoredBackupTags {
    pub fn new(clients: &Arc<AwsClients>) -> Self {
        let config = aws_sdk_backup::config::Builder::from(clients.config())
            .endpoint_url(clients.endpoint())
            .build();
        Self {
            client: aws_sdk_backup::Client::from_conf(config),
        }
    }
}

impl ServiceGroup for ScenariosAuthoredBackupTags {
    fn name(&self) -> &'static str {
        "scenarios/authored-backup-tags"
    }

    fn impls(&self) -> HashMap<String, TestFn> {
        let mut impls: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            impls.insert(
                "backup-tags:TagResource".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BACKUP_TAGS
                            .run_test(&ctx, "TagResource", test_backup_tags_tag_resource(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "backup-tags:ListTags".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BACKUP_TAGS
                            .run_test(&ctx, "ListTags", test_backup_tags_list_tags(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "backup-tags:UntagResource".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BACKUP_TAGS
                            .run_test(&ctx, "UntagResource", test_backup_tags_untag_resource(&client))
                            .await
                    })
                }),
            );
        }
        impls
    }

    fn setups(&self) -> HashMap<String, TestFn> {
        let mut setups: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            setups.insert(
                "backup-tags".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BACKUP_TAGS.run_setup(&ctx, setup_backup_tags(&client)).await
                    })
                }),
            );
        }
        setups
    }

    fn teardowns(&self) -> HashMap<String, TestFn> {
        let mut teardowns: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            teardowns.insert(
                "backup-tags".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_BACKUP_TAGS.run_teardown(&ctx, teardown_backup_tags(&client)).await
                    })
                }),
            );
        }
        teardowns
    }
}

fn setup_backup_tags(client: &aws_sdk_backup::Client) -> Vec<Call> {
    vec![
        Call {
            op: "CreateBackupVault",
            params: scenario::map(vec![
                ("BackupVaultName", scenario::name("vault")),
            ]),
            export: vec![
                ("vault.arn", "$.BackupVaultArn"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_backup_vault()
                            .backup_vault_name(b.string("BackupVaultName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn teardown_backup_tags(client: &aws_sdk_backup::Client) -> Vec<Call> {
    vec![
        Call {
            op: "UntagResource",
            params: scenario::map(vec![
                ("ResourceArn", scenario::context("vault.arn")),
                ("TagKeyList", scenario::lit(::serde_json::json!(["oc-compat"]))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .untag_resource()
                            .resource_arn(b.string("ResourceArn")?)
                            .tag_key_list("oc-compat")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "DeleteBackupVault",
            params: scenario::map(vec![
                ("BackupVaultName", scenario::name("vault")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_backup_vault()
                            .backup_vault_name(b.string("BackupVaultName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn test_backup_tags_tag_resource(client: &aws_sdk_backup::Client) -> Test {
    Test {
        call: Call {
            op: "TagResource",
            params: scenario::map(vec![
                ("ResourceArn", scenario::context("vault.arn")),
                ("Tags", scenario::map(vec![("oc-compat", scenario::name("vault"))])),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .tag_resource()
                            .resource_arn(b.string("ResourceArn")?)
                            .tags("oc-compat", b.string("Tags.oc-compat")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::readback(
                Call {
                    op: "ListTags",
                    params: scenario::map(vec![
                        ("ResourceArn", scenario::context("vault.arn")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_tags()
                                    .resource_arn(b.string("ResourceArn")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                vec![
                    scenario::equals("$.Tags.oc-compat", scenario::name("vault")),
                ],
            ),
        ],
    }
}

fn test_backup_tags_list_tags(client: &aws_sdk_backup::Client) -> Test {
    Test {
        call: Call {
            op: "ListTags",
            params: scenario::map(vec![
                ("ResourceArn", scenario::context("vault.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_tags()
                            .resource_arn(b.string("ResourceArn")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.Tags.oc-compat", scenario::name("vault")),
            ]),
        ],
    }
}

fn test_backup_tags_untag_resource(client: &aws_sdk_backup::Client) -> Test {
    Test {
        call: Call {
            op: "UntagResource",
            params: scenario::map(vec![
                ("ResourceArn", scenario::context("vault.arn")),
                ("TagKeyList", scenario::lit(::serde_json::json!(["oc-compat"]))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .untag_resource()
                            .resource_arn(b.string("ResourceArn")?)
                            .tag_key_list("oc-compat")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::readback(
                Call {
                    op: "ListTags",
                    params: scenario::map(vec![
                        ("ResourceArn", scenario::context("vault.arn")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_tags()
                                    .resource_arn(b.string("ResourceArn")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                vec![
                    scenario::missing("$.Tags.oc-compat"),
                ],
            ),
        ],
    }
}
