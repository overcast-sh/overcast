// Code generated by cmd/compatgen; DO NOT EDIT.
//
// Generated from compat/model/authored/ecr-policies.json by cmd/compatgen.
// The semantics live in crate::scenario; this file is the data and the typed
// SDK calls.

use std::collections::HashMap;
use std::sync::Arc;

use crate::clients::AwsClients;
use crate::groups::ServiceGroup;
use crate::harness::{TestContext, TestFn};
use crate::scenario::{self, Call, Group, Test};

/// The scenario file every group in this file was generated from.
const SCENARIO_FILE: &str = "compat/model/authored/ecr-policies.json";

const GROUP_ECR_POLICIES: Group = Group {
    name: "ecr-policies",
    file: SCENARIO_FILE,
};

/// The authored ecr-policies groups.
///
/// The client is built once, from the config the suite's hand-written groups
/// share, and cloned into every test — a generated group builds its own rather
/// than adding an accessor to AwsClients for every service the generator learns
/// to cover; nothing else about the client differs.
pub struct ScenariosAuthoredEcrPolicies {
    client: aws_sdk_ecr::Client,
}

impl ScenariosAuthoredEcrPolicies {
    pub fn new(clients: &Arc<AwsClients>) -> Self {
        let config = aws_sdk_ecr::config::Builder::from(clients.config())
            .endpoint_url(clients.endpoint())
            .build();
        Self {
            client: aws_sdk_ecr::Client::from_conf(config),
        }
    }
}

impl ServiceGroup for ScenariosAuthoredEcrPolicies {
    fn name(&self) -> &'static str {
        "scenarios/authored-ecr-policies"
    }

    fn impls(&self) -> HashMap<String, TestFn> {
        let mut impls: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            impls.insert(
                "ecr-policies:PutLifecyclePolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ECR_POLICIES
                            .run_test(&ctx, "PutLifecyclePolicy", test_ecr_policies_put_lifecycle_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "ecr-policies:GetLifecyclePolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ECR_POLICIES
                            .run_test(&ctx, "GetLifecyclePolicy", test_ecr_policies_get_lifecycle_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "ecr-policies:DeleteLifecyclePolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ECR_POLICIES
                            .run_test(&ctx, "DeleteLifecyclePolicy", test_ecr_policies_delete_lifecycle_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "ecr-policies:GetLifecyclePolicyNotFound".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ECR_POLICIES
                            .run_test(&ctx, "GetLifecyclePolicyNotFound", test_ecr_policies_get_lifecycle_policy_not_found(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "ecr-policies:SetRepositoryPolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ECR_POLICIES
                            .run_test(&ctx, "SetRepositoryPolicy", test_ecr_policies_set_repository_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "ecr-policies:GetRepositoryPolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ECR_POLICIES
                            .run_test(&ctx, "GetRepositoryPolicy", test_ecr_policies_get_repository_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "ecr-policies:DeleteRepositoryPolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ECR_POLICIES
                            .run_test(&ctx, "DeleteRepositoryPolicy", test_ecr_policies_delete_repository_policy(&client))
                            .await
                    })
                }),
            );
        }
        impls
    }

    fn setups(&self) -> HashMap<String, TestFn> {
        let mut setups: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            setups.insert(
                "ecr-policies".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ECR_POLICIES.run_setup(&ctx, setup_ecr_policies(&client)).await
                    })
                }),
            );
        }
        setups
    }

    fn teardowns(&self) -> HashMap<String, TestFn> {
        let mut teardowns: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            teardowns.insert(
                "ecr-policies".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ECR_POLICIES.run_teardown(&ctx, teardown_ecr_policies(&client)).await
                    })
                }),
            );
        }
        teardowns
    }
}

fn setup_ecr_policies(client: &aws_sdk_ecr::Client) -> Vec<Call> {
    vec![
        Call {
            op: "CreateRepository",
            params: scenario::map(vec![("repositoryName", scenario::name("repo"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_repository()
                            .repository_name(b.string("repositoryName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "CreateRepository",
            params: scenario::map(vec![("repositoryName", scenario::name("bare"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_repository()
                            .repository_name(b.string("repositoryName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn teardown_ecr_policies(client: &aws_sdk_ecr::Client) -> Vec<Call> {
    vec![
        Call {
            op: "DeleteRepositoryPolicy",
            params: scenario::map(vec![("repositoryName", scenario::name("repo"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_repository_policy()
                            .repository_name(b.string("repositoryName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "DeleteLifecyclePolicy",
            params: scenario::map(vec![("repositoryName", scenario::name("repo"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_lifecycle_policy()
                            .repository_name(b.string("repositoryName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "DeleteRepository",
            params: scenario::map(vec![
                ("force", scenario::lit(::serde_json::json!(true))),
                ("repositoryName", scenario::name("bare")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_repository()
                            .force(true)
                            .repository_name(b.string("repositoryName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "DeleteRepository",
            params: scenario::map(vec![
                ("force", scenario::lit(::serde_json::json!(true))),
                ("repositoryName", scenario::name("repo")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_repository()
                            .force(true)
                            .repository_name(b.string("repositoryName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn test_ecr_policies_put_lifecycle_policy(client: &aws_sdk_ecr::Client) -> Test {
    Test {
        call: Call {
            op: "PutLifecyclePolicy",
            params: scenario::map(vec![
                ("lifecyclePolicyText", scenario::lit(::serde_json::json!("{\"rules\":[{\"rulePriority\":1,\"description\":\"expire untagged\",\"selection\":{\"tagStatus\":\"untagged\",\"countType\":\"imageCountMoreThan\",\"countNumber\":5},\"action\":{\"type\":\"expire\"}}]}"))),
                ("repositoryName", scenario::name("repo")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .put_lifecycle_policy()
                            .lifecycle_policy_text("{\"rules\":[{\"rulePriority\":1,\"description\":\"expire untagged\",\"selection\":{\"tagStatus\":\"untagged\",\"countType\":\"imageCountMoreThan\",\"countNumber\":5},\"action\":{\"type\":\"expire\"}}]}")
                            .repository_name(b.string("repositoryName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.lifecyclePolicyText", scenario::lit(::serde_json::json!("{\"rules\":[{\"rulePriority\":1,\"description\":\"expire untagged\",\"selection\":{\"tagStatus\":\"untagged\",\"countType\":\"imageCountMoreThan\",\"countNumber\":5},\"action\":{\"type\":\"expire\"}}]}"))),
                scenario::equals("$.repositoryName", scenario::name("repo")),
            ]),
            scenario::readback(
                Call {
                    op: "GetLifecyclePolicy",
                    params: scenario::map(vec![
                        ("repositoryName", scenario::name("repo")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .get_lifecycle_policy()
                                    .repository_name(b.string("repositoryName")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                vec![
                    scenario::equals("$.lifecyclePolicyText", scenario::lit(::serde_json::json!("{\"rules\":[{\"rulePriority\":1,\"description\":\"expire untagged\",\"selection\":{\"tagStatus\":\"untagged\",\"countType\":\"imageCountMoreThan\",\"countNumber\":5},\"action\":{\"type\":\"expire\"}}]}"))),
                ],
            ),
        ],
    }
}

fn test_ecr_policies_get_lifecycle_policy(client: &aws_sdk_ecr::Client) -> Test {
    Test {
        call: Call {
            op: "GetLifecyclePolicy",
            params: scenario::map(vec![("repositoryName", scenario::name("repo"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_lifecycle_policy()
                            .repository_name(b.string("repositoryName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.lifecyclePolicyText", scenario::lit(::serde_json::json!("{\"rules\":[{\"rulePriority\":1,\"description\":\"expire untagged\",\"selection\":{\"tagStatus\":\"untagged\",\"countType\":\"imageCountMoreThan\",\"countNumber\":5},\"action\":{\"type\":\"expire\"}}]}"))),
                scenario::equals("$.repositoryName", scenario::name("repo")),
            ]),
        ],
    }
}

fn test_ecr_policies_delete_lifecycle_policy(client: &aws_sdk_ecr::Client) -> Test {
    Test {
        call: Call {
            op: "DeleteLifecyclePolicy",
            params: scenario::map(vec![("repositoryName", scenario::name("repo"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_lifecycle_policy()
                            .repository_name(b.string("repositoryName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.repositoryName", scenario::name("repo")),
            ]),
            scenario::absent_by_error(
                Call {
                    op: "GetLifecyclePolicy",
                    params: scenario::map(vec![
                        ("repositoryName", scenario::name("repo")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .get_lifecycle_policy()
                                    .repository_name(b.string("repositoryName")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                scenario::error("LifecyclePolicyNotFoundException", "LifecyclePolicyNotFoundException"),
            ),
        ],
    }
}

fn test_ecr_policies_get_lifecycle_policy_not_found(client: &aws_sdk_ecr::Client) -> Test {
    Test {
        call: Call {
            op: "GetLifecyclePolicy",
            params: scenario::map(vec![("repositoryName", scenario::name("bare"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_lifecycle_policy()
                            .repository_name(b.string("repositoryName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::error_code(scenario::error("LifecyclePolicyNotFoundException", "LifecyclePolicyNotFoundException")),
        ],
    }
}

fn test_ecr_policies_set_repository_policy(client: &aws_sdk_ecr::Client) -> Test {
    Test {
        call: Call {
            op: "SetRepositoryPolicy",
            params: scenario::map(vec![
                ("policyText", scenario::lit(::serde_json::json!("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Sid\":\"AllowPull\",\"Effect\":\"Allow\",\"Principal\":\"*\",\"Action\":[\"ecr:GetDownloadUrlForLayer\",\"ecr:BatchGetImage\"]}]}"))),
                ("repositoryName", scenario::name("repo")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .set_repository_policy()
                            .policy_text("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Sid\":\"AllowPull\",\"Effect\":\"Allow\",\"Principal\":\"*\",\"Action\":[\"ecr:GetDownloadUrlForLayer\",\"ecr:BatchGetImage\"]}]}")
                            .repository_name(b.string("repositoryName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.policyText", scenario::lit(::serde_json::json!("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Sid\":\"AllowPull\",\"Effect\":\"Allow\",\"Principal\":\"*\",\"Action\":[\"ecr:GetDownloadUrlForLayer\",\"ecr:BatchGetImage\"]}]}"))),
                scenario::equals("$.repositoryName", scenario::name("repo")),
            ]),
            scenario::readback(
                Call {
                    op: "GetRepositoryPolicy",
                    params: scenario::map(vec![
                        ("repositoryName", scenario::name("repo")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .get_repository_policy()
                                    .repository_name(b.string("repositoryName")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                vec![
                    scenario::equals("$.policyText", scenario::lit(::serde_json::json!("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Sid\":\"AllowPull\",\"Effect\":\"Allow\",\"Principal\":\"*\",\"Action\":[\"ecr:GetDownloadUrlForLayer\",\"ecr:BatchGetImage\"]}]}"))),
                ],
            ),
        ],
    }
}

fn test_ecr_policies_get_repository_policy(client: &aws_sdk_ecr::Client) -> Test {
    Test {
        call: Call {
            op: "GetRepositoryPolicy",
            params: scenario::map(vec![("repositoryName", scenario::name("repo"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_repository_policy()
                            .repository_name(b.string("repositoryName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.policyText", scenario::lit(::serde_json::json!("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Sid\":\"AllowPull\",\"Effect\":\"Allow\",\"Principal\":\"*\",\"Action\":[\"ecr:GetDownloadUrlForLayer\",\"ecr:BatchGetImage\"]}]}"))),
                scenario::equals("$.repositoryName", scenario::name("repo")),
            ]),
        ],
    }
}

fn test_ecr_policies_delete_repository_policy(client: &aws_sdk_ecr::Client) -> Test {
    Test {
        call: Call {
            op: "DeleteRepositoryPolicy",
            params: scenario::map(vec![("repositoryName", scenario::name("repo"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_repository_policy()
                            .repository_name(b.string("repositoryName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.repositoryName", scenario::name("repo")),
            ]),
            scenario::absent_by_error(
                Call {
                    op: "GetRepositoryPolicy",
                    params: scenario::map(vec![
                        ("repositoryName", scenario::name("repo")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .get_repository_policy()
                                    .repository_name(b.string("repositoryName")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                scenario::error("RepositoryPolicyNotFoundException", "RepositoryPolicyNotFoundException"),
            ),
        ],
    }
}
