// Code generated by cmd/compatgen; DO NOT EDIT.
//
// Generated from compat/model/authored/ecr-tags.json by cmd/compatgen.
// The semantics live in crate::scenario; this file is the data and the typed
// SDK calls.

use std::collections::HashMap;
use std::sync::Arc;

use crate::clients::AwsClients;
use crate::groups::ServiceGroup;
use crate::harness::{TestContext, TestFn};
use crate::scenario::{self, Call, Group, Test};

/// The scenario file every group in this file was generated from.
const SCENARIO_FILE: &str = "compat/model/authored/ecr-tags.json";

const GROUP_ECR_TAGS_SHADOW: Group = Group {
    name: "ecr-tags-shadow",
    file: SCENARIO_FILE,
};

/// The authored ecr-tags groups.
///
/// The client is built once, from the config the suite's hand-written groups
/// share, and cloned into every test — a generated group builds its own rather
/// than adding an accessor to AwsClients for every service the generator learns
/// to cover; nothing else about the client differs.
pub struct ScenariosAuthoredEcrTags {
    client: aws_sdk_ecr::Client,
}

impl ScenariosAuthoredEcrTags {
    pub fn new(clients: &Arc<AwsClients>) -> Self {
        let config = aws_sdk_ecr::config::Builder::from(clients.config())
            .endpoint_url(clients.endpoint())
            .build();
        Self {
            client: aws_sdk_ecr::Client::from_conf(config),
        }
    }
}

impl ServiceGroup for ScenariosAuthoredEcrTags {
    fn name(&self) -> &'static str {
        "scenarios/authored-ecr-tags"
    }

    fn impls(&self) -> HashMap<String, TestFn> {
        let mut impls: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            impls.insert(
                "ecr-tags-shadow:TagResource".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ECR_TAGS_SHADOW
                            .run_test(&ctx, "TagResource", test_ecr_tags_shadow_tag_resource(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "ecr-tags-shadow:ListTagsForResource".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ECR_TAGS_SHADOW
                            .run_test(&ctx, "ListTagsForResource", test_ecr_tags_shadow_list_tags_for_resource(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "ecr-tags-shadow:UntagResource".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ECR_TAGS_SHADOW
                            .run_test(&ctx, "UntagResource", test_ecr_tags_shadow_untag_resource(&client))
                            .await
                    })
                }),
            );
        }
        impls
    }

    fn setups(&self) -> HashMap<String, TestFn> {
        let mut setups: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            setups.insert(
                "ecr-tags-shadow".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ECR_TAGS_SHADOW.run_setup(&ctx, setup_ecr_tags_shadow(&client)).await
                    })
                }),
            );
        }
        setups
    }

    fn teardowns(&self) -> HashMap<String, TestFn> {
        let mut teardowns: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            teardowns.insert(
                "ecr-tags-shadow".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_ECR_TAGS_SHADOW.run_teardown(&ctx, teardown_ecr_tags_shadow(&client)).await
                    })
                }),
            );
        }
        teardowns
    }
}

fn setup_ecr_tags_shadow(client: &aws_sdk_ecr::Client) -> Vec<Call> {
    vec![
        Call {
            op: "CreateRepository",
            params: scenario::map(vec![("repositoryName", scenario::name("repo"))]),
            export: vec![
                ("repo.arn", "$.repository.repositoryArn"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_repository()
                            .repository_name(b.string("repositoryName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn teardown_ecr_tags_shadow(client: &aws_sdk_ecr::Client) -> Vec<Call> {
    vec![
        Call {
            op: "UntagResource",
            params: scenario::map(vec![
                ("resourceArn", scenario::context("repo.arn")),
                ("tagKeys", scenario::lit(::serde_json::json!(["env", "owner"]))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .untag_resource()
                            .resource_arn(b.string("resourceArn")?)
                            .tag_keys("env")
                            .tag_keys("owner")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "DeleteRepository",
            params: scenario::map(vec![
                ("force", scenario::lit(::serde_json::json!(true))),
                ("repositoryName", scenario::name("repo")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_repository()
                            .force(true)
                            .repository_name(b.string("repositoryName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn test_ecr_tags_shadow_tag_resource(client: &aws_sdk_ecr::Client) -> Test {
    Test {
        call: Call {
            op: "TagResource",
            params: scenario::map(vec![
                ("resourceArn", scenario::context("repo.arn")),
                ("tags", scenario::lit(::serde_json::json!([{"Key": "env", "Value": "compat"}, {"Key": "owner", "Value": "ecr-tags"}]))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .tag_resource()
                            .resource_arn(b.string("resourceArn")?)
                            .tags(
                                aws_sdk_ecr::types::Tag::builder()
                                    .key("env")
                                    .value("compat")
                                    .build()
                                    .map_err(|err| scenario::build_error("tags[0]", err))?
                            )
                            .tags(
                                aws_sdk_ecr::types::Tag::builder()
                                    .key("owner")
                                    .value("ecr-tags")
                                    .build()
                                    .map_err(|err| scenario::build_error("tags[1]", err))?
                            )
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                Some(Call {
                    op: "ListTagsForResource",
                    params: scenario::map(vec![
                        ("resourceArn", scenario::context("repo.arn")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_tags_for_resource()
                                    .resource_arn(b.string("resourceArn")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                }),
                "$.tags",
                vec![
                    scenario::where_entry("$.Key", scenario::lit(::serde_json::json!("env"))),
                    scenario::where_entry("$.Value", scenario::lit(::serde_json::json!("compat"))),
                ],
            ),
            scenario::list_contains(
                Some(Call {
                    op: "ListTagsForResource",
                    params: scenario::map(vec![
                        ("resourceArn", scenario::context("repo.arn")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_tags_for_resource()
                                    .resource_arn(b.string("resourceArn")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                }),
                "$.tags",
                vec![
                    scenario::where_entry("$.Key", scenario::lit(::serde_json::json!("owner"))),
                    scenario::where_entry("$.Value", scenario::lit(::serde_json::json!("ecr-tags"))),
                ],
            ),
        ],
    }
}

fn test_ecr_tags_shadow_list_tags_for_resource(client: &aws_sdk_ecr::Client) -> Test {
    Test {
        call: Call {
            op: "ListTagsForResource",
            params: scenario::map(vec![
                ("resourceArn", scenario::context("repo.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_tags_for_resource()
                            .resource_arn(b.string("resourceArn")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.tags[1]"),
                scenario::missing("$.tags[2]"),
            ]),
            scenario::list_contains(
                None,
                "$.tags",
                vec![
                    scenario::where_entry("$.Key", scenario::lit(::serde_json::json!("env"))),
                    scenario::where_entry("$.Value", scenario::lit(::serde_json::json!("compat"))),
                ],
            ),
            scenario::list_contains(
                None,
                "$.tags",
                vec![
                    scenario::where_entry("$.Key", scenario::lit(::serde_json::json!("owner"))),
                    scenario::where_entry("$.Value", scenario::lit(::serde_json::json!("ecr-tags"))),
                ],
            ),
        ],
    }
}

fn test_ecr_tags_shadow_untag_resource(client: &aws_sdk_ecr::Client) -> Test {
    Test {
        call: Call {
            op: "UntagResource",
            params: scenario::map(vec![
                ("resourceArn", scenario::context("repo.arn")),
                ("tagKeys", scenario::lit(::serde_json::json!(["owner"]))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .untag_resource()
                            .resource_arn(b.string("resourceArn")?)
                            .tag_keys("owner")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::absent_from_list(
                Some(Call {
                    op: "ListTagsForResource",
                    params: scenario::map(vec![
                        ("resourceArn", scenario::context("repo.arn")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_tags_for_resource()
                                    .resource_arn(b.string("resourceArn")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                }),
                "$.tags",
                vec![
                    scenario::where_entry("$.Key", scenario::lit(::serde_json::json!("owner"))),
                ],
            ),
            scenario::list_contains(
                Some(Call {
                    op: "ListTagsForResource",
                    params: scenario::map(vec![
                        ("resourceArn", scenario::context("repo.arn")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_tags_for_resource()
                                    .resource_arn(b.string("resourceArn")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                }),
                "$.tags",
                vec![
                    scenario::where_entry("$.Key", scenario::lit(::serde_json::json!("env"))),
                    scenario::where_entry("$.Value", scenario::lit(::serde_json::json!("compat"))),
                ],
            ),
        ],
    }
}
