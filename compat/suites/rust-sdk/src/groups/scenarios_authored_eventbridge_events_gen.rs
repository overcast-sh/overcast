// Code generated by cmd/compatgen; DO NOT EDIT.
//
// Generated from compat/model/authored/eventbridge-events.json by cmd/compatgen.
// The semantics live in crate::scenario; this file is the data and the typed
// SDK calls.

use std::collections::HashMap;
use std::sync::Arc;

use crate::clients::AwsClients;
use crate::groups::ServiceGroup;
use crate::harness::{TestContext, TestFn};
use crate::scenario::{self, Call, Group, Test};

/// The scenario file every group in this file was generated from.
const SCENARIO_FILE: &str = "compat/model/authored/eventbridge-events.json";

const GROUP_EVENTBRIDGE_EVENTS: Group = Group {
    name: "eventbridge-events",
    file: SCENARIO_FILE,
};

/// The authored eventbridge-events groups.
///
/// The client is built once, from the config the suite's hand-written groups
/// share, and cloned into every test — a generated group builds its own rather
/// than adding an accessor to AwsClients for every service the generator learns
/// to cover; nothing else about the client differs.
pub struct ScenariosAuthoredEventbridgeEvents {
    client: aws_sdk_eventbridge::Client,
}

impl ScenariosAuthoredEventbridgeEvents {
    pub fn new(clients: &Arc<AwsClients>) -> Self {
        let config = aws_sdk_eventbridge::config::Builder::from(clients.config())
            .endpoint_url(clients.endpoint())
            .build();
        Self {
            client: aws_sdk_eventbridge::Client::from_conf(config),
        }
    }
}

impl ServiceGroup for ScenariosAuthoredEventbridgeEvents {
    fn name(&self) -> &'static str {
        "scenarios/authored-eventbridge-events"
    }

    fn impls(&self) -> HashMap<String, TestFn> {
        let mut impls: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            impls.insert(
                "eventbridge-events:PutEvents".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_EVENTBRIDGE_EVENTS
                            .run_test(&ctx, "PutEvents", test_eventbridge_events_put_events(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "eventbridge-events:PutEventsBatch".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_EVENTBRIDGE_EVENTS
                            .run_test(&ctx, "PutEventsBatch", test_eventbridge_events_put_events_batch(&client))
                            .await
                    })
                }),
            );
        }
        impls
    }

    fn setups(&self) -> HashMap<String, TestFn> {
        let mut setups: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            setups.insert(
                "eventbridge-events".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_EVENTBRIDGE_EVENTS.run_setup(&ctx, setup_eventbridge_events(&client)).await
                    })
                }),
            );
        }
        setups
    }

    fn teardowns(&self) -> HashMap<String, TestFn> {
        let mut teardowns: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            teardowns.insert(
                "eventbridge-events".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_EVENTBRIDGE_EVENTS.run_teardown(&ctx, teardown_eventbridge_events(&client)).await
                    })
                }),
            );
        }
        teardowns
    }
}

fn setup_eventbridge_events(client: &aws_sdk_eventbridge::Client) -> Vec<Call> {
    vec![
        Call {
            op: "CreateEventBus",
            params: scenario::map(vec![("Name", scenario::name("bus"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_event_bus()
                            .name(b.string("Name")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn teardown_eventbridge_events(client: &aws_sdk_eventbridge::Client) -> Vec<Call> {
    vec![
        Call {
            op: "DeleteEventBus",
            params: scenario::map(vec![("Name", scenario::name("bus"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_event_bus()
                            .name(b.string("Name")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn test_eventbridge_events_put_events(client: &aws_sdk_eventbridge::Client) -> Test {
    Test {
        call: Call {
            op: "PutEvents",
            params: scenario::map(vec![
                ("Entries", scenario::list(vec![
                    scenario::map(vec![
                        ("Detail", scenario::lit(::serde_json::json!("{\"key\":\"value\"}"))),
                        ("DetailType", scenario::lit(::serde_json::json!("CompatTest"))),
                        ("EventBusName", scenario::name("bus")),
                        ("Source", scenario::lit(::serde_json::json!("compat.eventbridge-events"))),
                    ]),
                ])),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .put_events()
                            .entries(
                                aws_sdk_eventbridge::types::PutEventsRequestEntry::builder()
                                    .detail("{\"key\":\"value\"}")
                                    .detail_type("CompatTest")
                                    .event_bus_name(b.string("Entries[0].EventBusName")?)
                                    .source("compat.eventbridge-events")
                                    .build()
                            )
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.Entries[0].EventId"),
                scenario::missing("$.Entries[1]"),
                scenario::equals("$.FailedEntryCount", scenario::lit(::serde_json::json!(0))),
            ]),
        ],
    }
}

fn test_eventbridge_events_put_events_batch(client: &aws_sdk_eventbridge::Client) -> Test {
    Test {
        call: Call {
            op: "PutEvents",
            params: scenario::map(vec![
                ("Entries", scenario::list(vec![
                    scenario::map(vec![
                        ("Detail", scenario::lit(::serde_json::json!("{\"index\":0}"))),
                        ("DetailType", scenario::lit(::serde_json::json!("CompatBatch"))),
                        ("EventBusName", scenario::name("bus")),
                        ("Source", scenario::lit(::serde_json::json!("compat.eventbridge-events"))),
                    ]),
                    scenario::map(vec![
                        ("Detail", scenario::lit(::serde_json::json!("{\"index\":1}"))),
                        ("DetailType", scenario::lit(::serde_json::json!("CompatBatch"))),
                        ("EventBusName", scenario::name("bus")),
                        ("Source", scenario::lit(::serde_json::json!("compat.eventbridge-events"))),
                    ]),
                    scenario::map(vec![
                        ("Detail", scenario::lit(::serde_json::json!("{\"index\":2}"))),
                        ("DetailType", scenario::lit(::serde_json::json!("CompatBatch"))),
                        ("EventBusName", scenario::name("bus")),
                        ("Source", scenario::lit(::serde_json::json!("compat.eventbridge-events"))),
                    ]),
                    scenario::map(vec![
                        ("Detail", scenario::lit(::serde_json::json!("{\"index\":3}"))),
                        ("DetailType", scenario::lit(::serde_json::json!("CompatBatch"))),
                        ("EventBusName", scenario::name("bus")),
                        ("Source", scenario::lit(::serde_json::json!("compat.eventbridge-events"))),
                    ]),
                    scenario::map(vec![
                        ("Detail", scenario::lit(::serde_json::json!("{\"index\":4}"))),
                        ("DetailType", scenario::lit(::serde_json::json!("CompatBatch"))),
                        ("EventBusName", scenario::name("bus")),
                        ("Source", scenario::lit(::serde_json::json!("compat.eventbridge-events"))),
                    ]),
                ])),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .put_events()
                            .entries(
                                aws_sdk_eventbridge::types::PutEventsRequestEntry::builder()
                                    .detail("{\"index\":0}")
                                    .detail_type("CompatBatch")
                                    .event_bus_name(b.string("Entries[0].EventBusName")?)
                                    .source("compat.eventbridge-events")
                                    .build()
                            )
                            .entries(
                                aws_sdk_eventbridge::types::PutEventsRequestEntry::builder()
                                    .detail("{\"index\":1}")
                                    .detail_type("CompatBatch")
                                    .event_bus_name(b.string("Entries[1].EventBusName")?)
                                    .source("compat.eventbridge-events")
                                    .build()
                            )
                            .entries(
                                aws_sdk_eventbridge::types::PutEventsRequestEntry::builder()
                                    .detail("{\"index\":2}")
                                    .detail_type("CompatBatch")
                                    .event_bus_name(b.string("Entries[2].EventBusName")?)
                                    .source("compat.eventbridge-events")
                                    .build()
                            )
                            .entries(
                                aws_sdk_eventbridge::types::PutEventsRequestEntry::builder()
                                    .detail("{\"index\":3}")
                                    .detail_type("CompatBatch")
                                    .event_bus_name(b.string("Entries[3].EventBusName")?)
                                    .source("compat.eventbridge-events")
                                    .build()
                            )
                            .entries(
                                aws_sdk_eventbridge::types::PutEventsRequestEntry::builder()
                                    .detail("{\"index\":4}")
                                    .detail_type("CompatBatch")
                                    .event_bus_name(b.string("Entries[4].EventBusName")?)
                                    .source("compat.eventbridge-events")
                                    .build()
                            )
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.Entries[0].EventId"),
                scenario::non_empty("$.Entries[1].EventId"),
                scenario::non_empty("$.Entries[2].EventId"),
                scenario::non_empty("$.Entries[3].EventId"),
                scenario::non_empty("$.Entries[4].EventId"),
                scenario::missing("$.Entries[5]"),
                scenario::equals("$.FailedEntryCount", scenario::lit(::serde_json::json!(0))),
            ]),
        ],
    }
}
