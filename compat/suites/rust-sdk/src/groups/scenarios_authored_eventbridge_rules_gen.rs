// Code generated by cmd/compatgen; DO NOT EDIT.
//
// Generated from compat/model/authored/eventbridge-rules.json by cmd/compatgen.
// The semantics live in crate::scenario; this file is the data and the typed
// SDK calls.

use std::collections::HashMap;
use std::sync::Arc;

use crate::clients::AwsClients;
use crate::groups::ServiceGroup;
use crate::harness::{TestContext, TestFn};
use crate::scenario::{self, Call, Group, Test};

/// The scenario file every group in this file was generated from.
const SCENARIO_FILE: &str = "compat/model/authored/eventbridge-rules.json";

const GROUP_EVENTBRIDGE_RULES_SHADOW: Group = Group {
    name: "eventbridge-rules-shadow",
    file: SCENARIO_FILE,
};

/// The authored eventbridge-rules groups.
///
/// The client is built once, from the config the suite's hand-written groups
/// share, and cloned into every test — a generated group builds its own rather
/// than adding an accessor to AwsClients for every service the generator learns
/// to cover; nothing else about the client differs.
pub struct ScenariosAuthoredEventbridgeRules {
    client: aws_sdk_eventbridge::Client,
}

impl ScenariosAuthoredEventbridgeRules {
    pub fn new(clients: &Arc<AwsClients>) -> Self {
        let config = aws_sdk_eventbridge::config::Builder::from(clients.config())
            .endpoint_url(clients.endpoint())
            .build();
        Self {
            client: aws_sdk_eventbridge::Client::from_conf(config),
        }
    }
}

impl ServiceGroup for ScenariosAuthoredEventbridgeRules {
    fn name(&self) -> &'static str {
        "scenarios/authored-eventbridge-rules"
    }

    fn impls(&self) -> HashMap<String, TestFn> {
        let mut impls: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            impls.insert(
                "eventbridge-rules-shadow:PutRule".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_EVENTBRIDGE_RULES_SHADOW
                            .run_test(&ctx, "PutRule", test_eventbridge_rules_shadow_put_rule(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "eventbridge-rules-shadow:DescribeRule".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_EVENTBRIDGE_RULES_SHADOW
                            .run_test(&ctx, "DescribeRule", test_eventbridge_rules_shadow_describe_rule(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "eventbridge-rules-shadow:ListRules".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_EVENTBRIDGE_RULES_SHADOW
                            .run_test(&ctx, "ListRules", test_eventbridge_rules_shadow_list_rules(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "eventbridge-rules-shadow:PutTargets".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_EVENTBRIDGE_RULES_SHADOW
                            .run_test(&ctx, "PutTargets", test_eventbridge_rules_shadow_put_targets(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "eventbridge-rules-shadow:ListTargetsByRule".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_EVENTBRIDGE_RULES_SHADOW
                            .run_test(&ctx, "ListTargetsByRule", test_eventbridge_rules_shadow_list_targets_by_rule(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "eventbridge-rules-shadow:DisableRule".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_EVENTBRIDGE_RULES_SHADOW
                            .run_test(&ctx, "DisableRule", test_eventbridge_rules_shadow_disable_rule(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "eventbridge-rules-shadow:EnableRule".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_EVENTBRIDGE_RULES_SHADOW
                            .run_test(&ctx, "EnableRule", test_eventbridge_rules_shadow_enable_rule(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "eventbridge-rules-shadow:RemoveTargets".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_EVENTBRIDGE_RULES_SHADOW
                            .run_test(&ctx, "RemoveTargets", test_eventbridge_rules_shadow_remove_targets(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "eventbridge-rules-shadow:DeleteRule".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_EVENTBRIDGE_RULES_SHADOW
                            .run_test(&ctx, "DeleteRule", test_eventbridge_rules_shadow_delete_rule(&client))
                            .await
                    })
                }),
            );
        }
        impls
    }

    fn setups(&self) -> HashMap<String, TestFn> {
        let mut setups: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            setups.insert(
                "eventbridge-rules-shadow".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_EVENTBRIDGE_RULES_SHADOW.run_setup(&ctx, setup_eventbridge_rules_shadow(&client)).await
                    })
                }),
            );
        }
        setups
    }

    fn teardowns(&self) -> HashMap<String, TestFn> {
        let mut teardowns: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            teardowns.insert(
                "eventbridge-rules-shadow".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_EVENTBRIDGE_RULES_SHADOW.run_teardown(&ctx, teardown_eventbridge_rules_shadow(&client)).await
                    })
                }),
            );
        }
        teardowns
    }
}

fn setup_eventbridge_rules_shadow(client: &aws_sdk_eventbridge::Client) -> Vec<Call> {
    vec![
        Call {
            op: "CreateEventBus",
            params: scenario::map(vec![("Name", scenario::name("bus"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_event_bus()
                            .name(b.string("Name")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn teardown_eventbridge_rules_shadow(client: &aws_sdk_eventbridge::Client) -> Vec<Call> {
    vec![
        Call {
            op: "RemoveTargets",
            params: scenario::map(vec![
                ("EventBusName", scenario::name("bus")),
                ("Ids", scenario::lit(::serde_json::json!(["t1"]))),
                ("Rule", scenario::name("rule")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .remove_targets()
                            .event_bus_name(b.string("EventBusName")?)
                            .ids("t1")
                            .rule(b.string("Rule")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "DeleteRule",
            params: scenario::map(vec![
                ("EventBusName", scenario::name("bus")),
                ("Name", scenario::name("rule")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_rule()
                            .event_bus_name(b.string("EventBusName")?)
                            .name(b.string("Name")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "DeleteEventBus",
            params: scenario::map(vec![("Name", scenario::name("bus"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_event_bus()
                            .name(b.string("Name")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn test_eventbridge_rules_shadow_put_rule(client: &aws_sdk_eventbridge::Client) -> Test {
    Test {
        call: Call {
            op: "PutRule",
            params: scenario::map(vec![
                ("EventBusName", scenario::name("bus")),
                ("EventPattern", scenario::lit(::serde_json::json!("{\"source\":[\"compat.eventbridge-rules\"]}"))),
                ("Name", scenario::name("rule")),
                ("State", scenario::lit(::serde_json::json!("ENABLED"))),
            ]),
            export: vec![
                ("rule.arn", "$.RuleArn"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .put_rule()
                            .event_bus_name(b.string("EventBusName")?)
                            .event_pattern("{\"source\":[\"compat.eventbridge-rules\"]}")
                            .name(b.string("Name")?)
                            .state(aws_sdk_eventbridge::types::RuleState::from("ENABLED"))
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.RuleArn"),
            ]),
        ],
    }
}

fn test_eventbridge_rules_shadow_describe_rule(client: &aws_sdk_eventbridge::Client) -> Test {
    Test {
        call: Call {
            op: "DescribeRule",
            params: scenario::map(vec![
                ("EventBusName", scenario::name("bus")),
                ("Name", scenario::name("rule")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .describe_rule()
                            .event_bus_name(b.string("EventBusName")?)
                            .name(b.string("Name")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.Arn", scenario::context("rule.arn")),
                scenario::equals("$.Name", scenario::name("rule")),
                scenario::equals("$.State", scenario::lit(::serde_json::json!("ENABLED"))),
            ]),
        ],
    }
}

fn test_eventbridge_rules_shadow_list_rules(client: &aws_sdk_eventbridge::Client) -> Test {
    Test {
        call: Call {
            op: "ListRules",
            params: scenario::map(vec![
                ("EventBusName", scenario::name("bus")),
                ("NamePrefix", scenario::name("rule")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_rules()
                            .event_bus_name(b.string("EventBusName")?)
                            .name_prefix(b.string("NamePrefix")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.Rules",
                vec![
                    scenario::where_entry("$.Name", scenario::name("rule")),
                ],
            ),
        ],
    }
}

fn test_eventbridge_rules_shadow_put_targets(client: &aws_sdk_eventbridge::Client) -> Test {
    Test {
        call: Call {
            op: "PutTargets",
            params: scenario::map(vec![
                ("EventBusName", scenario::name("bus")),
                ("Rule", scenario::name("rule")),
                ("Targets", scenario::lit(::serde_json::json!([{"Arn": "arn:aws:sqs:us-east-1:000000000000:compat-eventbridge-rules-target", "Id": "t1"}]))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .put_targets()
                            .event_bus_name(b.string("EventBusName")?)
                            .rule(b.string("Rule")?)
                            .targets(
                                aws_sdk_eventbridge::types::Target::builder()
                                    .arn("arn:aws:sqs:us-east-1:000000000000:compat-eventbridge-rules-target")
                                    .id("t1")
                                    .build()
                                    .map_err(|err| scenario::build_error("Targets[0]", err))?
                            )
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.FailedEntryCount", scenario::lit(::serde_json::json!(0))),
            ]),
            scenario::list_contains(
                Some(Call {
                    op: "ListTargetsByRule",
                    params: scenario::map(vec![
                        ("EventBusName", scenario::name("bus")),
                        ("Rule", scenario::name("rule")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_targets_by_rule()
                                    .event_bus_name(b.string("EventBusName")?)
                                    .rule(b.string("Rule")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                }),
                "$.Targets",
                vec![
                    scenario::where_entry("$.Id", scenario::lit(::serde_json::json!("t1"))),
                ],
            ),
        ],
    }
}

fn test_eventbridge_rules_shadow_list_targets_by_rule(client: &aws_sdk_eventbridge::Client) -> Test {
    Test {
        call: Call {
            op: "ListTargetsByRule",
            params: scenario::map(vec![
                ("EventBusName", scenario::name("bus")),
                ("Rule", scenario::name("rule")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_targets_by_rule()
                            .event_bus_name(b.string("EventBusName")?)
                            .rule(b.string("Rule")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.Targets",
                vec![
                    scenario::where_entry("$.Arn", scenario::lit(::serde_json::json!("arn:aws:sqs:us-east-1:000000000000:compat-eventbridge-rules-target"))),
                    scenario::where_entry("$.Id", scenario::lit(::serde_json::json!("t1"))),
                ],
            ),
        ],
    }
}

fn test_eventbridge_rules_shadow_disable_rule(client: &aws_sdk_eventbridge::Client) -> Test {
    Test {
        call: Call {
            op: "DisableRule",
            params: scenario::map(vec![
                ("EventBusName", scenario::name("bus")),
                ("Name", scenario::name("rule")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .disable_rule()
                            .event_bus_name(b.string("EventBusName")?)
                            .name(b.string("Name")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::readback(
                Call {
                    op: "DescribeRule",
                    params: scenario::map(vec![
                        ("EventBusName", scenario::name("bus")),
                        ("Name", scenario::name("rule")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .describe_rule()
                                    .event_bus_name(b.string("EventBusName")?)
                                    .name(b.string("Name")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                vec![
                    scenario::equals("$.State", scenario::lit(::serde_json::json!("DISABLED"))),
                ],
            ),
        ],
    }
}

fn test_eventbridge_rules_shadow_enable_rule(client: &aws_sdk_eventbridge::Client) -> Test {
    Test {
        call: Call {
            op: "EnableRule",
            params: scenario::map(vec![
                ("EventBusName", scenario::name("bus")),
                ("Name", scenario::name("rule")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .enable_rule()
                            .event_bus_name(b.string("EventBusName")?)
                            .name(b.string("Name")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::readback(
                Call {
                    op: "DescribeRule",
                    params: scenario::map(vec![
                        ("EventBusName", scenario::name("bus")),
                        ("Name", scenario::name("rule")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .describe_rule()
                                    .event_bus_name(b.string("EventBusName")?)
                                    .name(b.string("Name")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                vec![
                    scenario::equals("$.State", scenario::lit(::serde_json::json!("ENABLED"))),
                ],
            ),
        ],
    }
}

fn test_eventbridge_rules_shadow_remove_targets(client: &aws_sdk_eventbridge::Client) -> Test {
    Test {
        call: Call {
            op: "RemoveTargets",
            params: scenario::map(vec![
                ("EventBusName", scenario::name("bus")),
                ("Ids", scenario::lit(::serde_json::json!(["t1"]))),
                ("Rule", scenario::name("rule")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .remove_targets()
                            .event_bus_name(b.string("EventBusName")?)
                            .ids("t1")
                            .rule(b.string("Rule")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.FailedEntryCount", scenario::lit(::serde_json::json!(0))),
            ]),
            scenario::absent_from_list(
                Some(Call {
                    op: "ListTargetsByRule",
                    params: scenario::map(vec![
                        ("EventBusName", scenario::name("bus")),
                        ("Rule", scenario::name("rule")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_targets_by_rule()
                                    .event_bus_name(b.string("EventBusName")?)
                                    .rule(b.string("Rule")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                }),
                "$.Targets",
                vec![
                    scenario::where_entry("$.Id", scenario::lit(::serde_json::json!("t1"))),
                ],
            ),
        ],
    }
}

fn test_eventbridge_rules_shadow_delete_rule(client: &aws_sdk_eventbridge::Client) -> Test {
    Test {
        call: Call {
            op: "DeleteRule",
            params: scenario::map(vec![
                ("EventBusName", scenario::name("bus")),
                ("Name", scenario::name("rule")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_rule()
                            .event_bus_name(b.string("EventBusName")?)
                            .name(b.string("Name")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::absent_by_error(
                Call {
                    op: "DescribeRule",
                    params: scenario::map(vec![
                        ("EventBusName", scenario::name("bus")),
                        ("Name", scenario::name("rule")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .describe_rule()
                                    .event_bus_name(b.string("EventBusName")?)
                                    .name(b.string("Name")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                scenario::error("ResourceNotFoundException", "ResourceNotFoundException"),
            ),
            scenario::absent_from_list(
                Some(Call {
                    op: "ListRules",
                    params: scenario::map(vec![
                        ("EventBusName", scenario::name("bus")),
                        ("NamePrefix", scenario::name("rule")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_rules()
                                    .event_bus_name(b.string("EventBusName")?)
                                    .name_prefix(b.string("NamePrefix")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                }),
                "$.Rules",
                vec![
                    scenario::where_entry("$.Name", scenario::name("rule")),
                ],
            ),
        ],
    }
}
