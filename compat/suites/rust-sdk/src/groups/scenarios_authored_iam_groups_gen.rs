// Code generated by cmd/compatgen; DO NOT EDIT.
//
// Generated from compat/model/authored/iam-groups.json by cmd/compatgen.
// The semantics live in crate::scenario; this file is the data and the typed
// SDK calls.

use std::collections::HashMap;
use std::sync::Arc;

use crate::clients::AwsClients;
use crate::groups::ServiceGroup;
use crate::harness::{TestContext, TestFn};
use crate::scenario::{self, Call, Group, Test};

/// The scenario file every group in this file was generated from.
const SCENARIO_FILE: &str = "compat/model/authored/iam-groups.json";

const GROUP_IAM_GROUPS: Group = Group {
    name: "iam-groups",
    file: SCENARIO_FILE,
};

/// The authored iam-groups groups.
///
/// The client is built once, from the config the suite's hand-written groups
/// share, and cloned into every test — a generated group builds its own rather
/// than adding an accessor to AwsClients for every service the generator learns
/// to cover; nothing else about the client differs.
pub struct ScenariosAuthoredIamGroups {
    client: aws_sdk_iam::Client,
}

impl ScenariosAuthoredIamGroups {
    pub fn new(clients: &Arc<AwsClients>) -> Self {
        let config = aws_sdk_iam::config::Builder::from(clients.config())
            .endpoint_url(clients.endpoint())
            .build();
        Self {
            client: aws_sdk_iam::Client::from_conf(config),
        }
    }
}

impl ServiceGroup for ScenariosAuthoredIamGroups {
    fn name(&self) -> &'static str {
        "scenarios/authored-iam-groups"
    }

    fn impls(&self) -> HashMap<String, TestFn> {
        let mut impls: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            impls.insert(
                "iam-groups:CreateGroup".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GROUPS
                            .run_test(&ctx, "CreateGroup", test_iam_groups_create_group(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-groups:AddUserToGroup".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GROUPS
                            .run_test(&ctx, "AddUserToGroup", test_iam_groups_add_user_to_group(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-groups:ListGroupsForUser".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GROUPS
                            .run_test(&ctx, "ListGroupsForUser", test_iam_groups_list_groups_for_user(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-groups:RemoveUserFromGroup".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GROUPS
                            .run_test(&ctx, "RemoveUserFromGroup", test_iam_groups_remove_user_from_group(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-groups:GetGroup".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GROUPS
                            .run_test(&ctx, "GetGroup", test_iam_groups_get_group(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-groups:DeleteGroup".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GROUPS
                            .run_test(&ctx, "DeleteGroup", test_iam_groups_delete_group(&client))
                            .await
                    })
                }),
            );
        }
        impls
    }

    fn setups(&self) -> HashMap<String, TestFn> {
        let mut setups: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            setups.insert(
                "iam-groups".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GROUPS.run_setup(&ctx, setup_iam_groups(&client)).await
                    })
                }),
            );
        }
        setups
    }

    fn teardowns(&self) -> HashMap<String, TestFn> {
        let mut teardowns: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            teardowns.insert(
                "iam-groups".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GROUPS.run_teardown(&ctx, teardown_iam_groups(&client)).await
                    })
                }),
            );
        }
        teardowns
    }
}

fn setup_iam_groups(client: &aws_sdk_iam::Client) -> Vec<Call> {
    vec![
        Call {
            op: "CreateUser",
            params: scenario::map(vec![("UserName", scenario::name("user"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_user()
                            .user_name(b.string("UserName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn teardown_iam_groups(client: &aws_sdk_iam::Client) -> Vec<Call> {
    vec![
        Call {
            op: "RemoveUserFromGroup",
            params: scenario::map(vec![
                ("GroupName", scenario::name("group")),
                ("UserName", scenario::name("user")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .remove_user_from_group()
                            .group_name(b.string("GroupName")?)
                            .user_name(b.string("UserName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "DeleteGroup",
            params: scenario::map(vec![("GroupName", scenario::name("group"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_group()
                            .group_name(b.string("GroupName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "DeleteUser",
            params: scenario::map(vec![("UserName", scenario::name("user"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_user()
                            .user_name(b.string("UserName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn test_iam_groups_create_group(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "CreateGroup",
            params: scenario::map(vec![("GroupName", scenario::name("group"))]),
            export: vec![
                ("group.id", "$.Group.GroupId"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_group()
                            .group_name(b.string("GroupName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.Group.Arn"),
                scenario::non_empty("$.Group.GroupId"),
                scenario::equals("$.Group.GroupName", scenario::name("group")),
            ]),
            scenario::eventually(6, 500,
                scenario::readback(
                    Call {
                        op: "GetGroup",
                        params: scenario::map(vec![
                            ("GroupName", scenario::name("group")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .get_group()
                                        .group_name(b.string("GroupName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::equals("$.Group.GroupId", scenario::context("group.id")),
                        scenario::equals("$.Group.GroupName", scenario::name("group")),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_groups_add_user_to_group(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "AddUserToGroup",
            params: scenario::map(vec![
                ("GroupName", scenario::name("group")),
                ("UserName", scenario::name("user")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .add_user_to_group()
                            .group_name(b.string("GroupName")?)
                            .user_name(b.string("UserName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::list_contains(
                    Some(Call {
                        op: "GetGroup",
                        params: scenario::map(vec![
                            ("GroupName", scenario::name("group")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .get_group()
                                        .group_name(b.string("GroupName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.Users",
                    vec![
                        scenario::where_entry("$.UserName", scenario::name("user")),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_groups_list_groups_for_user(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "ListGroupsForUser",
            params: scenario::map(vec![("UserName", scenario::name("user"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_groups_for_user()
                            .user_name(b.string("UserName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.Groups",
                vec![
                    scenario::where_entry("$.GroupId", scenario::context("group.id")),
                    scenario::where_entry("$.GroupName", scenario::name("group")),
                ],
            ),
        ],
    }
}

fn test_iam_groups_remove_user_from_group(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "RemoveUserFromGroup",
            params: scenario::map(vec![
                ("GroupName", scenario::name("group")),
                ("UserName", scenario::name("user")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .remove_user_from_group()
                            .group_name(b.string("GroupName")?)
                            .user_name(b.string("UserName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::absent_from_list(
                    Some(Call {
                        op: "ListGroupsForUser",
                        params: scenario::map(vec![
                            ("UserName", scenario::name("user")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_groups_for_user()
                                        .user_name(b.string("UserName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.Groups",
                    vec![
                        scenario::where_entry("$.GroupName", scenario::name("group")),
                    ],
                ),
            ),
            scenario::eventually(6, 500,
                scenario::absent_from_list(
                    Some(Call {
                        op: "GetGroup",
                        params: scenario::map(vec![
                            ("GroupName", scenario::name("group")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .get_group()
                                        .group_name(b.string("GroupName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.Users",
                    vec![
                        scenario::where_entry("$.UserName", scenario::name("user")),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_groups_get_group(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "GetGroup",
            params: scenario::map(vec![("GroupName", scenario::name("group"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_group()
                            .group_name(b.string("GroupName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.Group.GroupId", scenario::context("group.id")),
                scenario::equals("$.Group.GroupName", scenario::name("group")),
                scenario::is_list("$.Users"),
            ]),
        ],
    }
}

fn test_iam_groups_delete_group(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "DeleteGroup",
            params: scenario::map(vec![("GroupName", scenario::name("group"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_group()
                            .group_name(b.string("GroupName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::absent_by_error(
                    Call {
                        op: "GetGroup",
                        params: scenario::map(vec![
                            ("GroupName", scenario::name("group")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .get_group()
                                        .group_name(b.string("GroupName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    scenario::error("NoSuchEntityException", "NoSuchEntity"),
                ),
            ),
        ],
    }
}
