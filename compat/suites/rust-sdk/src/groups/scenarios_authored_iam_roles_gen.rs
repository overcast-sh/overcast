// Code generated by cmd/compatgen; DO NOT EDIT.
//
// Generated from compat/model/authored/iam-roles.json by cmd/compatgen.
// The semantics live in crate::scenario; this file is the data and the typed
// SDK calls.

use std::collections::HashMap;
use std::sync::Arc;

use crate::clients::AwsClients;
use crate::groups::ServiceGroup;
use crate::harness::{TestContext, TestFn};
use crate::scenario::{self, Call, Group, Test};

/// The scenario file every group in this file was generated from.
const SCENARIO_FILE: &str = "compat/model/authored/iam-roles.json";

const GROUP_IAM_ROLES_SHADOW: Group = Group {
    name: "iam-roles-shadow",
    file: SCENARIO_FILE,
};

/// The authored iam-roles groups.
///
/// The client is built once, from the config the suite's hand-written groups
/// share, and cloned into every test — a generated group builds its own rather
/// than adding an accessor to AwsClients for every service the generator learns
/// to cover; nothing else about the client differs.
pub struct ScenariosAuthoredIamRoles {
    client: aws_sdk_iam::Client,
}

impl ScenariosAuthoredIamRoles {
    pub fn new(clients: &Arc<AwsClients>) -> Self {
        let config = aws_sdk_iam::config::Builder::from(clients.config())
            .endpoint_url(clients.endpoint())
            .build();
        Self {
            client: aws_sdk_iam::Client::from_conf(config),
        }
    }
}

impl ServiceGroup for ScenariosAuthoredIamRoles {
    fn name(&self) -> &'static str {
        "scenarios/authored-iam-roles"
    }

    fn impls(&self) -> HashMap<String, TestFn> {
        let mut impls: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            impls.insert(
                "iam-roles-shadow:CreateRole".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_ROLES_SHADOW
                            .run_test(&ctx, "CreateRole", test_iam_roles_shadow_create_role(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-roles-shadow:CreateRoleMalformedDocument".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_ROLES_SHADOW
                            .run_test(&ctx, "CreateRoleMalformedDocument", test_iam_roles_shadow_create_role_malformed_document(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-roles-shadow:GetRole".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_ROLES_SHADOW
                            .run_test(&ctx, "GetRole", test_iam_roles_shadow_get_role(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-roles-shadow:GetRoleReturnsTags".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_ROLES_SHADOW
                            .run_test(&ctx, "GetRoleReturnsTags", test_iam_roles_shadow_get_role_returns_tags(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-roles-shadow:ListRoles".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_ROLES_SHADOW
                            .run_test(&ctx, "ListRoles", test_iam_roles_shadow_list_roles(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-roles-shadow:AttachRolePolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_ROLES_SHADOW
                            .run_test(&ctx, "AttachRolePolicy", test_iam_roles_shadow_attach_role_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-roles-shadow:ListAttachedRolePolicies".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_ROLES_SHADOW
                            .run_test(&ctx, "ListAttachedRolePolicies", test_iam_roles_shadow_list_attached_role_policies(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-roles-shadow:DetachRolePolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_ROLES_SHADOW
                            .run_test(&ctx, "DetachRolePolicy", test_iam_roles_shadow_detach_role_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-roles-shadow:CreateInstanceProfile".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_ROLES_SHADOW
                            .run_test(&ctx, "CreateInstanceProfile", test_iam_roles_shadow_create_instance_profile(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-roles-shadow:AddRoleToInstanceProfile".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_ROLES_SHADOW
                            .run_test(&ctx, "AddRoleToInstanceProfile", test_iam_roles_shadow_add_role_to_instance_profile(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-roles-shadow:GetInstanceProfile".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_ROLES_SHADOW
                            .run_test(&ctx, "GetInstanceProfile", test_iam_roles_shadow_get_instance_profile(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-roles-shadow:DeleteRole".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_ROLES_SHADOW
                            .run_test(&ctx, "DeleteRole", test_iam_roles_shadow_delete_role(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-roles-shadow:PutRolePolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_ROLES_SHADOW
                            .run_test(&ctx, "PutRolePolicy", test_iam_roles_shadow_put_role_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-roles-shadow:GetRolePolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_ROLES_SHADOW
                            .run_test(&ctx, "GetRolePolicy", test_iam_roles_shadow_get_role_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-roles-shadow:ListRolePolicies".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_ROLES_SHADOW
                            .run_test(&ctx, "ListRolePolicies", test_iam_roles_shadow_list_role_policies(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-roles-shadow:DeleteRolePolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_ROLES_SHADOW
                            .run_test(&ctx, "DeleteRolePolicy", test_iam_roles_shadow_delete_role_policy(&client))
                            .await
                    })
                }),
            );
        }
        impls
    }

    fn setups(&self) -> HashMap<String, TestFn> {
        let mut setups: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            setups.insert(
                "iam-roles-shadow".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_ROLES_SHADOW.run_setup(&ctx, setup_iam_roles_shadow(&client)).await
                    })
                }),
            );
        }
        setups
    }

    fn teardowns(&self) -> HashMap<String, TestFn> {
        let mut teardowns: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            teardowns.insert(
                "iam-roles-shadow".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_ROLES_SHADOW.run_teardown(&ctx, teardown_iam_roles_shadow(&client)).await
                    })
                }),
            );
        }
        teardowns
    }
}

fn setup_iam_roles_shadow(client: &aws_sdk_iam::Client) -> Vec<Call> {
    vec![
        Call {
            op: "CreateRole",
            params: scenario::map(vec![
                ("AssumeRolePolicyDocument", scenario::lit(::serde_json::json!("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Principal\":{\"Service\":\"lambda.amazonaws.com\"},\"Action\":\"sts:AssumeRole\"}]}"))),
                ("RoleName", scenario::name("doomed")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_role()
                            .assume_role_policy_document("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Principal\":{\"Service\":\"lambda.amazonaws.com\"},\"Action\":\"sts:AssumeRole\"}]}")
                            .role_name(b.string("RoleName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn teardown_iam_roles_shadow(client: &aws_sdk_iam::Client) -> Vec<Call> {
    vec![
        Call {
            op: "RemoveRoleFromInstanceProfile",
            params: scenario::map(vec![
                ("InstanceProfileName", scenario::name("profile")),
                ("RoleName", scenario::name("role")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .remove_role_from_instance_profile()
                            .instance_profile_name(b.string("InstanceProfileName")?)
                            .role_name(b.string("RoleName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "DeleteInstanceProfile",
            params: scenario::map(vec![
                ("InstanceProfileName", scenario::name("profile")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_instance_profile()
                            .instance_profile_name(b.string("InstanceProfileName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "DeleteRolePolicy",
            params: scenario::map(vec![
                ("PolicyName", scenario::lit(::serde_json::json!("inline-role-policy"))),
                ("RoleName", scenario::name("role")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_role_policy()
                            .policy_name("inline-role-policy")
                            .role_name(b.string("RoleName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "DetachRolePolicy",
            params: scenario::map(vec![
                ("PolicyArn", scenario::lit(::serde_json::json!("arn:aws:iam::aws:policy/AmazonS3ReadOnlyAccess"))),
                ("RoleName", scenario::name("role")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .detach_role_policy()
                            .policy_arn("arn:aws:iam::aws:policy/AmazonS3ReadOnlyAccess")
                            .role_name(b.string("RoleName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "DeleteRole",
            params: scenario::map(vec![("RoleName", scenario::name("role"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_role()
                            .role_name(b.string("RoleName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "DeleteRole",
            params: scenario::map(vec![("RoleName", scenario::name("doomed"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_role()
                            .role_name(b.string("RoleName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "DeleteRole",
            params: scenario::map(vec![("RoleName", scenario::name("malformed"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_role()
                            .role_name(b.string("RoleName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn test_iam_roles_shadow_create_role(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "CreateRole",
            params: scenario::map(vec![
                ("AssumeRolePolicyDocument", scenario::lit(::serde_json::json!("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Principal\":{\"Service\":\"lambda.amazonaws.com\"},\"Action\":\"sts:AssumeRole\"}]}"))),
                ("RoleName", scenario::name("role")),
                ("Tags", scenario::lit(::serde_json::json!([{"Key": "owner", "Value": "compat"}, {"Key": "stage", "Value": "dev"}]))),
            ]),
            export: vec![
                ("role.arn", "$.Role.Arn"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_role()
                            .assume_role_policy_document("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Principal\":{\"Service\":\"lambda.amazonaws.com\"},\"Action\":\"sts:AssumeRole\"}]}")
                            .role_name(b.string("RoleName")?)
                            .tags(
                                aws_sdk_iam::types::Tag::builder()
                                    .key("owner")
                                    .value("compat")
                                    .build()
                                    .map_err(|err| scenario::build_error("Tags[0]", err))?
                            )
                            .tags(
                                aws_sdk_iam::types::Tag::builder()
                                    .key("stage")
                                    .value("dev")
                                    .build()
                                    .map_err(|err| scenario::build_error("Tags[1]", err))?
                            )
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.Role.Arn"),
                scenario::non_empty("$.Role.RoleId"),
                scenario::equals("$.Role.RoleName", scenario::name("role")),
            ]),
            scenario::eventually(6, 500,
                scenario::list_contains(
                    Some(Call {
                        op: "ListRoles",
                        params: scenario::lit(::serde_json::json!({"MaxItems": 1000})),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |_b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_roles()
                                        .max_items(1000)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.Roles",
                    vec![
                        scenario::where_entry("$.RoleName", scenario::name("role")),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_roles_shadow_create_role_malformed_document(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "CreateRole",
            params: scenario::map(vec![
                ("AssumeRolePolicyDocument", scenario::lit(::serde_json::json!("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Resource\":\"*\"}]}"))),
                ("RoleName", scenario::name("malformed")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_role()
                            .assume_role_policy_document("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Resource\":\"*\"}]}")
                            .role_name(b.string("RoleName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::error_code(scenario::error("MalformedPolicyDocumentException", "MalformedPolicyDocument")),
        ],
    }
}

fn test_iam_roles_shadow_get_role(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "GetRole",
            params: scenario::map(vec![("RoleName", scenario::name("role"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_role()
                            .role_name(b.string("RoleName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.Role.Arn", scenario::context("role.arn")),
                scenario::equals_json("$.Role.AssumeRolePolicyDocument", "{\"Statement\":[{\"Action\":\"sts:AssumeRole\",\"Effect\":\"Allow\",\"Principal\":{\"Service\":\"lambda.amazonaws.com\"}}],\"Version\":\"2012-10-17\"}"),
                scenario::equals("$.Role.RoleName", scenario::name("role")),
            ]),
        ],
    }
}

fn test_iam_roles_shadow_get_role_returns_tags(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "GetRole",
            params: scenario::map(vec![("RoleName", scenario::name("role"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_role()
                            .role_name(b.string("RoleName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.Role.Tags",
                vec![
                    scenario::where_entry("$.Key", scenario::lit(::serde_json::json!("owner"))),
                    scenario::where_entry("$.Value", scenario::lit(::serde_json::json!("compat"))),
                ],
            ),
            scenario::list_contains(
                None,
                "$.Role.Tags",
                vec![
                    scenario::where_entry("$.Key", scenario::lit(::serde_json::json!("stage"))),
                    scenario::where_entry("$.Value", scenario::lit(::serde_json::json!("dev"))),
                ],
            ),
        ],
    }
}

fn test_iam_roles_shadow_list_roles(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "ListRoles",
            params: scenario::lit(::serde_json::json!({"MaxItems": 1000})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_roles()
                            .max_items(1000)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.Roles",
                vec![
                    scenario::where_entry("$.RoleName", scenario::name("role")),
                ],
            ),
        ],
    }
}

fn test_iam_roles_shadow_attach_role_policy(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "AttachRolePolicy",
            params: scenario::map(vec![
                ("PolicyArn", scenario::lit(::serde_json::json!("arn:aws:iam::aws:policy/AmazonS3ReadOnlyAccess"))),
                ("RoleName", scenario::name("role")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .attach_role_policy()
                            .policy_arn("arn:aws:iam::aws:policy/AmazonS3ReadOnlyAccess")
                            .role_name(b.string("RoleName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::list_contains(
                    Some(Call {
                        op: "ListAttachedRolePolicies",
                        params: scenario::map(vec![
                            ("RoleName", scenario::name("role")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_attached_role_policies()
                                        .role_name(b.string("RoleName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.AttachedPolicies",
                    vec![
                        scenario::where_entry("$.PolicyArn", scenario::lit(::serde_json::json!("arn:aws:iam::aws:policy/AmazonS3ReadOnlyAccess"))),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_roles_shadow_list_attached_role_policies(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "ListAttachedRolePolicies",
            params: scenario::map(vec![("RoleName", scenario::name("role"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_attached_role_policies()
                            .role_name(b.string("RoleName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.AttachedPolicies",
                vec![
                    scenario::where_entry("$.PolicyArn", scenario::lit(::serde_json::json!("arn:aws:iam::aws:policy/AmazonS3ReadOnlyAccess"))),
                ],
            ),
        ],
    }
}

fn test_iam_roles_shadow_detach_role_policy(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "DetachRolePolicy",
            params: scenario::map(vec![
                ("PolicyArn", scenario::lit(::serde_json::json!("arn:aws:iam::aws:policy/AmazonS3ReadOnlyAccess"))),
                ("RoleName", scenario::name("role")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .detach_role_policy()
                            .policy_arn("arn:aws:iam::aws:policy/AmazonS3ReadOnlyAccess")
                            .role_name(b.string("RoleName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::absent_from_list(
                    Some(Call {
                        op: "ListAttachedRolePolicies",
                        params: scenario::map(vec![
                            ("RoleName", scenario::name("role")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_attached_role_policies()
                                        .role_name(b.string("RoleName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.AttachedPolicies",
                    vec![
                        scenario::where_entry("$.PolicyArn", scenario::lit(::serde_json::json!("arn:aws:iam::aws:policy/AmazonS3ReadOnlyAccess"))),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_roles_shadow_create_instance_profile(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "CreateInstanceProfile",
            params: scenario::map(vec![
                ("InstanceProfileName", scenario::name("profile")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_instance_profile()
                            .instance_profile_name(b.string("InstanceProfileName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.InstanceProfile.Arn"),
                scenario::equals("$.InstanceProfile.InstanceProfileName", scenario::name("profile")),
            ]),
        ],
    }
}

fn test_iam_roles_shadow_add_role_to_instance_profile(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "AddRoleToInstanceProfile",
            params: scenario::map(vec![
                ("InstanceProfileName", scenario::name("profile")),
                ("RoleName", scenario::name("role")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .add_role_to_instance_profile()
                            .instance_profile_name(b.string("InstanceProfileName")?)
                            .role_name(b.string("RoleName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::list_contains(
                    Some(Call {
                        op: "GetInstanceProfile",
                        params: scenario::map(vec![
                            ("InstanceProfileName", scenario::name("profile")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .get_instance_profile()
                                        .instance_profile_name(b.string("InstanceProfileName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.InstanceProfile.Roles",
                    vec![
                        scenario::where_entry("$.RoleName", scenario::name("role")),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_roles_shadow_get_instance_profile(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "GetInstanceProfile",
            params: scenario::map(vec![
                ("InstanceProfileName", scenario::name("profile")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_instance_profile()
                            .instance_profile_name(b.string("InstanceProfileName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.InstanceProfile.InstanceProfileName", scenario::name("profile")),
            ]),
            scenario::list_contains(
                None,
                "$.InstanceProfile.Roles",
                vec![
                    scenario::where_entry("$.RoleName", scenario::name("role")),
                ],
            ),
        ],
    }
}

fn test_iam_roles_shadow_delete_role(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "DeleteRole",
            params: scenario::map(vec![("RoleName", scenario::name("doomed"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_role()
                            .role_name(b.string("RoleName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::absent_from_list(
                    Some(Call {
                        op: "ListRoles",
                        params: scenario::lit(::serde_json::json!({"MaxItems": 1000})),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |_b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_roles()
                                        .max_items(1000)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.Roles",
                    vec![
                        scenario::where_entry("$.RoleName", scenario::name("doomed")),
                    ],
                ),
            ),
            scenario::eventually(6, 500,
                scenario::absent_by_error(
                    Call {
                        op: "GetRole",
                        params: scenario::map(vec![
                            ("RoleName", scenario::name("doomed")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .get_role()
                                        .role_name(b.string("RoleName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    scenario::error("NoSuchEntityException", "NoSuchEntity"),
                ),
            ),
        ],
    }
}

fn test_iam_roles_shadow_put_role_policy(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "PutRolePolicy",
            params: scenario::map(vec![
                ("PolicyDocument", scenario::lit(::serde_json::json!("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Action\":\"logs:*\",\"Resource\":\"*\"}]}"))),
                ("PolicyName", scenario::lit(::serde_json::json!("inline-role-policy"))),
                ("RoleName", scenario::name("role")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .put_role_policy()
                            .policy_document("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Action\":\"logs:*\",\"Resource\":\"*\"}]}")
                            .policy_name("inline-role-policy")
                            .role_name(b.string("RoleName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::readback(
                    Call {
                        op: "GetRolePolicy",
                        params: scenario::map(vec![
                            ("PolicyName", scenario::lit(::serde_json::json!("inline-role-policy"))),
                            ("RoleName", scenario::name("role")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .get_role_policy()
                                        .policy_name("inline-role-policy")
                                        .role_name(b.string("RoleName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::equals_json("$.PolicyDocument", "{\"Statement\":[{\"Action\":\"logs:*\",\"Effect\":\"Allow\",\"Resource\":\"*\"}],\"Version\":\"2012-10-17\"}"),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_roles_shadow_get_role_policy(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "GetRolePolicy",
            params: scenario::map(vec![
                ("PolicyName", scenario::lit(::serde_json::json!("inline-role-policy"))),
                ("RoleName", scenario::name("role")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_role_policy()
                            .policy_name("inline-role-policy")
                            .role_name(b.string("RoleName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals_json("$.PolicyDocument", "{\"Statement\":[{\"Action\":\"logs:*\",\"Effect\":\"Allow\",\"Resource\":\"*\"}],\"Version\":\"2012-10-17\"}"),
                scenario::equals("$.PolicyName", scenario::lit(::serde_json::json!("inline-role-policy"))),
                scenario::equals("$.RoleName", scenario::name("role")),
            ]),
        ],
    }
}

fn test_iam_roles_shadow_list_role_policies(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "ListRolePolicies",
            params: scenario::map(vec![("RoleName", scenario::name("role"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_role_policies()
                            .role_name(b.string("RoleName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.PolicyNames",
                vec![
                    scenario::where_entry("$", scenario::lit(::serde_json::json!("inline-role-policy"))),
                ],
            ),
        ],
    }
}

fn test_iam_roles_shadow_delete_role_policy(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "DeleteRolePolicy",
            params: scenario::map(vec![
                ("PolicyName", scenario::lit(::serde_json::json!("inline-role-policy"))),
                ("RoleName", scenario::name("role")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_role_policy()
                            .policy_name("inline-role-policy")
                            .role_name(b.string("RoleName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::absent_from_list(
                    Some(Call {
                        op: "ListRolePolicies",
                        params: scenario::map(vec![
                            ("RoleName", scenario::name("role")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_role_policies()
                                        .role_name(b.string("RoleName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.PolicyNames",
                    vec![
                        scenario::where_entry("$", scenario::lit(::serde_json::json!("inline-role-policy"))),
                    ],
                ),
            ),
        ],
    }
}
