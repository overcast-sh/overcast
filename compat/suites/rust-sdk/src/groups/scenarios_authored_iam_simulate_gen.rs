// Code generated by cmd/compatgen; DO NOT EDIT.
//
// Generated from compat/model/authored/iam-simulate.json by cmd/compatgen.
// The semantics live in crate::scenario; this file is the data and the typed
// SDK calls.

use std::collections::HashMap;
use std::sync::Arc;

use crate::clients::AwsClients;
use crate::groups::ServiceGroup;
use crate::harness::{TestContext, TestFn};
use crate::scenario::{self, Call, Group, Test};

/// The scenario file every group in this file was generated from.
const SCENARIO_FILE: &str = "compat/model/authored/iam-simulate.json";

const GROUP_IAM_SIMULATE_SHADOW: Group = Group {
    name: "iam-simulate-shadow",
    file: SCENARIO_FILE,
};

/// The authored iam-simulate groups.
///
/// The client is built once, from the config the suite's hand-written groups
/// share, and cloned into every test — a generated group builds its own rather
/// than adding an accessor to AwsClients for every service the generator learns
/// to cover; nothing else about the client differs.
pub struct ScenariosAuthoredIamSimulate {
    client: aws_sdk_iam::Client,
}

impl ScenariosAuthoredIamSimulate {
    pub fn new(clients: &Arc<AwsClients>) -> Self {
        let config = aws_sdk_iam::config::Builder::from(clients.config())
            .endpoint_url(clients.endpoint())
            .build();
        Self {
            client: aws_sdk_iam::Client::from_conf(config),
        }
    }
}

impl ServiceGroup for ScenariosAuthoredIamSimulate {
    fn name(&self) -> &'static str {
        "scenarios/authored-iam-simulate"
    }

    fn impls(&self) -> HashMap<String, TestFn> {
        let mut impls: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            impls.insert(
                "iam-simulate-shadow:SimulateCustomPolicyAllowed".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_SIMULATE_SHADOW
                            .run_test(&ctx, "SimulateCustomPolicyAllowed", test_iam_simulate_shadow_simulate_custom_policy_allowed(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-simulate-shadow:SimulateCustomPolicyImplicitDeny".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_SIMULATE_SHADOW
                            .run_test(&ctx, "SimulateCustomPolicyImplicitDeny", test_iam_simulate_shadow_simulate_custom_policy_implicit_deny(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-simulate-shadow:SimulateCustomPolicyExplicitDeny".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_SIMULATE_SHADOW
                            .run_test(&ctx, "SimulateCustomPolicyExplicitDeny", test_iam_simulate_shadow_simulate_custom_policy_explicit_deny(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-simulate-shadow:SimulatePrincipalPolicyAllowed".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_SIMULATE_SHADOW
                            .run_test(&ctx, "SimulatePrincipalPolicyAllowed", test_iam_simulate_shadow_simulate_principal_policy_allowed(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-simulate-shadow:SimulatePrincipalPolicyImplicitDeny".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_SIMULATE_SHADOW
                            .run_test(&ctx, "SimulatePrincipalPolicyImplicitDeny", test_iam_simulate_shadow_simulate_principal_policy_implicit_deny(&client))
                            .await
                    })
                }),
            );
        }
        impls
    }

    fn setups(&self) -> HashMap<String, TestFn> {
        let mut setups: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            setups.insert(
                "iam-simulate-shadow".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_SIMULATE_SHADOW.run_setup(&ctx, setup_iam_simulate_shadow(&client)).await
                    })
                }),
            );
        }
        setups
    }

    fn teardowns(&self) -> HashMap<String, TestFn> {
        let mut teardowns: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            teardowns.insert(
                "iam-simulate-shadow".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_SIMULATE_SHADOW.run_teardown(&ctx, teardown_iam_simulate_shadow(&client)).await
                    })
                }),
            );
        }
        teardowns
    }
}

fn setup_iam_simulate_shadow(client: &aws_sdk_iam::Client) -> Vec<Call> {
    vec![
        Call {
            op: "CreateUser",
            params: scenario::map(vec![("UserName", scenario::name("user"))]),
            export: vec![
                ("user.arn", "$.User.Arn"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_user()
                            .user_name(b.string("UserName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "PutUserPolicy",
            params: scenario::map(vec![
                ("PolicyDocument", scenario::concat(vec![
                    scenario::lit(::serde_json::json!("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Action\":\"s3:GetObject\",\"Resource\":\"arn:aws:s3:::")),
                    scenario::name("sim"),
                    scenario::lit(::serde_json::json!("/*\"}]}")),
                ])),
                ("PolicyName", scenario::lit(::serde_json::json!("sim-allow-read"))),
                ("UserName", scenario::name("user")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .put_user_policy()
                            .policy_document(b.string("PolicyDocument")?)
                            .policy_name("sim-allow-read")
                            .user_name(b.string("UserName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn teardown_iam_simulate_shadow(client: &aws_sdk_iam::Client) -> Vec<Call> {
    vec![
        Call {
            op: "DeleteUserPolicy",
            params: scenario::map(vec![
                ("PolicyName", scenario::lit(::serde_json::json!("sim-allow-read"))),
                ("UserName", scenario::name("user")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_user_policy()
                            .policy_name("sim-allow-read")
                            .user_name(b.string("UserName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "DeleteUser",
            params: scenario::map(vec![("UserName", scenario::name("user"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_user()
                            .user_name(b.string("UserName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn test_iam_simulate_shadow_simulate_custom_policy_allowed(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "SimulateCustomPolicy",
            params: scenario::map(vec![
                ("ActionNames", scenario::lit(::serde_json::json!(["s3:GetObject"]))),
                ("PolicyInputList", scenario::list(vec![
                    scenario::concat(vec![
                        scenario::lit(::serde_json::json!("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Action\":\"s3:GetObject\",\"Resource\":\"arn:aws:s3:::")),
                        scenario::name("sim"),
                        scenario::lit(::serde_json::json!("/*\"}]}")),
                    ]),
                ])),
                ("ResourceArns", scenario::list(vec![
                    scenario::concat(vec![
                        scenario::lit(::serde_json::json!("arn:aws:s3:::")),
                        scenario::name("sim"),
                        scenario::lit(::serde_json::json!("/report.csv")),
                    ]),
                ])),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .simulate_custom_policy()
                            .action_names("s3:GetObject")
                            .policy_input_list(b.string("PolicyInputList[0]")?)
                            .resource_arns(b.string("ResourceArns[0]")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.EvaluationResults",
                vec![
                    scenario::where_entry("$.EvalActionName", scenario::lit(::serde_json::json!("s3:GetObject"))),
                    scenario::where_entry("$.EvalDecision", scenario::lit(::serde_json::json!("allowed"))),
                ],
            ),
        ],
    }
}

fn test_iam_simulate_shadow_simulate_custom_policy_implicit_deny(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "SimulateCustomPolicy",
            params: scenario::map(vec![
                ("ActionNames", scenario::lit(::serde_json::json!(["s3:PutObject"]))),
                ("PolicyInputList", scenario::list(vec![
                    scenario::concat(vec![
                        scenario::lit(::serde_json::json!("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Action\":\"s3:GetObject\",\"Resource\":\"arn:aws:s3:::")),
                        scenario::name("sim"),
                        scenario::lit(::serde_json::json!("/*\"}]}")),
                    ]),
                ])),
                ("ResourceArns", scenario::list(vec![
                    scenario::concat(vec![
                        scenario::lit(::serde_json::json!("arn:aws:s3:::")),
                        scenario::name("sim"),
                        scenario::lit(::serde_json::json!("/report.csv")),
                    ]),
                ])),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .simulate_custom_policy()
                            .action_names("s3:PutObject")
                            .policy_input_list(b.string("PolicyInputList[0]")?)
                            .resource_arns(b.string("ResourceArns[0]")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.EvaluationResults",
                vec![
                    scenario::where_entry("$.EvalActionName", scenario::lit(::serde_json::json!("s3:PutObject"))),
                    scenario::where_entry("$.EvalDecision", scenario::lit(::serde_json::json!("implicitDeny"))),
                ],
            ),
        ],
    }
}

fn test_iam_simulate_shadow_simulate_custom_policy_explicit_deny(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "SimulateCustomPolicy",
            params: scenario::map(vec![
                ("ActionNames", scenario::lit(::serde_json::json!(["s3:DeleteObject"]))),
                ("PolicyInputList", scenario::lit(::serde_json::json!(["{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Action\":\"s3:*\",\"Resource\":\"*\"},{\"Effect\":\"Deny\",\"Action\":\"s3:DeleteObject\",\"Resource\":\"*\"}]}"]))),
                ("ResourceArns", scenario::list(vec![
                    scenario::concat(vec![
                        scenario::lit(::serde_json::json!("arn:aws:s3:::")),
                        scenario::name("sim"),
                        scenario::lit(::serde_json::json!("/report.csv")),
                    ]),
                ])),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .simulate_custom_policy()
                            .action_names("s3:DeleteObject")
                            .policy_input_list("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Action\":\"s3:*\",\"Resource\":\"*\"},{\"Effect\":\"Deny\",\"Action\":\"s3:DeleteObject\",\"Resource\":\"*\"}]}")
                            .resource_arns(b.string("ResourceArns[0]")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.EvaluationResults",
                vec![
                    scenario::where_entry("$.EvalActionName", scenario::lit(::serde_json::json!("s3:DeleteObject"))),
                    scenario::where_entry("$.EvalDecision", scenario::lit(::serde_json::json!("explicitDeny"))),
                ],
            ),
        ],
    }
}

fn test_iam_simulate_shadow_simulate_principal_policy_allowed(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "SimulatePrincipalPolicy",
            params: scenario::map(vec![
                ("ActionNames", scenario::lit(::serde_json::json!(["s3:GetObject"]))),
                ("PolicySourceArn", scenario::context("user.arn")),
                ("ResourceArns", scenario::list(vec![
                    scenario::concat(vec![
                        scenario::lit(::serde_json::json!("arn:aws:s3:::")),
                        scenario::name("sim"),
                        scenario::lit(::serde_json::json!("/report.csv")),
                    ]),
                ])),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .simulate_principal_policy()
                            .action_names("s3:GetObject")
                            .policy_source_arn(b.string("PolicySourceArn")?)
                            .resource_arns(b.string("ResourceArns[0]")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.EvaluationResults",
                vec![
                    scenario::where_entry("$.EvalActionName", scenario::lit(::serde_json::json!("s3:GetObject"))),
                    scenario::where_entry("$.EvalDecision", scenario::lit(::serde_json::json!("allowed"))),
                ],
            ),
            scenario::list_contains(
                None,
                "$.EvaluationResults[0].MatchedStatements",
                vec![
                    scenario::where_entry("$.SourcePolicyId", scenario::lit(::serde_json::json!("sim-allow-read"))),
                ],
            ),
        ],
    }
}

fn test_iam_simulate_shadow_simulate_principal_policy_implicit_deny(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "SimulatePrincipalPolicy",
            params: scenario::map(vec![
                ("ActionNames", scenario::lit(::serde_json::json!(["s3:DeleteObject"]))),
                ("PolicySourceArn", scenario::context("user.arn")),
                ("ResourceArns", scenario::list(vec![
                    scenario::concat(vec![
                        scenario::lit(::serde_json::json!("arn:aws:s3:::")),
                        scenario::name("sim"),
                        scenario::lit(::serde_json::json!("/report.csv")),
                    ]),
                ])),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .simulate_principal_policy()
                            .action_names("s3:DeleteObject")
                            .policy_source_arn(b.string("PolicySourceArn")?)
                            .resource_arns(b.string("ResourceArns[0]")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.EvaluationResults",
                vec![
                    scenario::where_entry("$.EvalActionName", scenario::lit(::serde_json::json!("s3:DeleteObject"))),
                    scenario::where_entry("$.EvalDecision", scenario::lit(::serde_json::json!("implicitDeny"))),
                ],
            ),
        ],
    }
}
