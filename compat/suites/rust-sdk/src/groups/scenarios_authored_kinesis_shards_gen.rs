// Code generated by cmd/compatgen; DO NOT EDIT.
//
// Generated from compat/model/authored/kinesis-shards.json by cmd/compatgen.
// The semantics live in crate::scenario; this file is the data and the typed
// SDK calls.

use std::collections::HashMap;
use std::sync::Arc;

use crate::clients::AwsClients;
use crate::groups::ServiceGroup;
use crate::harness::{TestContext, TestFn};
use crate::scenario::{self, Call, Group, Test};

/// The scenario file every group in this file was generated from.
const SCENARIO_FILE: &str = "compat/model/authored/kinesis-shards.json";

const GROUP_KINESIS_SHARDS_SHADOW: Group = Group {
    name: "kinesis-shards-shadow",
    file: SCENARIO_FILE,
};

/// The authored kinesis-shards groups.
///
/// The client is built once, from the config the suite's hand-written groups
/// share, and cloned into every test — a generated group builds its own rather
/// than adding an accessor to AwsClients for every service the generator learns
/// to cover; nothing else about the client differs.
pub struct ScenariosAuthoredKinesisShards {
    client: aws_sdk_kinesis::Client,
}

impl ScenariosAuthoredKinesisShards {
    pub fn new(clients: &Arc<AwsClients>) -> Self {
        let config = aws_sdk_kinesis::config::Builder::from(clients.config())
            .endpoint_url(clients.endpoint())
            .build();
        Self {
            client: aws_sdk_kinesis::Client::from_conf(config),
        }
    }
}

impl ServiceGroup for ScenariosAuthoredKinesisShards {
    fn name(&self) -> &'static str {
        "scenarios/authored-kinesis-shards"
    }

    fn impls(&self) -> HashMap<String, TestFn> {
        let mut impls: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            impls.insert(
                "kinesis-shards-shadow:ListShards".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_KINESIS_SHARDS_SHADOW
                            .run_test(&ctx, "ListShards", test_kinesis_shards_shadow_list_shards(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "kinesis-shards-shadow:SplitShard".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_KINESIS_SHARDS_SHADOW
                            .run_test(&ctx, "SplitShard", test_kinesis_shards_shadow_split_shard(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "kinesis-shards-shadow:MergeShards".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_KINESIS_SHARDS_SHADOW
                            .run_test(&ctx, "MergeShards", test_kinesis_shards_shadow_merge_shards(&client))
                            .await
                    })
                }),
            );
        }
        impls
    }

    fn setups(&self) -> HashMap<String, TestFn> {
        let mut setups: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            setups.insert(
                "kinesis-shards-shadow".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_KINESIS_SHARDS_SHADOW.run_setup(&ctx, setup_kinesis_shards_shadow(&client)).await
                    })
                }),
            );
        }
        setups
    }

    fn teardowns(&self) -> HashMap<String, TestFn> {
        let mut teardowns: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            teardowns.insert(
                "kinesis-shards-shadow".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_KINESIS_SHARDS_SHADOW.run_teardown(&ctx, teardown_kinesis_shards_shadow(&client)).await
                    })
                }),
            );
        }
        teardowns
    }
}

fn setup_kinesis_shards_shadow(client: &aws_sdk_kinesis::Client) -> Vec<Call> {
    vec![
        Call {
            op: "CreateStream",
            params: scenario::map(vec![
                ("ShardCount", scenario::lit(::serde_json::json!(2))),
                ("StreamName", scenario::name("s")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_stream()
                            .shard_count(2)
                            .stream_name(b.string("StreamName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn teardown_kinesis_shards_shadow(client: &aws_sdk_kinesis::Client) -> Vec<Call> {
    vec![
        Call {
            op: "DeleteStream",
            params: scenario::map(vec![("StreamName", scenario::name("s"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_stream()
                            .stream_name(b.string("StreamName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn test_kinesis_shards_shadow_list_shards(client: &aws_sdk_kinesis::Client) -> Test {
    Test {
        call: Call {
            op: "ListShards",
            params: scenario::map(vec![("StreamName", scenario::name("s"))]),
            export: vec![
                ("shard.first", "$.Shards[0].ShardId"),
                ("shard.second", "$.Shards[1].ShardId"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_shards()
                            .stream_name(b.string("StreamName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.Shards[0].HashKeyRange.StartingHashKey", scenario::lit(::serde_json::json!("0"))),
                scenario::non_empty("$.Shards[0].ShardId"),
                scenario::non_empty("$.Shards[1].ShardId"),
                scenario::missing("$.Shards[2]"),
            ]),
            scenario::eventually(30, 500,
                scenario::readback(
                    Call {
                        op: "DescribeStreamSummary",
                        params: scenario::map(vec![
                            ("StreamName", scenario::name("s")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .describe_stream_summary()
                                        .stream_name(b.string("StreamName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::equals("$.StreamDescriptionSummary.OpenShardCount", scenario::lit(::serde_json::json!(2))),
                        scenario::equals("$.StreamDescriptionSummary.StreamStatus", scenario::lit(::serde_json::json!("ACTIVE"))),
                    ],
                ),
            ),
        ],
    }
}

fn test_kinesis_shards_shadow_split_shard(client: &aws_sdk_kinesis::Client) -> Test {
    Test {
        call: Call {
            op: "SplitShard",
            params: scenario::map(vec![
                ("NewStartingHashKey", scenario::lit(::serde_json::json!("85070591730234615865843651857942052863"))),
                ("ShardToSplit", scenario::context("shard.first")),
                ("StreamName", scenario::name("s")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .split_shard()
                            .new_starting_hash_key("85070591730234615865843651857942052863")
                            .shard_to_split(b.string("ShardToSplit")?)
                            .stream_name(b.string("StreamName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(30, 500,
                scenario::readback(
                    Call {
                        op: "DescribeStreamSummary",
                        params: scenario::map(vec![
                            ("StreamName", scenario::name("s")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .describe_stream_summary()
                                        .stream_name(b.string("StreamName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::equals("$.StreamDescriptionSummary.OpenShardCount", scenario::lit(::serde_json::json!(3))),
                        scenario::equals("$.StreamDescriptionSummary.StreamStatus", scenario::lit(::serde_json::json!("ACTIVE"))),
                    ],
                ),
            ),
            scenario::readback(
                Call {
                    op: "ListShards",
                    params: scenario::map(vec![
                        ("ShardFilter", scenario::lit(::serde_json::json!({"Type": "AT_LATEST"}))),
                        ("StreamName", scenario::name("s")),
                    ]),
                    export: vec![
                        ("child.first", "$.Shards[1].ShardId"),
                        ("child.second", "$.Shards[2].ShardId"),
                    ],
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_shards()
                                    .shard_filter(
                                        aws_sdk_kinesis::types::ShardFilter::builder()
                                            .r#type(aws_sdk_kinesis::types::ShardFilterType::from("AT_LATEST"))
                                            .build()
                                            .map_err(|err| scenario::build_error("ShardFilter", err))?
                                    )
                                    .stream_name(b.string("StreamName")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                vec![
                    scenario::equals("$.Shards[0].ShardId", scenario::context("shard.second")),
                    scenario::non_empty("$.Shards[1].ShardId"),
                    scenario::non_empty("$.Shards[2].ShardId"),
                    scenario::missing("$.Shards[3]"),
                ],
            ),
            scenario::list_contains(
                Some(Call {
                    op: "ListShards",
                    params: scenario::map(vec![
                        ("ShardFilter", scenario::lit(::serde_json::json!({"Type": "AT_LATEST"}))),
                        ("StreamName", scenario::name("s")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_shards()
                                    .shard_filter(
                                        aws_sdk_kinesis::types::ShardFilter::builder()
                                            .r#type(aws_sdk_kinesis::types::ShardFilterType::from("AT_LATEST"))
                                            .build()
                                            .map_err(|err| scenario::build_error("ShardFilter", err))?
                                    )
                                    .stream_name(b.string("StreamName")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                }),
                "$.Shards",
                vec![
                    scenario::where_entry("$.HashKeyRange.StartingHashKey", scenario::lit(::serde_json::json!("0"))),
                ],
            ),
            scenario::list_contains(
                Some(Call {
                    op: "ListShards",
                    params: scenario::map(vec![
                        ("ShardFilter", scenario::lit(::serde_json::json!({"Type": "AT_LATEST"}))),
                        ("StreamName", scenario::name("s")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_shards()
                                    .shard_filter(
                                        aws_sdk_kinesis::types::ShardFilter::builder()
                                            .r#type(aws_sdk_kinesis::types::ShardFilterType::from("AT_LATEST"))
                                            .build()
                                            .map_err(|err| scenario::build_error("ShardFilter", err))?
                                    )
                                    .stream_name(b.string("StreamName")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                }),
                "$.Shards",
                vec![
                    scenario::where_entry("$.HashKeyRange.StartingHashKey", scenario::lit(::serde_json::json!("85070591730234615865843651857942052863"))),
                ],
            ),
        ],
    }
}

fn test_kinesis_shards_shadow_merge_shards(client: &aws_sdk_kinesis::Client) -> Test {
    Test {
        call: Call {
            op: "MergeShards",
            params: scenario::map(vec![
                ("AdjacentShardToMerge", scenario::context("child.second")),
                ("ShardToMerge", scenario::context("child.first")),
                ("StreamName", scenario::name("s")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .merge_shards()
                            .adjacent_shard_to_merge(b.string("AdjacentShardToMerge")?)
                            .shard_to_merge(b.string("ShardToMerge")?)
                            .stream_name(b.string("StreamName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(30, 500,
                scenario::readback(
                    Call {
                        op: "DescribeStreamSummary",
                        params: scenario::map(vec![
                            ("StreamName", scenario::name("s")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .describe_stream_summary()
                                        .stream_name(b.string("StreamName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::equals("$.StreamDescriptionSummary.OpenShardCount", scenario::lit(::serde_json::json!(2))),
                        scenario::equals("$.StreamDescriptionSummary.StreamStatus", scenario::lit(::serde_json::json!("ACTIVE"))),
                    ],
                ),
            ),
            scenario::readback(
                Call {
                    op: "ListShards",
                    params: scenario::map(vec![
                        ("ShardFilter", scenario::lit(::serde_json::json!({"Type": "AT_LATEST"}))),
                        ("StreamName", scenario::name("s")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_shards()
                                    .shard_filter(
                                        aws_sdk_kinesis::types::ShardFilter::builder()
                                            .r#type(aws_sdk_kinesis::types::ShardFilterType::from("AT_LATEST"))
                                            .build()
                                            .map_err(|err| scenario::build_error("ShardFilter", err))?
                                    )
                                    .stream_name(b.string("StreamName")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                vec![
                    scenario::equals("$.Shards[0].ShardId", scenario::context("shard.second")),
                    scenario::equals("$.Shards[1].HashKeyRange.StartingHashKey", scenario::lit(::serde_json::json!("0"))),
                    scenario::missing("$.Shards[2]"),
                ],
            ),
            scenario::absent_from_list(
                Some(Call {
                    op: "ListShards",
                    params: scenario::map(vec![
                        ("ShardFilter", scenario::lit(::serde_json::json!({"Type": "AT_LATEST"}))),
                        ("StreamName", scenario::name("s")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_shards()
                                    .shard_filter(
                                        aws_sdk_kinesis::types::ShardFilter::builder()
                                            .r#type(aws_sdk_kinesis::types::ShardFilterType::from("AT_LATEST"))
                                            .build()
                                            .map_err(|err| scenario::build_error("ShardFilter", err))?
                                    )
                                    .stream_name(b.string("StreamName")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                }),
                "$.Shards",
                vec![
                    scenario::where_entry("$.ShardId", scenario::context("child.first")),
                ],
            ),
            scenario::absent_from_list(
                Some(Call {
                    op: "ListShards",
                    params: scenario::map(vec![
                        ("ShardFilter", scenario::lit(::serde_json::json!({"Type": "AT_LATEST"}))),
                        ("StreamName", scenario::name("s")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_shards()
                                    .shard_filter(
                                        aws_sdk_kinesis::types::ShardFilter::builder()
                                            .r#type(aws_sdk_kinesis::types::ShardFilterType::from("AT_LATEST"))
                                            .build()
                                            .map_err(|err| scenario::build_error("ShardFilter", err))?
                                    )
                                    .stream_name(b.string("StreamName")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                }),
                "$.Shards",
                vec![
                    scenario::where_entry("$.ShardId", scenario::context("child.second")),
                ],
            ),
        ],
    }
}
