// Code generated by cmd/compatgen; DO NOT EDIT.
//
// Generated from compat/model/authored/kinesis-streams.json by cmd/compatgen.
// The semantics live in crate::scenario; this file is the data and the typed
// SDK calls.

use std::collections::HashMap;
use std::sync::Arc;

use crate::clients::AwsClients;
use crate::groups::ServiceGroup;
use crate::harness::{TestContext, TestFn};
use crate::scenario::{self, Call, Group, Test};

/// The scenario file every group in this file was generated from.
const SCENARIO_FILE: &str = "compat/model/authored/kinesis-streams.json";

const GROUP_KINESIS_STREAMS_SHADOW: Group = Group {
    name: "kinesis-streams-shadow",
    file: SCENARIO_FILE,
};

/// The authored kinesis-streams groups.
///
/// The client is built once, from the config the suite's hand-written groups
/// share, and cloned into every test — a generated group builds its own rather
/// than adding an accessor to AwsClients for every service the generator learns
/// to cover; nothing else about the client differs.
pub struct ScenariosAuthoredKinesisStreams {
    client: aws_sdk_kinesis::Client,
}

impl ScenariosAuthoredKinesisStreams {
    pub fn new(clients: &Arc<AwsClients>) -> Self {
        let config = aws_sdk_kinesis::config::Builder::from(clients.config())
            .endpoint_url(clients.endpoint())
            .build();
        Self {
            client: aws_sdk_kinesis::Client::from_conf(config),
        }
    }
}

impl ServiceGroup for ScenariosAuthoredKinesisStreams {
    fn name(&self) -> &'static str {
        "scenarios/authored-kinesis-streams"
    }

    fn impls(&self) -> HashMap<String, TestFn> {
        let mut impls: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            impls.insert(
                "kinesis-streams-shadow:CreateStream".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_KINESIS_STREAMS_SHADOW
                            .run_test(&ctx, "CreateStream", test_kinesis_streams_shadow_create_stream(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "kinesis-streams-shadow:DescribeStream".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_KINESIS_STREAMS_SHADOW
                            .run_test(&ctx, "DescribeStream", test_kinesis_streams_shadow_describe_stream(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "kinesis-streams-shadow:DescribeStreamSummary".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_KINESIS_STREAMS_SHADOW
                            .run_test(&ctx, "DescribeStreamSummary", test_kinesis_streams_shadow_describe_stream_summary(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "kinesis-streams-shadow:ListStreams".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_KINESIS_STREAMS_SHADOW
                            .run_test(&ctx, "ListStreams", test_kinesis_streams_shadow_list_streams(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "kinesis-streams-shadow:AddTagsToStream".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_KINESIS_STREAMS_SHADOW
                            .run_test(&ctx, "AddTagsToStream", test_kinesis_streams_shadow_add_tags_to_stream(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "kinesis-streams-shadow:ListTagsForStream".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_KINESIS_STREAMS_SHADOW
                            .run_test(&ctx, "ListTagsForStream", test_kinesis_streams_shadow_list_tags_for_stream(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "kinesis-streams-shadow:DeleteStream".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_KINESIS_STREAMS_SHADOW
                            .run_test(&ctx, "DeleteStream", test_kinesis_streams_shadow_delete_stream(&client))
                            .await
                    })
                }),
            );
        }
        impls
    }

    fn setups(&self) -> HashMap<String, TestFn> {
        let mut setups: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            setups.insert(
                "kinesis-streams-shadow".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_KINESIS_STREAMS_SHADOW.run_setup(&ctx, setup_kinesis_streams_shadow(&client)).await
                    })
                }),
            );
        }
        setups
    }

    fn teardowns(&self) -> HashMap<String, TestFn> {
        let mut teardowns: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            teardowns.insert(
                "kinesis-streams-shadow".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_KINESIS_STREAMS_SHADOW.run_teardown(&ctx, teardown_kinesis_streams_shadow(&client)).await
                    })
                }),
            );
        }
        teardowns
    }
}

fn setup_kinesis_streams_shadow(client: &aws_sdk_kinesis::Client) -> Vec<Call> {
    vec![
        Call {
            op: "CreateStream",
            params: scenario::map(vec![
                ("ShardCount", scenario::lit(::serde_json::json!(1))),
                ("StreamName", scenario::name("s")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_stream()
                            .shard_count(1)
                            .stream_name(b.string("StreamName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn teardown_kinesis_streams_shadow(client: &aws_sdk_kinesis::Client) -> Vec<Call> {
    vec![
        Call {
            op: "DeleteStream",
            params: scenario::map(vec![("StreamName", scenario::name("created"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_stream()
                            .stream_name(b.string("StreamName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "DeleteStream",
            params: scenario::map(vec![("StreamName", scenario::name("s"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_stream()
                            .stream_name(b.string("StreamName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn test_kinesis_streams_shadow_create_stream(client: &aws_sdk_kinesis::Client) -> Test {
    Test {
        call: Call {
            op: "CreateStream",
            params: scenario::map(vec![
                ("ShardCount", scenario::lit(::serde_json::json!(1))),
                ("StreamName", scenario::name("created")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_stream()
                            .shard_count(1)
                            .stream_name(b.string("StreamName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(30, 500,
                scenario::readback(
                    Call {
                        op: "DescribeStreamSummary",
                        params: scenario::map(vec![
                            ("StreamName", scenario::name("created")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .describe_stream_summary()
                                        .stream_name(b.string("StreamName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::non_empty("$.StreamDescriptionSummary.StreamARN"),
                        scenario::equals("$.StreamDescriptionSummary.StreamName", scenario::name("created")),
                        scenario::equals("$.StreamDescriptionSummary.StreamStatus", scenario::lit(::serde_json::json!("ACTIVE"))),
                    ],
                ),
            ),
            scenario::list_contains(
                Some(Call {
                    op: "ListStreams",
                    params: scenario::lit(::serde_json::json!({})),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |_b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_streams()
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                }),
                "$.StreamNames",
                vec![
                    scenario::where_entry("$", scenario::name("created")),
                ],
            ),
        ],
    }
}

fn test_kinesis_streams_shadow_describe_stream(client: &aws_sdk_kinesis::Client) -> Test {
    Test {
        call: Call {
            op: "DescribeStream",
            params: scenario::map(vec![("StreamName", scenario::name("s"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .describe_stream()
                            .stream_name(b.string("StreamName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.StreamDescription.StreamARN"),
                scenario::equals("$.StreamDescription.StreamName", scenario::name("s")),
            ]),
            scenario::eventually(30, 500,
                scenario::readback(
                    Call {
                        op: "DescribeStream",
                        params: scenario::map(vec![
                            ("StreamName", scenario::name("s")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .describe_stream()
                                        .stream_name(b.string("StreamName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::non_empty("$.StreamDescription.Shards[0].ShardId"),
                        scenario::equals("$.StreamDescription.StreamStatus", scenario::lit(::serde_json::json!("ACTIVE"))),
                    ],
                ),
            ),
        ],
    }
}

fn test_kinesis_streams_shadow_describe_stream_summary(client: &aws_sdk_kinesis::Client) -> Test {
    Test {
        call: Call {
            op: "DescribeStreamSummary",
            params: scenario::map(vec![("StreamName", scenario::name("s"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .describe_stream_summary()
                            .stream_name(b.string("StreamName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.StreamDescriptionSummary.StreamARN"),
                scenario::equals("$.StreamDescriptionSummary.StreamName", scenario::name("s")),
            ]),
            scenario::eventually(30, 500,
                scenario::readback(
                    Call {
                        op: "DescribeStreamSummary",
                        params: scenario::map(vec![
                            ("StreamName", scenario::name("s")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .describe_stream_summary()
                                        .stream_name(b.string("StreamName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::equals("$.StreamDescriptionSummary.OpenShardCount", scenario::lit(::serde_json::json!(1))),
                        scenario::equals("$.StreamDescriptionSummary.StreamStatus", scenario::lit(::serde_json::json!("ACTIVE"))),
                    ],
                ),
            ),
        ],
    }
}

fn test_kinesis_streams_shadow_list_streams(client: &aws_sdk_kinesis::Client) -> Test {
    Test {
        call: Call {
            op: "ListStreams",
            params: scenario::lit(::serde_json::json!({})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_streams()
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.StreamNames",
                vec![
                    scenario::where_entry("$", scenario::name("s")),
                ],
            ),
        ],
    }
}

fn test_kinesis_streams_shadow_add_tags_to_stream(client: &aws_sdk_kinesis::Client) -> Test {
    Test {
        call: Call {
            op: "AddTagsToStream",
            params: scenario::map(vec![
                ("StreamName", scenario::name("s")),
                ("Tags", scenario::lit(::serde_json::json!({"env": "compat", "project": "overcast"}))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .add_tags_to_stream()
                            .stream_name(b.string("StreamName")?)
                            .tags("env", "compat")
                            .tags("project", "overcast")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                Some(Call {
                    op: "ListTagsForStream",
                    params: scenario::map(vec![("StreamName", scenario::name("s"))]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_tags_for_stream()
                                    .stream_name(b.string("StreamName")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                }),
                "$.Tags",
                vec![
                    scenario::where_entry("$.Key", scenario::lit(::serde_json::json!("env"))),
                    scenario::where_entry("$.Value", scenario::lit(::serde_json::json!("compat"))),
                ],
            ),
            scenario::list_contains(
                Some(Call {
                    op: "ListTagsForStream",
                    params: scenario::map(vec![("StreamName", scenario::name("s"))]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_tags_for_stream()
                                    .stream_name(b.string("StreamName")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                }),
                "$.Tags",
                vec![
                    scenario::where_entry("$.Key", scenario::lit(::serde_json::json!("project"))),
                    scenario::where_entry("$.Value", scenario::lit(::serde_json::json!("overcast"))),
                ],
            ),
        ],
    }
}

fn test_kinesis_streams_shadow_list_tags_for_stream(client: &aws_sdk_kinesis::Client) -> Test {
    Test {
        call: Call {
            op: "ListTagsForStream",
            params: scenario::map(vec![("StreamName", scenario::name("s"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_tags_for_stream()
                            .stream_name(b.string("StreamName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.Tags",
                vec![
                    scenario::where_entry("$.Key", scenario::lit(::serde_json::json!("env"))),
                    scenario::where_entry("$.Value", scenario::lit(::serde_json::json!("compat"))),
                ],
            ),
            scenario::list_contains(
                None,
                "$.Tags",
                vec![
                    scenario::where_entry("$.Key", scenario::lit(::serde_json::json!("project"))),
                    scenario::where_entry("$.Value", scenario::lit(::serde_json::json!("overcast"))),
                ],
            ),
        ],
    }
}

fn test_kinesis_streams_shadow_delete_stream(client: &aws_sdk_kinesis::Client) -> Test {
    Test {
        call: Call {
            op: "DeleteStream",
            params: scenario::map(vec![("StreamName", scenario::name("created"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_stream()
                            .stream_name(b.string("StreamName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(20, 500,
                scenario::absent_from_list(
                    Some(Call {
                        op: "ListStreams",
                        params: scenario::lit(::serde_json::json!({})),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |_b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_streams()
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.StreamNames",
                    vec![
                        scenario::where_entry("$", scenario::name("created")),
                    ],
                ),
            ),
        ],
    }
}
