// Code generated by cmd/compatgen; DO NOT EDIT.
//
// Generated from compat/model/authored/logs-groups.json by cmd/compatgen.
// The semantics live in crate::scenario; this file is the data and the typed
// SDK calls.

use std::collections::HashMap;
use std::sync::Arc;

use crate::clients::AwsClients;
use crate::groups::ServiceGroup;
use crate::harness::{TestContext, TestFn};
use crate::scenario::{self, Call, Group, Test};

/// The scenario file every group in this file was generated from.
const SCENARIO_FILE: &str = "compat/model/authored/logs-groups.json";

const GROUP_LOGS_GROUPS_SHADOW: Group = Group {
    name: "logs-groups-shadow",
    file: SCENARIO_FILE,
};

/// The authored logs-groups groups.
///
/// The client is built once, from the config the suite's hand-written groups
/// share, and cloned into every test — a generated group builds its own rather
/// than adding an accessor to AwsClients for every service the generator learns
/// to cover; nothing else about the client differs.
pub struct ScenariosAuthoredLogsGroups {
    client: aws_sdk_cloudwatchlogs::Client,
}

impl ScenariosAuthoredLogsGroups {
    pub fn new(clients: &Arc<AwsClients>) -> Self {
        let config = aws_sdk_cloudwatchlogs::config::Builder::from(clients.config())
            .endpoint_url(clients.endpoint())
            .build();
        Self {
            client: aws_sdk_cloudwatchlogs::Client::from_conf(config),
        }
    }
}

impl ServiceGroup for ScenariosAuthoredLogsGroups {
    fn name(&self) -> &'static str {
        "scenarios/authored-logs-groups"
    }

    fn impls(&self) -> HashMap<String, TestFn> {
        let mut impls: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            impls.insert(
                "logs-groups-shadow:CreateLogGroup".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_LOGS_GROUPS_SHADOW
                            .run_test(&ctx, "CreateLogGroup", test_logs_groups_shadow_create_log_group(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "logs-groups-shadow:DescribeLogGroups".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_LOGS_GROUPS_SHADOW
                            .run_test(&ctx, "DescribeLogGroups", test_logs_groups_shadow_describe_log_groups(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "logs-groups-shadow:PutRetentionPolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_LOGS_GROUPS_SHADOW
                            .run_test(&ctx, "PutRetentionPolicy", test_logs_groups_shadow_put_retention_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "logs-groups-shadow:VerifyRetentionPolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_LOGS_GROUPS_SHADOW
                            .run_test(&ctx, "VerifyRetentionPolicy", test_logs_groups_shadow_verify_retention_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "logs-groups-shadow:DeleteRetentionPolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_LOGS_GROUPS_SHADOW
                            .run_test(&ctx, "DeleteRetentionPolicy", test_logs_groups_shadow_delete_retention_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "logs-groups-shadow:DeleteLogGroup".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_LOGS_GROUPS_SHADOW
                            .run_test(&ctx, "DeleteLogGroup", test_logs_groups_shadow_delete_log_group(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "logs-groups-shadow:CreateLogStream".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_LOGS_GROUPS_SHADOW
                            .run_test(&ctx, "CreateLogStream", test_logs_groups_shadow_create_log_stream(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "logs-groups-shadow:TagLogGroup".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_LOGS_GROUPS_SHADOW
                            .run_test(&ctx, "TagLogGroup", test_logs_groups_shadow_tag_log_group(&client))
                            .await
                    })
                }),
            );
        }
        impls
    }

    fn setups(&self) -> HashMap<String, TestFn> {
        let mut setups: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            setups.insert(
                "logs-groups-shadow".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_LOGS_GROUPS_SHADOW.run_setup(&ctx, setup_logs_groups_shadow(&client)).await
                    })
                }),
            );
        }
        setups
    }

    fn teardowns(&self) -> HashMap<String, TestFn> {
        let mut teardowns: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            teardowns.insert(
                "logs-groups-shadow".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_LOGS_GROUPS_SHADOW.run_teardown(&ctx, teardown_logs_groups_shadow(&client)).await
                    })
                }),
            );
        }
        teardowns
    }
}

fn setup_logs_groups_shadow(client: &aws_sdk_cloudwatchlogs::Client) -> Vec<Call> {
    vec![
        Call {
            op: "CreateLogGroup",
            params: scenario::map(vec![("logGroupName", scenario::name("group"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_log_group()
                            .log_group_name(b.string("logGroupName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn teardown_logs_groups_shadow(client: &aws_sdk_cloudwatchlogs::Client) -> Vec<Call> {
    vec![
        Call {
            op: "DeleteLogGroup",
            params: scenario::map(vec![("logGroupName", scenario::name("created"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_log_group()
                            .log_group_name(b.string("logGroupName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "DeleteLogGroup",
            params: scenario::map(vec![("logGroupName", scenario::name("group"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_log_group()
                            .log_group_name(b.string("logGroupName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn test_logs_groups_shadow_create_log_group(client: &aws_sdk_cloudwatchlogs::Client) -> Test {
    Test {
        call: Call {
            op: "CreateLogGroup",
            params: scenario::map(vec![("logGroupName", scenario::name("created"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_log_group()
                            .log_group_name(b.string("logGroupName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                Some(Call {
                    op: "DescribeLogGroups",
                    params: scenario::map(vec![
                        ("logGroupNamePrefix", scenario::name("created")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .describe_log_groups()
                                    .log_group_name_prefix(b.string("logGroupNamePrefix")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                }),
                "$.logGroups",
                vec![
                    scenario::where_entry("$.logGroupName", scenario::name("created")),
                ],
            ),
        ],
    }
}

fn test_logs_groups_shadow_describe_log_groups(client: &aws_sdk_cloudwatchlogs::Client) -> Test {
    Test {
        call: Call {
            op: "DescribeLogGroups",
            params: scenario::map(vec![
                ("logGroupNamePrefix", scenario::name("group")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .describe_log_groups()
                            .log_group_name_prefix(b.string("logGroupNamePrefix")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.logGroups",
                vec![
                    scenario::where_entry("$.logGroupName", scenario::name("group")),
                ],
            ),
        ],
    }
}

fn test_logs_groups_shadow_put_retention_policy(client: &aws_sdk_cloudwatchlogs::Client) -> Test {
    Test {
        call: Call {
            op: "PutRetentionPolicy",
            params: scenario::map(vec![
                ("logGroupName", scenario::name("group")),
                ("retentionInDays", scenario::lit(::serde_json::json!(7))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .put_retention_policy()
                            .log_group_name(b.string("logGroupName")?)
                            .retention_in_days(7)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::readback(
                Call {
                    op: "DescribeLogGroups",
                    params: scenario::map(vec![
                        ("logGroupNamePrefix", scenario::name("group")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .describe_log_groups()
                                    .log_group_name_prefix(b.string("logGroupNamePrefix")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                vec![
                    scenario::equals("$.logGroups[0].logGroupName", scenario::name("group")),
                    scenario::equals("$.logGroups[0].retentionInDays", scenario::lit(::serde_json::json!(7))),
                ],
            ),
        ],
    }
}

fn test_logs_groups_shadow_verify_retention_policy(client: &aws_sdk_cloudwatchlogs::Client) -> Test {
    Test {
        call: Call {
            op: "DescribeLogGroups",
            params: scenario::map(vec![
                ("logGroupNamePrefix", scenario::name("group")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .describe_log_groups()
                            .log_group_name_prefix(b.string("logGroupNamePrefix")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.logGroups[0].logGroupName", scenario::name("group")),
                scenario::equals("$.logGroups[0].retentionInDays", scenario::lit(::serde_json::json!(7))),
            ]),
        ],
    }
}

fn test_logs_groups_shadow_delete_retention_policy(client: &aws_sdk_cloudwatchlogs::Client) -> Test {
    Test {
        call: Call {
            op: "DeleteRetentionPolicy",
            params: scenario::map(vec![("logGroupName", scenario::name("group"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_retention_policy()
                            .log_group_name(b.string("logGroupName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::readback(
                Call {
                    op: "DescribeLogGroups",
                    params: scenario::map(vec![
                        ("logGroupNamePrefix", scenario::name("group")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .describe_log_groups()
                                    .log_group_name_prefix(b.string("logGroupNamePrefix")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                vec![
                    scenario::equals("$.logGroups[0].logGroupName", scenario::name("group")),
                    scenario::missing("$.logGroups[0].retentionInDays"),
                ],
            ),
        ],
    }
}

fn test_logs_groups_shadow_delete_log_group(client: &aws_sdk_cloudwatchlogs::Client) -> Test {
    Test {
        call: Call {
            op: "DeleteLogGroup",
            params: scenario::map(vec![("logGroupName", scenario::name("created"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_log_group()
                            .log_group_name(b.string("logGroupName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::absent_from_list(
                Some(Call {
                    op: "DescribeLogGroups",
                    params: scenario::map(vec![
                        ("logGroupNamePrefix", scenario::name("created")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .describe_log_groups()
                                    .log_group_name_prefix(b.string("logGroupNamePrefix")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                }),
                "$.logGroups",
                vec![
                    scenario::where_entry("$.logGroupName", scenario::name("created")),
                ],
            ),
        ],
    }
}

fn test_logs_groups_shadow_create_log_stream(client: &aws_sdk_cloudwatchlogs::Client) -> Test {
    Test {
        call: Call {
            op: "CreateLogStream",
            params: scenario::map(vec![
                ("logGroupName", scenario::name("group")),
                ("logStreamName", scenario::name("stream")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_log_stream()
                            .log_group_name(b.string("logGroupName")?)
                            .log_stream_name(b.string("logStreamName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                Some(Call {
                    op: "DescribeLogStreams",
                    params: scenario::map(vec![
                        ("logGroupName", scenario::name("group")),
                        ("logStreamNamePrefix", scenario::name("stream")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .describe_log_streams()
                                    .log_group_name(b.string("logGroupName")?)
                                    .log_stream_name_prefix(b.string("logStreamNamePrefix")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                }),
                "$.logStreams",
                vec![
                    scenario::where_entry("$.logStreamName", scenario::name("stream")),
                ],
            ),
        ],
    }
}

fn test_logs_groups_shadow_tag_log_group(client: &aws_sdk_cloudwatchlogs::Client) -> Test {
    Test {
        call: Call {
            op: "TagLogGroup",
            params: scenario::map(vec![
                ("logGroupName", scenario::name("group")),
                ("tags", scenario::lit(::serde_json::json!({"env": "compat"}))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .tag_log_group()
                            .log_group_name(b.string("logGroupName")?)
                            .tags("env", "compat")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::readback(
                Call {
                    op: "ListTagsLogGroup",
                    params: scenario::map(vec![
                        ("logGroupName", scenario::name("group")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_tags_log_group()
                                    .log_group_name(b.string("logGroupName")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                vec![
                    scenario::equals("$.tags.env", scenario::lit(::serde_json::json!("compat"))),
                ],
            ),
        ],
    }
}
