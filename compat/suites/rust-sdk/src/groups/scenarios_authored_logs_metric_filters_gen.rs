// Code generated by cmd/compatgen; DO NOT EDIT.
//
// Generated from compat/model/authored/logs-metric-filters.json by cmd/compatgen.
// The semantics live in crate::scenario; this file is the data and the typed
// SDK calls.

use std::collections::HashMap;
use std::sync::Arc;

use crate::clients::AwsClients;
use crate::groups::ServiceGroup;
use crate::harness::{TestContext, TestFn};
use crate::scenario::{self, Call, Group, Test};

/// The scenario file every group in this file was generated from.
const SCENARIO_FILE: &str = "compat/model/authored/logs-metric-filters.json";

const GROUP_LOGS_METRIC_FILTERS: Group = Group {
    name: "logs-metric-filters",
    file: SCENARIO_FILE,
};

/// The authored logs-metric-filters groups.
///
/// The client is built once, from the config the suite's hand-written groups
/// share, and cloned into every test — a generated group builds its own rather
/// than adding an accessor to AwsClients for every service the generator learns
/// to cover; nothing else about the client differs.
pub struct ScenariosAuthoredLogsMetricFilters {
    client: aws_sdk_cloudwatchlogs::Client,
}

impl ScenariosAuthoredLogsMetricFilters {
    pub fn new(clients: &Arc<AwsClients>) -> Self {
        let config = aws_sdk_cloudwatchlogs::config::Builder::from(clients.config())
            .endpoint_url(clients.endpoint())
            .build();
        Self {
            client: aws_sdk_cloudwatchlogs::Client::from_conf(config),
        }
    }
}

impl ServiceGroup for ScenariosAuthoredLogsMetricFilters {
    fn name(&self) -> &'static str {
        "scenarios/authored-logs-metric-filters"
    }

    fn impls(&self) -> HashMap<String, TestFn> {
        let mut impls: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            impls.insert(
                "logs-metric-filters:PutMetricFilter".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_LOGS_METRIC_FILTERS
                            .run_test(&ctx, "PutMetricFilter", test_logs_metric_filters_put_metric_filter(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "logs-metric-filters:DescribeMetricFilters".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_LOGS_METRIC_FILTERS
                            .run_test(&ctx, "DescribeMetricFilters", test_logs_metric_filters_describe_metric_filters(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "logs-metric-filters:TestMetricFilter".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_LOGS_METRIC_FILTERS
                            .run_test(&ctx, "TestMetricFilter", test_logs_metric_filters_test_metric_filter(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "logs-metric-filters:DeleteMetricFilter".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_LOGS_METRIC_FILTERS
                            .run_test(&ctx, "DeleteMetricFilter", test_logs_metric_filters_delete_metric_filter(&client))
                            .await
                    })
                }),
            );
        }
        impls
    }

    fn setups(&self) -> HashMap<String, TestFn> {
        let mut setups: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            setups.insert(
                "logs-metric-filters".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_LOGS_METRIC_FILTERS.run_setup(&ctx, setup_logs_metric_filters(&client)).await
                    })
                }),
            );
        }
        setups
    }

    fn teardowns(&self) -> HashMap<String, TestFn> {
        let mut teardowns: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            teardowns.insert(
                "logs-metric-filters".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_LOGS_METRIC_FILTERS.run_teardown(&ctx, teardown_logs_metric_filters(&client)).await
                    })
                }),
            );
        }
        teardowns
    }
}

fn setup_logs_metric_filters(client: &aws_sdk_cloudwatchlogs::Client) -> Vec<Call> {
    vec![
        Call {
            op: "CreateLogGroup",
            params: scenario::map(vec![("logGroupName", scenario::name("group"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_log_group()
                            .log_group_name(b.string("logGroupName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn teardown_logs_metric_filters(client: &aws_sdk_cloudwatchlogs::Client) -> Vec<Call> {
    vec![
        Call {
            op: "DeleteLogGroup",
            params: scenario::map(vec![("logGroupName", scenario::name("group"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_log_group()
                            .log_group_name(b.string("logGroupName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn test_logs_metric_filters_put_metric_filter(client: &aws_sdk_cloudwatchlogs::Client) -> Test {
    Test {
        call: Call {
            op: "PutMetricFilter",
            params: scenario::map(vec![
                ("filterName", scenario::name("filter")),
                ("filterPattern", scenario::lit(::serde_json::json!("ERROR"))),
                ("logGroupName", scenario::name("group")),
                ("metricTransformations", scenario::list(vec![
                    scenario::map(vec![
                        ("metricName", scenario::name("metric")),
                        ("metricNamespace", scenario::lit(::serde_json::json!("Overcast/Compat"))),
                        ("metricValue", scenario::lit(::serde_json::json!("1"))),
                    ]),
                ])),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .put_metric_filter()
                            .filter_name(b.string("filterName")?)
                            .filter_pattern("ERROR")
                            .log_group_name(b.string("logGroupName")?)
                            .metric_transformations(
                                aws_sdk_cloudwatchlogs::types::MetricTransformation::builder()
                                    .metric_name(b.string("metricTransformations[0].metricName")?)
                                    .metric_namespace("Overcast/Compat")
                                    .metric_value("1")
                                    .build()
                                    .map_err(|err| scenario::build_error("metricTransformations[0]", err))?
                            )
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::readback(
                Call {
                    op: "DescribeMetricFilters",
                    params: scenario::map(vec![
                        ("filterNamePrefix", scenario::name("filter")),
                        ("logGroupName", scenario::name("group")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .describe_metric_filters()
                                    .filter_name_prefix(b.string("filterNamePrefix")?)
                                    .log_group_name(b.string("logGroupName")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                vec![
                    scenario::non_empty("$.metricFilters[0].creationTime"),
                    scenario::equals("$.metricFilters[0].filterName", scenario::name("filter")),
                    scenario::equals("$.metricFilters[0].filterPattern", scenario::lit(::serde_json::json!("ERROR"))),
                    scenario::equals("$.metricFilters[0].logGroupName", scenario::name("group")),
                    scenario::equals("$.metricFilters[0].metricTransformations[0].metricName", scenario::name("metric")),
                    scenario::equals("$.metricFilters[0].metricTransformations[0].metricNamespace", scenario::lit(::serde_json::json!("Overcast/Compat"))),
                    scenario::equals("$.metricFilters[0].metricTransformations[0].metricValue", scenario::lit(::serde_json::json!("1"))),
                ],
            ),
        ],
    }
}

fn test_logs_metric_filters_describe_metric_filters(client: &aws_sdk_cloudwatchlogs::Client) -> Test {
    Test {
        call: Call {
            op: "DescribeMetricFilters",
            params: scenario::map(vec![("logGroupName", scenario::name("group"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .describe_metric_filters()
                            .log_group_name(b.string("logGroupName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.metricFilters",
                vec![
                    scenario::where_entry("$.filterName", scenario::name("filter")),
                ],
            ),
        ],
    }
}

fn test_logs_metric_filters_test_metric_filter(client: &aws_sdk_cloudwatchlogs::Client) -> Test {
    Test {
        call: Call {
            op: "TestMetricFilter",
            params: scenario::lit(::serde_json::json!({"filterPattern": "ERROR", "logEventMessages": ["INFO all fine", "ERROR something broke"]})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .test_metric_filter()
                            .filter_pattern("ERROR")
                            .log_event_messages("INFO all fine")
                            .log_event_messages("ERROR something broke")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.matches[0].eventMessage", scenario::lit(::serde_json::json!("ERROR something broke"))),
                scenario::equals("$.matches[0].eventNumber", scenario::lit(::serde_json::json!(1))),
            ]),
            scenario::absent_from_list(
                None,
                "$.matches",
                vec![
                    scenario::where_entry("$.eventMessage", scenario::lit(::serde_json::json!("INFO all fine"))),
                ],
            ),
        ],
    }
}

fn test_logs_metric_filters_delete_metric_filter(client: &aws_sdk_cloudwatchlogs::Client) -> Test {
    Test {
        call: Call {
            op: "DeleteMetricFilter",
            params: scenario::map(vec![
                ("filterName", scenario::name("filter")),
                ("logGroupName", scenario::name("group")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_metric_filter()
                            .filter_name(b.string("filterName")?)
                            .log_group_name(b.string("logGroupName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::absent_from_list(
                Some(Call {
                    op: "DescribeMetricFilters",
                    params: scenario::map(vec![
                        ("logGroupName", scenario::name("group")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .describe_metric_filters()
                                    .log_group_name(b.string("logGroupName")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                }),
                "$.metricFilters",
                vec![
                    scenario::where_entry("$.filterName", scenario::name("filter")),
                ],
            ),
        ],
    }
}
