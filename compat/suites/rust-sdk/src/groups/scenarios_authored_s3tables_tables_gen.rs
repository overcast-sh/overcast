// Code generated by cmd/compatgen; DO NOT EDIT.
//
// Generated from compat/model/authored/s3tables-tables.json by cmd/compatgen.
// The semantics live in crate::scenario; this file is the data and the typed
// SDK calls.

use std::collections::HashMap;
use std::sync::Arc;

use crate::clients::AwsClients;
use crate::groups::ServiceGroup;
use crate::harness::{TestContext, TestFn};
use crate::scenario::{self, Call, Group, Test};

/// The scenario file every group in this file was generated from.
const SCENARIO_FILE: &str = "compat/model/authored/s3tables-tables.json";

const GROUP_S3TABLES_TABLES_SHADOW: Group = Group {
    name: "s3tables-tables-shadow",
    file: SCENARIO_FILE,
};

/// The authored s3tables-tables groups.
///
/// The client is built once, from the config the suite's hand-written groups
/// share, and cloned into every test — a generated group builds its own rather
/// than adding an accessor to AwsClients for every service the generator learns
/// to cover; nothing else about the client differs.
pub struct ScenariosAuthoredS3tablesTables {
    client: aws_sdk_s3tables::Client,
}

impl ScenariosAuthoredS3tablesTables {
    pub fn new(clients: &Arc<AwsClients>) -> Self {
        let config = aws_sdk_s3tables::config::Builder::from(clients.config())
            .endpoint_url(clients.endpoint())
            .build();
        Self {
            client: aws_sdk_s3tables::Client::from_conf(config),
        }
    }
}

impl ServiceGroup for ScenariosAuthoredS3tablesTables {
    fn name(&self) -> &'static str {
        "scenarios/authored-s3tables-tables"
    }

    fn impls(&self) -> HashMap<String, TestFn> {
        let mut impls: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            impls.insert(
                "s3tables-tables-shadow:CreateTableBucket".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_S3TABLES_TABLES_SHADOW
                            .run_test(&ctx, "CreateTableBucket", test_s3tables_tables_shadow_create_table_bucket(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "s3tables-tables-shadow:GetTableBucket".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_S3TABLES_TABLES_SHADOW
                            .run_test(&ctx, "GetTableBucket", test_s3tables_tables_shadow_get_table_bucket(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "s3tables-tables-shadow:ListTableBuckets".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_S3TABLES_TABLES_SHADOW
                            .run_test(&ctx, "ListTableBuckets", test_s3tables_tables_shadow_list_table_buckets(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "s3tables-tables-shadow:CreateNamespace".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_S3TABLES_TABLES_SHADOW
                            .run_test(&ctx, "CreateNamespace", test_s3tables_tables_shadow_create_namespace(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "s3tables-tables-shadow:ListNamespaces".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_S3TABLES_TABLES_SHADOW
                            .run_test(&ctx, "ListNamespaces", test_s3tables_tables_shadow_list_namespaces(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "s3tables-tables-shadow:CreateTable".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_S3TABLES_TABLES_SHADOW
                            .run_test(&ctx, "CreateTable", test_s3tables_tables_shadow_create_table(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "s3tables-tables-shadow:GetTable".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_S3TABLES_TABLES_SHADOW
                            .run_test(&ctx, "GetTable", test_s3tables_tables_shadow_get_table(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "s3tables-tables-shadow:ListTables".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_S3TABLES_TABLES_SHADOW
                            .run_test(&ctx, "ListTables", test_s3tables_tables_shadow_list_tables(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "s3tables-tables-shadow:UpdateTableMetadataLocation".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_S3TABLES_TABLES_SHADOW
                            .run_test(&ctx, "UpdateTableMetadataLocation", test_s3tables_tables_shadow_update_table_metadata_location(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "s3tables-tables-shadow:UpdateTableMetadataLocationStaleToken".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_S3TABLES_TABLES_SHADOW
                            .run_test(&ctx, "UpdateTableMetadataLocationStaleToken", test_s3tables_tables_shadow_update_table_metadata_location_stale_token(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "s3tables-tables-shadow:DeleteTable".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_S3TABLES_TABLES_SHADOW
                            .run_test(&ctx, "DeleteTable", test_s3tables_tables_shadow_delete_table(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "s3tables-tables-shadow:DeleteNamespace".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_S3TABLES_TABLES_SHADOW
                            .run_test(&ctx, "DeleteNamespace", test_s3tables_tables_shadow_delete_namespace(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "s3tables-tables-shadow:DeleteTableBucket".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_S3TABLES_TABLES_SHADOW
                            .run_test(&ctx, "DeleteTableBucket", test_s3tables_tables_shadow_delete_table_bucket(&client))
                            .await
                    })
                }),
            );
        }
        impls
    }

    fn setups(&self) -> HashMap<String, TestFn> {
        let mut setups: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            setups.insert(
                "s3tables-tables-shadow".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_S3TABLES_TABLES_SHADOW.run_setup(&ctx, setup_s3tables_tables_shadow(&client)).await
                    })
                }),
            );
        }
        setups
    }

    fn teardowns(&self) -> HashMap<String, TestFn> {
        let mut teardowns: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            teardowns.insert(
                "s3tables-tables-shadow".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_S3TABLES_TABLES_SHADOW.run_teardown(&ctx, teardown_s3tables_tables_shadow(&client)).await
                    })
                }),
            );
        }
        teardowns
    }
}

fn setup_s3tables_tables_shadow(_client: &aws_sdk_s3tables::Client) -> Vec<Call> {
    // An empty phase is a no-op, not a missing one.
    Vec::new()
}

fn teardown_s3tables_tables_shadow(client: &aws_sdk_s3tables::Client) -> Vec<Call> {
    vec![
        Call {
            op: "DeleteTable",
            params: scenario::map(vec![
                ("name", scenario::lit(::serde_json::json!("orders"))),
                ("namespace", scenario::lit(::serde_json::json!("compat"))),
                ("tableBucketARN", scenario::context("bucket.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_table()
                            .name("orders")
                            .namespace("compat")
                            .table_bucket_arn(b.string("tableBucketARN")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "DeleteNamespace",
            params: scenario::map(vec![
                ("namespace", scenario::lit(::serde_json::json!("compat"))),
                ("tableBucketARN", scenario::context("bucket.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_namespace()
                            .namespace("compat")
                            .table_bucket_arn(b.string("tableBucketARN")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "DeleteTableBucket",
            params: scenario::map(vec![
                ("tableBucketARN", scenario::context("bucket.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_table_bucket()
                            .table_bucket_arn(b.string("tableBucketARN")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn test_s3tables_tables_shadow_create_table_bucket(client: &aws_sdk_s3tables::Client) -> Test {
    Test {
        call: Call {
            op: "CreateTableBucket",
            params: scenario::map(vec![("name", scenario::name("bucket"))]),
            export: vec![
                ("bucket.arn", "$.arn"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_table_bucket()
                            .name(b.string("name")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::matches("$.arn", "^arn:aws:s3tables:[-a-z0-9]*:[0-9]{12}:bucket/[a-z0-9_-]{3,63}$"),
            ]),
            scenario::readback(
                Call {
                    op: "GetTableBucket",
                    params: scenario::map(vec![
                        ("tableBucketARN", scenario::context("bucket.arn")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .get_table_bucket()
                                    .table_bucket_arn(b.string("tableBucketARN")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                vec![
                    scenario::equals("$.name", scenario::name("bucket")),
                ],
            ),
        ],
    }
}

fn test_s3tables_tables_shadow_get_table_bucket(client: &aws_sdk_s3tables::Client) -> Test {
    Test {
        call: Call {
            op: "GetTableBucket",
            params: scenario::map(vec![
                ("tableBucketARN", scenario::context("bucket.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_table_bucket()
                            .table_bucket_arn(b.string("tableBucketARN")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.arn", scenario::context("bucket.arn")),
                scenario::non_empty("$.createdAt"),
                scenario::equals("$.name", scenario::name("bucket")),
                scenario::non_empty("$.ownerAccountId"),
            ]),
        ],
    }
}

fn test_s3tables_tables_shadow_list_table_buckets(client: &aws_sdk_s3tables::Client) -> Test {
    Test {
        call: Call {
            op: "ListTableBuckets",
            params: scenario::map(vec![("prefix", scenario::name("bucket"))]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_table_buckets()
                            .prefix(b.string("prefix")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.tableBuckets",
                vec![
                    scenario::where_entry("$.arn", scenario::context("bucket.arn")),
                    scenario::where_entry("$.name", scenario::name("bucket")),
                ],
            ),
        ],
    }
}

fn test_s3tables_tables_shadow_create_namespace(client: &aws_sdk_s3tables::Client) -> Test {
    Test {
        call: Call {
            op: "CreateNamespace",
            params: scenario::map(vec![
                ("namespace", scenario::lit(::serde_json::json!(["compat"]))),
                ("tableBucketARN", scenario::context("bucket.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_namespace()
                            .namespace("compat")
                            .table_bucket_arn(b.string("tableBucketARN")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.namespace", scenario::lit(::serde_json::json!(["compat"]))),
                scenario::equals("$.tableBucketARN", scenario::context("bucket.arn")),
            ]),
            scenario::readback(
                Call {
                    op: "GetNamespace",
                    params: scenario::map(vec![
                        ("namespace", scenario::lit(::serde_json::json!("compat"))),
                        ("tableBucketARN", scenario::context("bucket.arn")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .get_namespace()
                                    .namespace("compat")
                                    .table_bucket_arn(b.string("tableBucketARN")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                vec![
                    scenario::equals("$.namespace", scenario::lit(::serde_json::json!(["compat"]))),
                ],
            ),
        ],
    }
}

fn test_s3tables_tables_shadow_list_namespaces(client: &aws_sdk_s3tables::Client) -> Test {
    Test {
        call: Call {
            op: "ListNamespaces",
            params: scenario::map(vec![
                ("tableBucketARN", scenario::context("bucket.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_namespaces()
                            .table_bucket_arn(b.string("tableBucketARN")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.namespaces[0].namespace", scenario::lit(::serde_json::json!(["compat"]))),
                scenario::missing("$.namespaces[1]"),
            ]),
        ],
    }
}

fn test_s3tables_tables_shadow_create_table(client: &aws_sdk_s3tables::Client) -> Test {
    Test {
        call: Call {
            op: "CreateTable",
            params: scenario::map(vec![
                ("format", scenario::lit(::serde_json::json!("ICEBERG"))),
                ("name", scenario::lit(::serde_json::json!("orders"))),
                ("namespace", scenario::lit(::serde_json::json!("compat"))),
                ("tableBucketARN", scenario::context("bucket.arn")),
            ]),
            export: vec![
                ("table.arn", "$.tableARN"),
                ("table.createToken", "$.versionToken"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_table()
                            .format(aws_sdk_s3tables::types::OpenTableFormat::from("ICEBERG"))
                            .name("orders")
                            .namespace("compat")
                            .table_bucket_arn(b.string("tableBucketARN")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::matches("$.tableARN", "^arn:aws:s3tables:[-a-z0-9]*:[0-9]{12}:bucket/[a-z0-9_-]{3,63}/table/[a-zA-Z0-9_-]{1,255}$"),
                scenario::non_empty("$.versionToken"),
            ]),
            scenario::readback(
                Call {
                    op: "GetTable",
                    params: scenario::map(vec![
                        ("name", scenario::lit(::serde_json::json!("orders"))),
                        ("namespace", scenario::lit(::serde_json::json!("compat"))),
                        ("tableBucketARN", scenario::context("bucket.arn")),
                    ]),
                    export: vec![
                        ("table.token", "$.versionToken"),
                        ("table.warehouse", "$.warehouseLocation"),
                    ],
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .get_table()
                                    .name("orders")
                                    .namespace("compat")
                                    .table_bucket_arn(b.string("tableBucketARN")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                vec![
                    scenario::equals("$.tableARN", scenario::context("table.arn")),
                    scenario::equals("$.versionToken", scenario::context("table.createToken")),
                ],
            ),
        ],
    }
}

fn test_s3tables_tables_shadow_get_table(client: &aws_sdk_s3tables::Client) -> Test {
    Test {
        call: Call {
            op: "GetTable",
            params: scenario::map(vec![
                ("name", scenario::lit(::serde_json::json!("orders"))),
                ("namespace", scenario::lit(::serde_json::json!("compat"))),
                ("tableBucketARN", scenario::context("bucket.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_table()
                            .name("orders")
                            .namespace("compat")
                            .table_bucket_arn(b.string("tableBucketARN")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.format", scenario::lit(::serde_json::json!("ICEBERG"))),
                scenario::equals("$.name", scenario::lit(::serde_json::json!("orders"))),
                scenario::equals("$.namespace", scenario::lit(::serde_json::json!(["compat"]))),
                scenario::equals("$.tableARN", scenario::context("table.arn")),
                scenario::matches("$.warehouseLocation", "^s3://"),
            ]),
        ],
    }
}

fn test_s3tables_tables_shadow_list_tables(client: &aws_sdk_s3tables::Client) -> Test {
    Test {
        call: Call {
            op: "ListTables",
            params: scenario::map(vec![
                ("namespace", scenario::lit(::serde_json::json!("compat"))),
                ("tableBucketARN", scenario::context("bucket.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_tables()
                            .namespace("compat")
                            .table_bucket_arn(b.string("tableBucketARN")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.tables[0].name", scenario::lit(::serde_json::json!("orders"))),
                scenario::equals("$.tables[0].tableARN", scenario::context("table.arn")),
                scenario::missing("$.tables[1]"),
            ]),
        ],
    }
}

fn test_s3tables_tables_shadow_update_table_metadata_location(client: &aws_sdk_s3tables::Client) -> Test {
    Test {
        call: Call {
            op: "UpdateTableMetadataLocation",
            params: scenario::map(vec![
                ("metadataLocation", scenario::concat(vec![
                    scenario::context("table.warehouse"),
                    scenario::lit(::serde_json::json!("/metadata/00001-compat.metadata.json")),
                ])),
                ("name", scenario::lit(::serde_json::json!("orders"))),
                ("namespace", scenario::lit(::serde_json::json!("compat"))),
                ("tableBucketARN", scenario::context("bucket.arn")),
                ("versionToken", scenario::context("table.token")),
            ]),
            export: vec![
                ("table.updatedToken", "$.versionToken"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .update_table_metadata_location()
                            .metadata_location(b.string("metadataLocation")?)
                            .name("orders")
                            .namespace("compat")
                            .table_bucket_arn(b.string("tableBucketARN")?)
                            .version_token(b.string("versionToken")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.metadataLocation", scenario::concat(vec![
                    scenario::context("table.warehouse"),
                    scenario::lit(::serde_json::json!("/metadata/00001-compat.metadata.json")),
                ])),
                scenario::equals("$.tableARN", scenario::context("table.arn")),
                scenario::non_empty("$.versionToken"),
            ]),
            scenario::readback(
                Call {
                    op: "GetTableMetadataLocation",
                    params: scenario::map(vec![
                        ("name", scenario::lit(::serde_json::json!("orders"))),
                        ("namespace", scenario::lit(::serde_json::json!("compat"))),
                        ("tableBucketARN", scenario::context("bucket.arn")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .get_table_metadata_location()
                                    .name("orders")
                                    .namespace("compat")
                                    .table_bucket_arn(b.string("tableBucketARN")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                vec![
                    scenario::equals("$.metadataLocation", scenario::concat(vec![
                        scenario::context("table.warehouse"),
                        scenario::lit(::serde_json::json!("/metadata/00001-compat.metadata.json")),
                    ])),
                    scenario::equals("$.versionToken", scenario::context("table.updatedToken")),
                ],
            ),
            scenario::absent_by_error(
                Call {
                    op: "UpdateTableMetadataLocation",
                    params: scenario::map(vec![
                        ("metadataLocation", scenario::concat(vec![
                            scenario::context("table.warehouse"),
                            scenario::lit(::serde_json::json!("/metadata/00002-compat.metadata.json")),
                        ])),
                        ("name", scenario::lit(::serde_json::json!("orders"))),
                        ("namespace", scenario::lit(::serde_json::json!("compat"))),
                        ("tableBucketARN", scenario::context("bucket.arn")),
                        ("versionToken", scenario::context("table.token")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .update_table_metadata_location()
                                    .metadata_location(b.string("metadataLocation")?)
                                    .name("orders")
                                    .namespace("compat")
                                    .table_bucket_arn(b.string("tableBucketARN")?)
                                    .version_token(b.string("versionToken")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                scenario::error("ConflictException", "ConflictException"),
            ),
        ],
    }
}

fn test_s3tables_tables_shadow_update_table_metadata_location_stale_token(client: &aws_sdk_s3tables::Client) -> Test {
    Test {
        call: Call {
            op: "UpdateTableMetadataLocation",
            params: scenario::map(vec![
                ("metadataLocation", scenario::concat(vec![
                    scenario::context("table.warehouse"),
                    scenario::lit(::serde_json::json!("/metadata/00002-compat.metadata.json")),
                ])),
                ("name", scenario::lit(::serde_json::json!("orders"))),
                ("namespace", scenario::lit(::serde_json::json!("compat"))),
                ("tableBucketARN", scenario::context("bucket.arn")),
                ("versionToken", scenario::context("table.token")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .update_table_metadata_location()
                            .metadata_location(b.string("metadataLocation")?)
                            .name("orders")
                            .namespace("compat")
                            .table_bucket_arn(b.string("tableBucketARN")?)
                            .version_token(b.string("versionToken")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::error_code(scenario::error("ConflictException", "ConflictException")),
            scenario::readback(
                Call {
                    op: "GetTableMetadataLocation",
                    params: scenario::map(vec![
                        ("name", scenario::lit(::serde_json::json!("orders"))),
                        ("namespace", scenario::lit(::serde_json::json!("compat"))),
                        ("tableBucketARN", scenario::context("bucket.arn")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .get_table_metadata_location()
                                    .name("orders")
                                    .namespace("compat")
                                    .table_bucket_arn(b.string("tableBucketARN")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                vec![
                    scenario::equals("$.metadataLocation", scenario::concat(vec![
                        scenario::context("table.warehouse"),
                        scenario::lit(::serde_json::json!("/metadata/00001-compat.metadata.json")),
                    ])),
                    scenario::equals("$.versionToken", scenario::context("table.updatedToken")),
                ],
            ),
        ],
    }
}

fn test_s3tables_tables_shadow_delete_table(client: &aws_sdk_s3tables::Client) -> Test {
    Test {
        call: Call {
            op: "DeleteTable",
            params: scenario::map(vec![
                ("name", scenario::lit(::serde_json::json!("orders"))),
                ("namespace", scenario::lit(::serde_json::json!("compat"))),
                ("tableBucketARN", scenario::context("bucket.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_table()
                            .name("orders")
                            .namespace("compat")
                            .table_bucket_arn(b.string("tableBucketARN")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::absent_by_error(
                Call {
                    op: "GetTable",
                    params: scenario::map(vec![
                        ("name", scenario::lit(::serde_json::json!("orders"))),
                        ("namespace", scenario::lit(::serde_json::json!("compat"))),
                        ("tableBucketARN", scenario::context("bucket.arn")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .get_table()
                                    .name("orders")
                                    .namespace("compat")
                                    .table_bucket_arn(b.string("tableBucketARN")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                scenario::error("NotFoundException", "NotFoundException"),
            ),
            scenario::absent_from_list(
                Some(Call {
                    op: "ListTables",
                    params: scenario::map(vec![
                        ("namespace", scenario::lit(::serde_json::json!("compat"))),
                        ("tableBucketARN", scenario::context("bucket.arn")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_tables()
                                    .namespace("compat")
                                    .table_bucket_arn(b.string("tableBucketARN")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                }),
                "$.tables",
                vec![
                    scenario::where_entry("$.name", scenario::lit(::serde_json::json!("orders"))),
                ],
            ),
        ],
    }
}

fn test_s3tables_tables_shadow_delete_namespace(client: &aws_sdk_s3tables::Client) -> Test {
    Test {
        call: Call {
            op: "DeleteNamespace",
            params: scenario::map(vec![
                ("namespace", scenario::lit(::serde_json::json!("compat"))),
                ("tableBucketARN", scenario::context("bucket.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_namespace()
                            .namespace("compat")
                            .table_bucket_arn(b.string("tableBucketARN")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::absent_by_error(
                Call {
                    op: "GetNamespace",
                    params: scenario::map(vec![
                        ("namespace", scenario::lit(::serde_json::json!("compat"))),
                        ("tableBucketARN", scenario::context("bucket.arn")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .get_namespace()
                                    .namespace("compat")
                                    .table_bucket_arn(b.string("tableBucketARN")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                scenario::error("NotFoundException", "NotFoundException"),
            ),
            scenario::absent_from_list(
                Some(Call {
                    op: "ListNamespaces",
                    params: scenario::map(vec![
                        ("tableBucketARN", scenario::context("bucket.arn")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_namespaces()
                                    .table_bucket_arn(b.string("tableBucketARN")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                }),
                "$.namespaces",
                vec![
                    scenario::where_entry("$.namespace", scenario::lit(::serde_json::json!(["compat"]))),
                ],
            ),
        ],
    }
}

fn test_s3tables_tables_shadow_delete_table_bucket(client: &aws_sdk_s3tables::Client) -> Test {
    Test {
        call: Call {
            op: "DeleteTableBucket",
            params: scenario::map(vec![
                ("tableBucketARN", scenario::context("bucket.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_table_bucket()
                            .table_bucket_arn(b.string("tableBucketARN")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::absent_by_error(
                Call {
                    op: "GetTableBucket",
                    params: scenario::map(vec![
                        ("tableBucketARN", scenario::context("bucket.arn")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .get_table_bucket()
                                    .table_bucket_arn(b.string("tableBucketARN")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                },
                scenario::error("NotFoundException", "NotFoundException"),
            ),
            scenario::absent_from_list(
                Some(Call {
                    op: "ListTableBuckets",
                    params: scenario::map(vec![
                        ("prefix", scenario::name("bucket")),
                    ]),
                    export: Vec::new(),
                    invoke: {
                        let client = client.clone();
                        scenario::invoker(move |b| {
                            let client = client.clone();
                            Box::pin(async move {
                                let capture = scenario::Capture::new();
                                let request = client
                                    .list_table_buckets()
                                    .prefix(b.string("prefix")?)
                                    .customize()
                                    .interceptor(capture.clone());
                                Ok(scenario::observe(request.send().await, &capture))
                            })
                        })
                    },
                }),
                "$.tableBuckets",
                vec![
                    scenario::where_entry("$.name", scenario::name("bucket")),
                ],
            ),
        ],
    }
}
