// Code generated by cmd/compatgen; DO NOT EDIT.
//
// Generated from compat/model/scenarios/iam.json by cmd/compatgen.
// The semantics live in crate::scenario; this file is the data and the typed
// SDK calls.

use std::collections::HashMap;
use std::sync::Arc;

use crate::clients::AwsClients;
use crate::groups::ServiceGroup;
use crate::harness::{TestContext, TestFn};
use crate::scenario::{self, Call, Group, Test};

/// The scenario file every group in this file was generated from.
const SCENARIO_FILE: &str = "compat/model/scenarios/iam.json";

const GROUP_IAM_GEN_POLICY: Group = Group {
    name: "iam-gen-policy",
    file: SCENARIO_FILE,
};

const GROUP_IAM_GEN_USER: Group = Group {
    name: "iam-gen-user",
    file: SCENARIO_FILE,
};

const GROUP_IAM_GEN_GROUP: Group = Group {
    name: "iam-gen-group",
    file: SCENARIO_FILE,
};

const GROUP_IAM_GEN_ROLE: Group = Group {
    name: "iam-gen-role",
    file: SCENARIO_FILE,
};

const GROUP_IAM_GEN_INSTANCEPROFILE: Group = Group {
    name: "iam-gen-instanceprofile",
    file: SCENARIO_FILE,
};

const GROUP_IAM_GEN_PROBE: Group = Group {
    name: "iam-gen-probe",
    file: SCENARIO_FILE,
};

/// The generated iam groups.
///
/// The client is built once, from the config the suite's hand-written groups
/// share, and cloned into every test — a generated group builds its own rather
/// than adding an accessor to AwsClients for every service the generator learns
/// to cover; nothing else about the client differs.
pub struct ScenariosIam {
    client: aws_sdk_iam::Client,
}

impl ScenariosIam {
    pub fn new(clients: &Arc<AwsClients>) -> Self {
        let config = aws_sdk_iam::config::Builder::from(clients.config())
            .endpoint_url(clients.endpoint())
            .build();
        Self {
            client: aws_sdk_iam::Client::from_conf(config),
        }
    }
}

impl ServiceGroup for ScenariosIam {
    fn name(&self) -> &'static str {
        "scenarios/iam"
    }

    fn impls(&self) -> HashMap<String, TestFn> {
        let mut impls: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-policy:CreatePolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_POLICY
                            .run_test(&ctx, "CreatePolicy", test_iam_gen_policy_create_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-policy:GetPolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_POLICY
                            .run_test(&ctx, "GetPolicy", test_iam_gen_policy_get_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-policy:TagPolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_POLICY
                            .run_test(&ctx, "TagPolicy", test_iam_gen_policy_tag_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-policy:ListPolicyTags".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_POLICY
                            .run_test(&ctx, "ListPolicyTags", test_iam_gen_policy_list_policy_tags(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-policy:UntagPolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_POLICY
                            .run_test(&ctx, "UntagPolicy", test_iam_gen_policy_untag_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-policy:CreatePolicyVersion".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_POLICY
                            .run_test(&ctx, "CreatePolicyVersion", test_iam_gen_policy_create_policy_version(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-policy:DeletePolicyVersion".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_POLICY
                            .run_test(&ctx, "DeletePolicyVersion", test_iam_gen_policy_delete_policy_version(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-policy:SimulateCustomPolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_POLICY
                            .run_test(&ctx, "SimulateCustomPolicy", test_iam_gen_policy_simulate_custom_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-policy:ListPolicies".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_POLICY
                            .run_test(&ctx, "ListPolicies", test_iam_gen_policy_list_policies(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-policy:DeletePolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_POLICY
                            .run_test(&ctx, "DeletePolicy", test_iam_gen_policy_delete_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-user:CreateUser".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_USER
                            .run_test(&ctx, "CreateUser", test_iam_gen_user_create_user(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-user:GetUser".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_USER
                            .run_test(&ctx, "GetUser", test_iam_gen_user_get_user(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-user:UpdateUser".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_USER
                            .run_test(&ctx, "UpdateUser", test_iam_gen_user_update_user(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-user:TagUser".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_USER
                            .run_test(&ctx, "TagUser", test_iam_gen_user_tag_user(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-user:ListUserTags".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_USER
                            .run_test(&ctx, "ListUserTags", test_iam_gen_user_list_user_tags(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-user:UntagUser".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_USER
                            .run_test(&ctx, "UntagUser", test_iam_gen_user_untag_user(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-user:PutUserPolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_USER
                            .run_test(&ctx, "PutUserPolicy", test_iam_gen_user_put_user_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-user:GetUserPolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_USER
                            .run_test(&ctx, "GetUserPolicy", test_iam_gen_user_get_user_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-user:ListUserPolicies".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_USER
                            .run_test(&ctx, "ListUserPolicies", test_iam_gen_user_list_user_policies(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-user:DeleteUserPolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_USER
                            .run_test(&ctx, "DeleteUserPolicy", test_iam_gen_user_delete_user_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-user:AttachUserPolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_USER
                            .run_test(&ctx, "AttachUserPolicy", test_iam_gen_user_attach_user_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-user:ListAttachedUserPolicies".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_USER
                            .run_test(&ctx, "ListAttachedUserPolicies", test_iam_gen_user_list_attached_user_policies(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-user:DetachUserPolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_USER
                            .run_test(&ctx, "DetachUserPolicy", test_iam_gen_user_detach_user_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-user:CreateAccessKey".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_USER
                            .run_test(&ctx, "CreateAccessKey", test_iam_gen_user_create_access_key(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-user:ListAccessKeys".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_USER
                            .run_test(&ctx, "ListAccessKeys", test_iam_gen_user_list_access_keys(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-user:DeleteAccessKey".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_USER
                            .run_test(&ctx, "DeleteAccessKey", test_iam_gen_user_delete_access_key(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-user:PutUserPermissionsBoundary".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_USER
                            .run_test(&ctx, "PutUserPermissionsBoundary", test_iam_gen_user_put_user_permissions_boundary(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-user:DeleteUserPermissionsBoundary".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_USER
                            .run_test(&ctx, "DeleteUserPermissionsBoundary", test_iam_gen_user_delete_user_permissions_boundary(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-user:ListUsers".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_USER
                            .run_test(&ctx, "ListUsers", test_iam_gen_user_list_users(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-user:DeleteUser".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_USER
                            .run_test(&ctx, "DeleteUser", test_iam_gen_user_delete_user(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-group:CreateGroup".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_GROUP
                            .run_test(&ctx, "CreateGroup", test_iam_gen_group_create_group(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-group:GetGroup".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_GROUP
                            .run_test(&ctx, "GetGroup", test_iam_gen_group_get_group(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-group:AddUserToGroup".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_GROUP
                            .run_test(&ctx, "AddUserToGroup", test_iam_gen_group_add_user_to_group(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-group:GetGroupMembers".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_GROUP
                            .run_test(&ctx, "GetGroupMembers", test_iam_gen_group_get_group_members(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-group:ListGroupsForUser".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_GROUP
                            .run_test(&ctx, "ListGroupsForUser", test_iam_gen_group_list_groups_for_user(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-group:PutGroupPolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_GROUP
                            .run_test(&ctx, "PutGroupPolicy", test_iam_gen_group_put_group_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-group:GetGroupPolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_GROUP
                            .run_test(&ctx, "GetGroupPolicy", test_iam_gen_group_get_group_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-group:ListGroupPolicies".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_GROUP
                            .run_test(&ctx, "ListGroupPolicies", test_iam_gen_group_list_group_policies(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-group:DeleteGroupPolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_GROUP
                            .run_test(&ctx, "DeleteGroupPolicy", test_iam_gen_group_delete_group_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-group:AttachGroupPolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_GROUP
                            .run_test(&ctx, "AttachGroupPolicy", test_iam_gen_group_attach_group_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-group:ListAttachedGroupPolicies".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_GROUP
                            .run_test(&ctx, "ListAttachedGroupPolicies", test_iam_gen_group_list_attached_group_policies(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-group:DetachGroupPolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_GROUP
                            .run_test(&ctx, "DetachGroupPolicy", test_iam_gen_group_detach_group_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-group:RemoveUserFromGroup".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_GROUP
                            .run_test(&ctx, "RemoveUserFromGroup", test_iam_gen_group_remove_user_from_group(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-group:ListGroups".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_GROUP
                            .run_test(&ctx, "ListGroups", test_iam_gen_group_list_groups(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-group:DeleteGroup".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_GROUP
                            .run_test(&ctx, "DeleteGroup", test_iam_gen_group_delete_group(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-role:CreateRole".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_ROLE
                            .run_test(&ctx, "CreateRole", test_iam_gen_role_create_role(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-role:GetRole".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_ROLE
                            .run_test(&ctx, "GetRole", test_iam_gen_role_get_role(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-role:UpdateRole".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_ROLE
                            .run_test(&ctx, "UpdateRole", test_iam_gen_role_update_role(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-role:TagRole".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_ROLE
                            .run_test(&ctx, "TagRole", test_iam_gen_role_tag_role(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-role:ListRoleTags".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_ROLE
                            .run_test(&ctx, "ListRoleTags", test_iam_gen_role_list_role_tags(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-role:UntagRole".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_ROLE
                            .run_test(&ctx, "UntagRole", test_iam_gen_role_untag_role(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-role:UpdateAssumeRolePolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_ROLE
                            .run_test(&ctx, "UpdateAssumeRolePolicy", test_iam_gen_role_update_assume_role_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-role:PutRolePolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_ROLE
                            .run_test(&ctx, "PutRolePolicy", test_iam_gen_role_put_role_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-role:GetRolePolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_ROLE
                            .run_test(&ctx, "GetRolePolicy", test_iam_gen_role_get_role_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-role:ListRolePolicies".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_ROLE
                            .run_test(&ctx, "ListRolePolicies", test_iam_gen_role_list_role_policies(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-role:SimulatePrincipalPolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_ROLE
                            .run_test(&ctx, "SimulatePrincipalPolicy", test_iam_gen_role_simulate_principal_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-role:DeleteRolePolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_ROLE
                            .run_test(&ctx, "DeleteRolePolicy", test_iam_gen_role_delete_role_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-role:AttachRolePolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_ROLE
                            .run_test(&ctx, "AttachRolePolicy", test_iam_gen_role_attach_role_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-role:ListAttachedRolePolicies".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_ROLE
                            .run_test(&ctx, "ListAttachedRolePolicies", test_iam_gen_role_list_attached_role_policies(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-role:DetachRolePolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_ROLE
                            .run_test(&ctx, "DetachRolePolicy", test_iam_gen_role_detach_role_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-role:PutRolePermissionsBoundary".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_ROLE
                            .run_test(&ctx, "PutRolePermissionsBoundary", test_iam_gen_role_put_role_permissions_boundary(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-role:DeleteRolePermissionsBoundary".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_ROLE
                            .run_test(&ctx, "DeleteRolePermissionsBoundary", test_iam_gen_role_delete_role_permissions_boundary(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-role:GetAccountAuthorizationDetails".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_ROLE
                            .run_test(&ctx, "GetAccountAuthorizationDetails", test_iam_gen_role_get_account_authorization_details(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-role:ListRoles".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_ROLE
                            .run_test(&ctx, "ListRoles", test_iam_gen_role_list_roles(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-role:DeleteRole".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_ROLE
                            .run_test(&ctx, "DeleteRole", test_iam_gen_role_delete_role(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-instanceprofile:CreateInstanceProfile".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_INSTANCEPROFILE
                            .run_test(&ctx, "CreateInstanceProfile", test_iam_gen_instanceprofile_create_instance_profile(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-instanceprofile:GetInstanceProfile".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_INSTANCEPROFILE
                            .run_test(&ctx, "GetInstanceProfile", test_iam_gen_instanceprofile_get_instance_profile(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-instanceprofile:TagInstanceProfile".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_INSTANCEPROFILE
                            .run_test(&ctx, "TagInstanceProfile", test_iam_gen_instanceprofile_tag_instance_profile(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-instanceprofile:ListInstanceProfileTags".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_INSTANCEPROFILE
                            .run_test(&ctx, "ListInstanceProfileTags", test_iam_gen_instanceprofile_list_instance_profile_tags(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-instanceprofile:UntagInstanceProfile".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_INSTANCEPROFILE
                            .run_test(&ctx, "UntagInstanceProfile", test_iam_gen_instanceprofile_untag_instance_profile(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-instanceprofile:AddRoleToInstanceProfile".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_INSTANCEPROFILE
                            .run_test(&ctx, "AddRoleToInstanceProfile", test_iam_gen_instanceprofile_add_role_to_instance_profile(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-instanceprofile:ListInstanceProfilesForRole".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_INSTANCEPROFILE
                            .run_test(&ctx, "ListInstanceProfilesForRole", test_iam_gen_instanceprofile_list_instance_profiles_for_role(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-instanceprofile:RemoveRoleFromInstanceProfile".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_INSTANCEPROFILE
                            .run_test(&ctx, "RemoveRoleFromInstanceProfile", test_iam_gen_instanceprofile_remove_role_from_instance_profile(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-instanceprofile:ListInstanceProfiles".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_INSTANCEPROFILE
                            .run_test(&ctx, "ListInstanceProfiles", test_iam_gen_instanceprofile_list_instance_profiles(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-instanceprofile:DeleteInstanceProfile".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_INSTANCEPROFILE
                            .run_test(&ctx, "DeleteInstanceProfile", test_iam_gen_instanceprofile_delete_instance_profile(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-probe:GetAccessKeyLastUsed".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_PROBE
                            .run_test(&ctx, "GetAccessKeyLastUsed", test_iam_gen_probe_get_access_key_last_used(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-probe:GetAccountPasswordPolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_PROBE
                            .run_test(&ctx, "GetAccountPasswordPolicy", test_iam_gen_probe_get_account_password_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-probe:GetAccountSummary".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_PROBE
                            .run_test(&ctx, "GetAccountSummary", test_iam_gen_probe_get_account_summary(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-probe:GetContextKeysForCustomPolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_PROBE
                            .run_test(&ctx, "GetContextKeysForCustomPolicy", test_iam_gen_probe_get_context_keys_for_custom_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-probe:GetContextKeysForPrincipalPolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_PROBE
                            .run_test(&ctx, "GetContextKeysForPrincipalPolicy", test_iam_gen_probe_get_context_keys_for_principal_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-probe:GetCredentialReport".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_PROBE
                            .run_test(&ctx, "GetCredentialReport", test_iam_gen_probe_get_credential_report(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-probe:GetDelegationRequest".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_PROBE
                            .run_test(&ctx, "GetDelegationRequest", test_iam_gen_probe_get_delegation_request(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-probe:GetHumanReadableSummary".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_PROBE
                            .run_test(&ctx, "GetHumanReadableSummary", test_iam_gen_probe_get_human_readable_summary(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-probe:GetLoginProfile".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_PROBE
                            .run_test(&ctx, "GetLoginProfile", test_iam_gen_probe_get_login_profile(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-probe:GetMFADevice".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_PROBE
                            .run_test(&ctx, "GetMFADevice", test_iam_gen_probe_get_mfa_device(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-probe:GetOpenIDConnectProvider".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_PROBE
                            .run_test(&ctx, "GetOpenIDConnectProvider", test_iam_gen_probe_get_open_id_connect_provider(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-probe:GetOrganizationsAccessReport".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_PROBE
                            .run_test(&ctx, "GetOrganizationsAccessReport", test_iam_gen_probe_get_organizations_access_report(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-probe:GetOutboundWebIdentityFederationInfo".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_PROBE
                            .run_test(&ctx, "GetOutboundWebIdentityFederationInfo", test_iam_gen_probe_get_outbound_web_identity_federation_info(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-probe:GetPolicyVersion".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_PROBE
                            .run_test(&ctx, "GetPolicyVersion", test_iam_gen_probe_get_policy_version(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-probe:GetSAMLProvider".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_PROBE
                            .run_test(&ctx, "GetSAMLProvider", test_iam_gen_probe_get_saml_provider(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-probe:GetSSHPublicKey".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_PROBE
                            .run_test(&ctx, "GetSSHPublicKey", test_iam_gen_probe_get_ssh_public_key(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-probe:GetServerCertificate".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_PROBE
                            .run_test(&ctx, "GetServerCertificate", test_iam_gen_probe_get_server_certificate(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-probe:GetServiceLastAccessedDetails".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_PROBE
                            .run_test(&ctx, "GetServiceLastAccessedDetails", test_iam_gen_probe_get_service_last_accessed_details(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-probe:GetServiceLastAccessedDetailsWithEntities".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_PROBE
                            .run_test(&ctx, "GetServiceLastAccessedDetailsWithEntities", test_iam_gen_probe_get_service_last_accessed_details_with_entities(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-probe:GetServiceLinkedRoleDeletionStatus".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_PROBE
                            .run_test(&ctx, "GetServiceLinkedRoleDeletionStatus", test_iam_gen_probe_get_service_linked_role_deletion_status(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-probe:ListAccountAliases".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_PROBE
                            .run_test(&ctx, "ListAccountAliases", test_iam_gen_probe_list_account_aliases(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-probe:ListDelegationRequests".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_PROBE
                            .run_test(&ctx, "ListDelegationRequests", test_iam_gen_probe_list_delegation_requests(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-probe:ListEntitiesForPolicy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_PROBE
                            .run_test(&ctx, "ListEntitiesForPolicy", test_iam_gen_probe_list_entities_for_policy(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-probe:ListMFADeviceTags".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_PROBE
                            .run_test(&ctx, "ListMFADeviceTags", test_iam_gen_probe_list_mfa_device_tags(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-probe:ListMFADevices".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_PROBE
                            .run_test(&ctx, "ListMFADevices", test_iam_gen_probe_list_mfa_devices(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-probe:ListOpenIDConnectProviderTags".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_PROBE
                            .run_test(&ctx, "ListOpenIDConnectProviderTags", test_iam_gen_probe_list_open_id_connect_provider_tags(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-probe:ListOpenIDConnectProviders".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_PROBE
                            .run_test(&ctx, "ListOpenIDConnectProviders", test_iam_gen_probe_list_open_id_connect_providers(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-probe:ListOrganizationsFeatures".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_PROBE
                            .run_test(&ctx, "ListOrganizationsFeatures", test_iam_gen_probe_list_organizations_features(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-probe:ListPoliciesGrantingServiceAccess".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_PROBE
                            .run_test(&ctx, "ListPoliciesGrantingServiceAccess", test_iam_gen_probe_list_policies_granting_service_access(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-probe:ListPolicyVersions".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_PROBE
                            .run_test(&ctx, "ListPolicyVersions", test_iam_gen_probe_list_policy_versions(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-probe:ListSAMLProviderTags".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_PROBE
                            .run_test(&ctx, "ListSAMLProviderTags", test_iam_gen_probe_list_saml_provider_tags(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-probe:ListSAMLProviders".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_PROBE
                            .run_test(&ctx, "ListSAMLProviders", test_iam_gen_probe_list_saml_providers(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-probe:ListSSHPublicKeys".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_PROBE
                            .run_test(&ctx, "ListSSHPublicKeys", test_iam_gen_probe_list_ssh_public_keys(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-probe:ListServerCertificateTags".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_PROBE
                            .run_test(&ctx, "ListServerCertificateTags", test_iam_gen_probe_list_server_certificate_tags(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-probe:ListServerCertificates".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_PROBE
                            .run_test(&ctx, "ListServerCertificates", test_iam_gen_probe_list_server_certificates(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-probe:ListServiceSpecificCredentials".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_PROBE
                            .run_test(&ctx, "ListServiceSpecificCredentials", test_iam_gen_probe_list_service_specific_credentials(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-probe:ListSigningCertificates".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_PROBE
                            .run_test(&ctx, "ListSigningCertificates", test_iam_gen_probe_list_signing_certificates(&client))
                            .await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            impls.insert(
                "iam-gen-probe:ListVirtualMFADevices".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_PROBE
                            .run_test(&ctx, "ListVirtualMFADevices", test_iam_gen_probe_list_virtual_mfa_devices(&client))
                            .await
                    })
                }),
            );
        }
        impls
    }

    fn setups(&self) -> HashMap<String, TestFn> {
        let mut setups: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            setups.insert(
                "iam-gen-policy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_POLICY.run_setup(&ctx, setup_iam_gen_policy(&client)).await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            setups.insert(
                "iam-gen-user".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_USER.run_setup(&ctx, setup_iam_gen_user(&client)).await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            setups.insert(
                "iam-gen-group".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_GROUP.run_setup(&ctx, setup_iam_gen_group(&client)).await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            setups.insert(
                "iam-gen-role".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_ROLE.run_setup(&ctx, setup_iam_gen_role(&client)).await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            setups.insert(
                "iam-gen-instanceprofile".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_INSTANCEPROFILE.run_setup(&ctx, setup_iam_gen_instanceprofile(&client)).await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            setups.insert(
                "iam-gen-probe".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_PROBE.run_setup(&ctx, setup_iam_gen_probe(&client)).await
                    })
                }),
            );
        }
        setups
    }

    fn teardowns(&self) -> HashMap<String, TestFn> {
        let mut teardowns: HashMap<String, TestFn> = HashMap::new();
        {
            let client = self.client.clone();
            teardowns.insert(
                "iam-gen-policy".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_POLICY.run_teardown(&ctx, teardown_iam_gen_policy(&client)).await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            teardowns.insert(
                "iam-gen-user".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_USER.run_teardown(&ctx, teardown_iam_gen_user(&client)).await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            teardowns.insert(
                "iam-gen-group".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_GROUP.run_teardown(&ctx, teardown_iam_gen_group(&client)).await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            teardowns.insert(
                "iam-gen-role".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_ROLE.run_teardown(&ctx, teardown_iam_gen_role(&client)).await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            teardowns.insert(
                "iam-gen-instanceprofile".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_INSTANCEPROFILE.run_teardown(&ctx, teardown_iam_gen_instanceprofile(&client)).await
                    })
                }),
            );
        }
        {
            let client = self.client.clone();
            teardowns.insert(
                "iam-gen-probe".to_string(),
                Arc::new(move |ctx: TestContext| {
                    let client = client.clone();
                    Box::pin(async move {
                        GROUP_IAM_GEN_PROBE.run_teardown(&ctx, teardown_iam_gen_probe(&client)).await
                    })
                }),
            );
        }
        teardowns
    }
}

fn setup_iam_gen_policy(_client: &aws_sdk_iam::Client) -> Vec<Call> {
    // An empty phase is a no-op, not a missing one.
    Vec::new()
}

fn teardown_iam_gen_policy(client: &aws_sdk_iam::Client) -> Vec<Call> {
    vec![
        Call {
            op: "DeletePolicy",
            params: scenario::map(vec![
                ("PolicyArn", scenario::context("policy.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_policy()
                            .policy_arn(b.string("PolicyArn")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn test_iam_gen_policy_create_policy(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "CreatePolicy",
            params: scenario::map(vec![
                ("Description", scenario::lit(::serde_json::json!("compat scenario managed policy"))),
                ("PolicyDocument", scenario::lit(::serde_json::json!("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Action\":\"s3:GetObject\",\"Resource\":\"arn:aws:s3:::compat-scenario-nonexistent/*\"}]}"))),
                ("PolicyName", scenario::name("p")),
            ]),
            export: vec![
                ("policy.arn", "$.Policy.Arn"),
                ("policy.name", "$.Policy.PolicyName"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_policy()
                            .description("compat scenario managed policy")
                            .policy_document("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Action\":\"s3:GetObject\",\"Resource\":\"arn:aws:s3:::compat-scenario-nonexistent/*\"}]}")
                            .policy_name(b.string("PolicyName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.Policy.Arn"),
                scenario::matches("$.Policy.PolicyName", "^[\\w+=,.@-]+$"),
            ]),
            scenario::eventually(6, 500,
                scenario::readback(
                    Call {
                        op: "GetPolicy",
                        params: scenario::map(vec![
                            ("PolicyArn", scenario::context("policy.arn")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .get_policy()
                                        .policy_arn(b.string("PolicyArn")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::equals("$.Policy.Arn", scenario::context("policy.arn")),
                    ],
                ),
            ),
            scenario::eventually(6, 500,
                scenario::list_contains(
                    Some(Call {
                        op: "ListPolicies",
                        params: scenario::lit(::serde_json::json!({"Scope": "Local"})),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |_b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_policies()
                                        .scope(aws_sdk_iam::types::PolicyScopeType::from("Local"))
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.Policies",
                    vec![
                        scenario::where_entry("$.Arn", scenario::context("policy.arn")),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_gen_policy_get_policy(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "GetPolicy",
            params: scenario::map(vec![
                ("PolicyArn", scenario::context("policy.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_policy()
                            .policy_arn(b.string("PolicyArn")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.Policy.Arn", scenario::context("policy.arn")),
            ]),
        ],
    }
}

fn test_iam_gen_policy_tag_policy(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "TagPolicy",
            params: scenario::map(vec![
                ("PolicyArn", scenario::context("policy.arn")),
                ("Tags", scenario::lit(::serde_json::json!([{"Key": "compat", "Value": "scenario"}]))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .tag_policy()
                            .policy_arn(b.string("PolicyArn")?)
                            .tags(
                                aws_sdk_iam::types::Tag::builder()
                                    .key("compat")
                                    .value("scenario")
                                    .build()
                                    .map_err(|err| scenario::build_error("Tags[0]", err))?
                            )
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::list_contains(
                    Some(Call {
                        op: "ListPolicyTags",
                        params: scenario::map(vec![
                            ("PolicyArn", scenario::context("policy.arn")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_policy_tags()
                                        .policy_arn(b.string("PolicyArn")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.Tags",
                    vec![
                        scenario::where_entry("$.Key", scenario::lit(::serde_json::json!("compat"))),
                        scenario::where_entry("$.Value", scenario::lit(::serde_json::json!("scenario"))),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_gen_policy_list_policy_tags(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "ListPolicyTags",
            params: scenario::map(vec![
                ("PolicyArn", scenario::context("policy.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_policy_tags()
                            .policy_arn(b.string("PolicyArn")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.Tags",
                vec![
                    scenario::where_entry("$.Key", scenario::lit(::serde_json::json!("compat"))),
                    scenario::where_entry("$.Value", scenario::lit(::serde_json::json!("scenario"))),
                ],
            ),
        ],
    }
}

fn test_iam_gen_policy_untag_policy(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "UntagPolicy",
            params: scenario::map(vec![
                ("PolicyArn", scenario::context("policy.arn")),
                ("TagKeys", scenario::lit(::serde_json::json!(["compat"]))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .untag_policy()
                            .policy_arn(b.string("PolicyArn")?)
                            .tag_keys("compat")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::absent_from_list(
                    Some(Call {
                        op: "ListPolicyTags",
                        params: scenario::map(vec![
                            ("PolicyArn", scenario::context("policy.arn")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_policy_tags()
                                        .policy_arn(b.string("PolicyArn")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.Tags",
                    vec![
                        scenario::where_entry("$.Key", scenario::lit(::serde_json::json!("compat"))),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_gen_policy_create_policy_version(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "CreatePolicyVersion",
            params: scenario::map(vec![
                ("PolicyArn", scenario::context("policy.arn")),
                ("PolicyDocument", scenario::lit(::serde_json::json!("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Action\":\"s3:PutObject\",\"Resource\":\"arn:aws:s3:::compat-scenario-nonexistent/*\"}]}"))),
                ("SetAsDefault", scenario::lit(::serde_json::json!(true))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_policy_version()
                            .policy_arn(b.string("PolicyArn")?)
                            .policy_document("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Action\":\"s3:PutObject\",\"Resource\":\"arn:aws:s3:::compat-scenario-nonexistent/*\"}]}")
                            .set_as_default(true)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.PolicyVersion.IsDefaultVersion", scenario::lit(::serde_json::json!(true))),
                scenario::equals("$.PolicyVersion.VersionId", scenario::lit(::serde_json::json!("v2"))),
            ]),
            scenario::eventually(6, 500,
                scenario::readback(
                    Call {
                        op: "GetPolicy",
                        params: scenario::map(vec![
                            ("PolicyArn", scenario::context("policy.arn")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .get_policy()
                                        .policy_arn(b.string("PolicyArn")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::equals("$.Policy.DefaultVersionId", scenario::lit(::serde_json::json!("v2"))),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_gen_policy_delete_policy_version(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "DeletePolicyVersion",
            params: scenario::map(vec![
                ("PolicyArn", scenario::context("policy.arn")),
                ("VersionId", scenario::lit(::serde_json::json!("v1"))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_policy_version()
                            .policy_arn(b.string("PolicyArn")?)
                            .version_id("v1")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::absent_from_list(
                    Some(Call {
                        op: "ListPolicyVersions",
                        params: scenario::map(vec![
                            ("PolicyArn", scenario::context("policy.arn")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_policy_versions()
                                        .policy_arn(b.string("PolicyArn")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.Versions",
                    vec![
                        scenario::where_entry("$.VersionId", scenario::lit(::serde_json::json!("v1"))),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_gen_policy_simulate_custom_policy(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "SimulateCustomPolicy",
            params: scenario::lit(::serde_json::json!({"ActionNames": ["s3:GetObject"], "PolicyInputList": ["{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Action\":\"s3:GetObject\",\"Resource\":\"arn:aws:s3:::compat-scenario-nonexistent/*\"}]}"], "ResourceArns": ["arn:aws:s3:::compat-scenario-nonexistent/report.csv"]})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .simulate_custom_policy()
                            .action_names("s3:GetObject")
                            .policy_input_list("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Action\":\"s3:GetObject\",\"Resource\":\"arn:aws:s3:::compat-scenario-nonexistent/*\"}]}")
                            .resource_arns("arn:aws:s3:::compat-scenario-nonexistent/report.csv")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.EvaluationResults[0].EvalActionName", scenario::lit(::serde_json::json!("s3:GetObject"))),
                scenario::equals("$.EvaluationResults[0].EvalDecision", scenario::lit(::serde_json::json!("allowed"))),
            ]),
        ],
    }
}

fn test_iam_gen_policy_list_policies(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "ListPolicies",
            params: scenario::lit(::serde_json::json!({"Scope": "Local"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_policies()
                            .scope(aws_sdk_iam::types::PolicyScopeType::from("Local"))
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.Policies",
                vec![
                    scenario::where_entry("$.Arn", scenario::context("policy.arn")),
                ],
            ),
        ],
    }
}

fn test_iam_gen_policy_delete_policy(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "DeletePolicy",
            params: scenario::map(vec![
                ("PolicyArn", scenario::context("policy.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_policy()
                            .policy_arn(b.string("PolicyArn")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::absent_by_error(
                    Call {
                        op: "GetPolicy",
                        params: scenario::map(vec![
                            ("PolicyArn", scenario::context("policy.arn")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .get_policy()
                                        .policy_arn(b.string("PolicyArn")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    scenario::error("NoSuchEntityException", "NoSuchEntity"),
                ),
            ),
        ],
    }
}

fn setup_iam_gen_user(client: &aws_sdk_iam::Client) -> Vec<Call> {
    vec![
        Call {
            op: "CreatePolicy",
            params: scenario::map(vec![
                ("Description", scenario::lit(::serde_json::json!("compat scenario managed policy"))),
                ("PolicyDocument", scenario::lit(::serde_json::json!("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Action\":\"s3:GetObject\",\"Resource\":\"arn:aws:s3:::compat-scenario-nonexistent/*\"}]}"))),
                ("PolicyName", scenario::name("p")),
            ]),
            export: vec![
                ("policy.arn", "$.Policy.Arn"),
                ("policy.name", "$.Policy.PolicyName"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_policy()
                            .description("compat scenario managed policy")
                            .policy_document("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Action\":\"s3:GetObject\",\"Resource\":\"arn:aws:s3:::compat-scenario-nonexistent/*\"}]}")
                            .policy_name(b.string("PolicyName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn teardown_iam_gen_user(client: &aws_sdk_iam::Client) -> Vec<Call> {
    vec![
        Call {
            op: "DeleteUser",
            params: scenario::map(vec![
                ("UserName", scenario::context("user.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_user()
                            .user_name(b.string("UserName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "DeletePolicy",
            params: scenario::map(vec![
                ("PolicyArn", scenario::context("policy.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_policy()
                            .policy_arn(b.string("PolicyArn")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn test_iam_gen_user_create_user(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "CreateUser",
            params: scenario::map(vec![("UserName", scenario::name("u"))]),
            export: vec![
                ("user.arn", "$.User.Arn"),
                ("user.name", "$.User.UserName"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_user()
                            .user_name(b.string("UserName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.User.Arn"),
                scenario::matches("$.User.UserName", "^[\\w+=,.@-]+$"),
            ]),
            scenario::eventually(6, 500,
                scenario::readback(
                    Call {
                        op: "GetUser",
                        params: scenario::map(vec![
                            ("UserName", scenario::context("user.name")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .get_user()
                                        .user_name(b.string("UserName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::equals("$.User.UserName", scenario::context("user.name")),
                    ],
                ),
            ),
            scenario::eventually(6, 500,
                scenario::list_contains(
                    Some(Call {
                        op: "ListUsers",
                        params: scenario::lit(::serde_json::json!({})),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |_b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_users()
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.Users",
                    vec![
                        scenario::where_entry("$.UserName", scenario::context("user.name")),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_gen_user_get_user(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "GetUser",
            params: scenario::map(vec![
                ("UserName", scenario::context("user.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_user()
                            .user_name(b.string("UserName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.User.UserName", scenario::context("user.name")),
            ]),
        ],
    }
}

fn test_iam_gen_user_update_user(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "UpdateUser",
            params: scenario::map(vec![
                ("NewPath", scenario::lit(::serde_json::json!("/compat-scenario-updated/"))),
                ("UserName", scenario::context("user.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .update_user()
                            .new_path("/compat-scenario-updated/")
                            .user_name(b.string("UserName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::readback(
                    Call {
                        op: "GetUser",
                        params: scenario::map(vec![
                            ("UserName", scenario::context("user.name")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .get_user()
                                        .user_name(b.string("UserName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::equals("$.User.Path", scenario::lit(::serde_json::json!("/compat-scenario-updated/"))),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_gen_user_tag_user(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "TagUser",
            params: scenario::map(vec![
                ("Tags", scenario::lit(::serde_json::json!([{"Key": "compat", "Value": "scenario"}]))),
                ("UserName", scenario::context("user.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .tag_user()
                            .tags(
                                aws_sdk_iam::types::Tag::builder()
                                    .key("compat")
                                    .value("scenario")
                                    .build()
                                    .map_err(|err| scenario::build_error("Tags[0]", err))?
                            )
                            .user_name(b.string("UserName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::list_contains(
                    Some(Call {
                        op: "ListUserTags",
                        params: scenario::map(vec![
                            ("UserName", scenario::context("user.name")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_user_tags()
                                        .user_name(b.string("UserName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.Tags",
                    vec![
                        scenario::where_entry("$.Key", scenario::lit(::serde_json::json!("compat"))),
                        scenario::where_entry("$.Value", scenario::lit(::serde_json::json!("scenario"))),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_gen_user_list_user_tags(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "ListUserTags",
            params: scenario::map(vec![
                ("UserName", scenario::context("user.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_user_tags()
                            .user_name(b.string("UserName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.Tags",
                vec![
                    scenario::where_entry("$.Key", scenario::lit(::serde_json::json!("compat"))),
                    scenario::where_entry("$.Value", scenario::lit(::serde_json::json!("scenario"))),
                ],
            ),
        ],
    }
}

fn test_iam_gen_user_untag_user(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "UntagUser",
            params: scenario::map(vec![
                ("TagKeys", scenario::lit(::serde_json::json!(["compat"]))),
                ("UserName", scenario::context("user.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .untag_user()
                            .tag_keys("compat")
                            .user_name(b.string("UserName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::absent_from_list(
                    Some(Call {
                        op: "ListUserTags",
                        params: scenario::map(vec![
                            ("UserName", scenario::context("user.name")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_user_tags()
                                        .user_name(b.string("UserName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.Tags",
                    vec![
                        scenario::where_entry("$.Key", scenario::lit(::serde_json::json!("compat"))),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_gen_user_put_user_policy(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "PutUserPolicy",
            params: scenario::map(vec![
                ("PolicyDocument", scenario::lit(::serde_json::json!("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Action\":\"s3:GetObject\",\"Resource\":\"arn:aws:s3:::compat-scenario-nonexistent/*\"}]}"))),
                ("PolicyName", scenario::name("inline")),
                ("UserName", scenario::context("user.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .put_user_policy()
                            .policy_document("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Action\":\"s3:GetObject\",\"Resource\":\"arn:aws:s3:::compat-scenario-nonexistent/*\"}]}")
                            .policy_name(b.string("PolicyName")?)
                            .user_name(b.string("UserName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::list_contains(
                    Some(Call {
                        op: "ListUserPolicies",
                        params: scenario::map(vec![
                            ("UserName", scenario::context("user.name")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_user_policies()
                                        .user_name(b.string("UserName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.PolicyNames",
                    vec![
                        scenario::where_entry("$", scenario::name("inline")),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_gen_user_get_user_policy(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "GetUserPolicy",
            params: scenario::map(vec![
                ("PolicyName", scenario::name("inline")),
                ("UserName", scenario::context("user.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_user_policy()
                            .policy_name(b.string("PolicyName")?)
                            .user_name(b.string("UserName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.PolicyName", scenario::name("inline")),
                scenario::equals("$.UserName", scenario::context("user.name")),
            ]),
        ],
    }
}

fn test_iam_gen_user_list_user_policies(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "ListUserPolicies",
            params: scenario::map(vec![
                ("UserName", scenario::context("user.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_user_policies()
                            .user_name(b.string("UserName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.PolicyNames",
                vec![
                    scenario::where_entry("$", scenario::name("inline")),
                ],
            ),
        ],
    }
}

fn test_iam_gen_user_delete_user_policy(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "DeleteUserPolicy",
            params: scenario::map(vec![
                ("PolicyName", scenario::name("inline")),
                ("UserName", scenario::context("user.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_user_policy()
                            .policy_name(b.string("PolicyName")?)
                            .user_name(b.string("UserName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::absent_from_list(
                    Some(Call {
                        op: "ListUserPolicies",
                        params: scenario::map(vec![
                            ("UserName", scenario::context("user.name")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_user_policies()
                                        .user_name(b.string("UserName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.PolicyNames",
                    vec![
                        scenario::where_entry("$", scenario::name("inline")),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_gen_user_attach_user_policy(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "AttachUserPolicy",
            params: scenario::map(vec![
                ("PolicyArn", scenario::context("policy.arn")),
                ("UserName", scenario::context("user.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .attach_user_policy()
                            .policy_arn(b.string("PolicyArn")?)
                            .user_name(b.string("UserName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::list_contains(
                    Some(Call {
                        op: "ListAttachedUserPolicies",
                        params: scenario::map(vec![
                            ("UserName", scenario::context("user.name")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_attached_user_policies()
                                        .user_name(b.string("UserName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.AttachedPolicies",
                    vec![
                        scenario::where_entry("$.PolicyArn", scenario::context("policy.arn")),
                        scenario::where_entry("$.PolicyName", scenario::context("policy.name")),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_gen_user_list_attached_user_policies(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "ListAttachedUserPolicies",
            params: scenario::map(vec![
                ("UserName", scenario::context("user.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_attached_user_policies()
                            .user_name(b.string("UserName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.AttachedPolicies",
                vec![
                    scenario::where_entry("$.PolicyArn", scenario::context("policy.arn")),
                    scenario::where_entry("$.PolicyName", scenario::context("policy.name")),
                ],
            ),
        ],
    }
}

fn test_iam_gen_user_detach_user_policy(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "DetachUserPolicy",
            params: scenario::map(vec![
                ("PolicyArn", scenario::context("policy.arn")),
                ("UserName", scenario::context("user.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .detach_user_policy()
                            .policy_arn(b.string("PolicyArn")?)
                            .user_name(b.string("UserName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::absent_from_list(
                    Some(Call {
                        op: "ListAttachedUserPolicies",
                        params: scenario::map(vec![
                            ("UserName", scenario::context("user.name")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_attached_user_policies()
                                        .user_name(b.string("UserName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.AttachedPolicies",
                    vec![
                        scenario::where_entry("$.PolicyArn", scenario::context("policy.arn")),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_gen_user_create_access_key(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "CreateAccessKey",
            params: scenario::map(vec![
                ("UserName", scenario::context("user.name")),
            ]),
            export: vec![
                ("user.accesskeyid", "$.AccessKey.AccessKeyId"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_access_key()
                            .user_name(b.string("UserName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::matches("$.AccessKey.AccessKeyId", "^AKIA[A-Z0-9]{16}$"),
                scenario::non_empty("$.AccessKey.SecretAccessKey"),
                scenario::equals("$.AccessKey.Status", scenario::lit(::serde_json::json!("Active"))),
                scenario::equals("$.AccessKey.UserName", scenario::context("user.name")),
            ]),
            scenario::eventually(6, 500,
                scenario::list_contains(
                    Some(Call {
                        op: "ListAccessKeys",
                        params: scenario::map(vec![
                            ("UserName", scenario::context("user.name")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_access_keys()
                                        .user_name(b.string("UserName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.AccessKeyMetadata",
                    vec![
                        scenario::where_entry("$.AccessKeyId", scenario::context("user.accesskeyid")),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_gen_user_list_access_keys(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "ListAccessKeys",
            params: scenario::map(vec![
                ("UserName", scenario::context("user.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_access_keys()
                            .user_name(b.string("UserName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.AccessKeyMetadata",
                vec![
                    scenario::where_entry("$.AccessKeyId", scenario::context("user.accesskeyid")),
                    scenario::where_entry("$.Status", scenario::lit(::serde_json::json!("Active"))),
                    scenario::where_entry("$.UserName", scenario::context("user.name")),
                ],
            ),
        ],
    }
}

fn test_iam_gen_user_delete_access_key(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "DeleteAccessKey",
            params: scenario::map(vec![
                ("AccessKeyId", scenario::context("user.accesskeyid")),
                ("UserName", scenario::context("user.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_access_key()
                            .access_key_id(b.string("AccessKeyId")?)
                            .user_name(b.string("UserName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::absent_from_list(
                    Some(Call {
                        op: "ListAccessKeys",
                        params: scenario::map(vec![
                            ("UserName", scenario::context("user.name")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_access_keys()
                                        .user_name(b.string("UserName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.AccessKeyMetadata",
                    vec![
                        scenario::where_entry("$.AccessKeyId", scenario::context("user.accesskeyid")),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_gen_user_put_user_permissions_boundary(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "PutUserPermissionsBoundary",
            params: scenario::map(vec![
                ("PermissionsBoundary", scenario::context("policy.arn")),
                ("UserName", scenario::context("user.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .put_user_permissions_boundary()
                            .permissions_boundary(b.string("PermissionsBoundary")?)
                            .user_name(b.string("UserName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::readback(
                    Call {
                        op: "GetUser",
                        params: scenario::map(vec![
                            ("UserName", scenario::context("user.name")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .get_user()
                                        .user_name(b.string("UserName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::equals("$.User.PermissionsBoundary.PermissionsBoundaryArn", scenario::context("policy.arn")),
                        scenario::equals("$.User.PermissionsBoundary.PermissionsBoundaryType", scenario::lit(::serde_json::json!("PermissionsBoundaryPolicy"))),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_gen_user_delete_user_permissions_boundary(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "DeleteUserPermissionsBoundary",
            params: scenario::map(vec![
                ("UserName", scenario::context("user.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_user_permissions_boundary()
                            .user_name(b.string("UserName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::readback(
                    Call {
                        op: "GetUser",
                        params: scenario::map(vec![
                            ("UserName", scenario::context("user.name")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .get_user()
                                        .user_name(b.string("UserName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::missing("$.User.PermissionsBoundary"),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_gen_user_list_users(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "ListUsers",
            params: scenario::lit(::serde_json::json!({})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_users()
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.Users",
                vec![
                    scenario::where_entry("$.UserName", scenario::context("user.name")),
                ],
            ),
        ],
    }
}

fn test_iam_gen_user_delete_user(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "DeleteUser",
            params: scenario::map(vec![
                ("UserName", scenario::context("user.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_user()
                            .user_name(b.string("UserName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::absent_by_error(
                    Call {
                        op: "GetUser",
                        params: scenario::map(vec![
                            ("UserName", scenario::context("user.name")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .get_user()
                                        .user_name(b.string("UserName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    scenario::error("NoSuchEntityException", "NoSuchEntity"),
                ),
            ),
        ],
    }
}

fn setup_iam_gen_group(client: &aws_sdk_iam::Client) -> Vec<Call> {
    vec![
        Call {
            op: "CreatePolicy",
            params: scenario::map(vec![
                ("Description", scenario::lit(::serde_json::json!("compat scenario managed policy"))),
                ("PolicyDocument", scenario::lit(::serde_json::json!("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Action\":\"s3:GetObject\",\"Resource\":\"arn:aws:s3:::compat-scenario-nonexistent/*\"}]}"))),
                ("PolicyName", scenario::name("p")),
            ]),
            export: vec![
                ("policy.arn", "$.Policy.Arn"),
                ("policy.name", "$.Policy.PolicyName"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_policy()
                            .description("compat scenario managed policy")
                            .policy_document("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Action\":\"s3:GetObject\",\"Resource\":\"arn:aws:s3:::compat-scenario-nonexistent/*\"}]}")
                            .policy_name(b.string("PolicyName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "CreateUser",
            params: scenario::map(vec![("UserName", scenario::name("u"))]),
            export: vec![
                ("user.arn", "$.User.Arn"),
                ("user.name", "$.User.UserName"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_user()
                            .user_name(b.string("UserName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn teardown_iam_gen_group(client: &aws_sdk_iam::Client) -> Vec<Call> {
    vec![
        Call {
            op: "DeleteGroup",
            params: scenario::map(vec![
                ("GroupName", scenario::context("group.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_group()
                            .group_name(b.string("GroupName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "DeleteUser",
            params: scenario::map(vec![
                ("UserName", scenario::context("user.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_user()
                            .user_name(b.string("UserName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "DeletePolicy",
            params: scenario::map(vec![
                ("PolicyArn", scenario::context("policy.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_policy()
                            .policy_arn(b.string("PolicyArn")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn test_iam_gen_group_create_group(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "CreateGroup",
            params: scenario::map(vec![("GroupName", scenario::name("g"))]),
            export: vec![
                ("group.arn", "$.Group.Arn"),
                ("group.name", "$.Group.GroupName"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_group()
                            .group_name(b.string("GroupName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.Group.Arn"),
                scenario::matches("$.Group.GroupName", "^[\\w+=,.@-]+$"),
            ]),
            scenario::eventually(6, 500,
                scenario::readback(
                    Call {
                        op: "GetGroup",
                        params: scenario::map(vec![
                            ("GroupName", scenario::context("group.name")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .get_group()
                                        .group_name(b.string("GroupName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::equals("$.Group.Arn", scenario::context("group.arn")),
                    ],
                ),
            ),
            scenario::eventually(6, 500,
                scenario::list_contains(
                    Some(Call {
                        op: "ListGroups",
                        params: scenario::lit(::serde_json::json!({})),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |_b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_groups()
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.Groups",
                    vec![
                        scenario::where_entry("$.Arn", scenario::context("group.arn")),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_gen_group_get_group(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "GetGroup",
            params: scenario::map(vec![
                ("GroupName", scenario::context("group.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_group()
                            .group_name(b.string("GroupName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.Group.Arn", scenario::context("group.arn")),
            ]),
        ],
    }
}

fn test_iam_gen_group_add_user_to_group(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "AddUserToGroup",
            params: scenario::map(vec![
                ("GroupName", scenario::context("group.name")),
                ("UserName", scenario::context("user.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .add_user_to_group()
                            .group_name(b.string("GroupName")?)
                            .user_name(b.string("UserName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::list_contains(
                    Some(Call {
                        op: "ListGroupsForUser",
                        params: scenario::map(vec![
                            ("UserName", scenario::context("user.name")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_groups_for_user()
                                        .user_name(b.string("UserName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.Groups",
                    vec![
                        scenario::where_entry("$.Arn", scenario::context("group.arn")),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_gen_group_get_group_members(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "GetGroup",
            params: scenario::map(vec![
                ("GroupName", scenario::context("group.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_group()
                            .group_name(b.string("GroupName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.Users",
                vec![
                    scenario::where_entry("$.UserName", scenario::context("user.name")),
                ],
            ),
        ],
    }
}

fn test_iam_gen_group_list_groups_for_user(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "ListGroupsForUser",
            params: scenario::map(vec![
                ("UserName", scenario::context("user.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_groups_for_user()
                            .user_name(b.string("UserName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.Groups",
                vec![
                    scenario::where_entry("$.Arn", scenario::context("group.arn")),
                    scenario::where_entry("$.GroupName", scenario::context("group.name")),
                ],
            ),
        ],
    }
}

fn test_iam_gen_group_put_group_policy(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "PutGroupPolicy",
            params: scenario::map(vec![
                ("GroupName", scenario::context("group.name")),
                ("PolicyDocument", scenario::lit(::serde_json::json!("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Action\":\"s3:GetObject\",\"Resource\":\"arn:aws:s3:::compat-scenario-nonexistent/*\"}]}"))),
                ("PolicyName", scenario::name("inline")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .put_group_policy()
                            .group_name(b.string("GroupName")?)
                            .policy_document("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Action\":\"s3:GetObject\",\"Resource\":\"arn:aws:s3:::compat-scenario-nonexistent/*\"}]}")
                            .policy_name(b.string("PolicyName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::list_contains(
                    Some(Call {
                        op: "ListGroupPolicies",
                        params: scenario::map(vec![
                            ("GroupName", scenario::context("group.name")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_group_policies()
                                        .group_name(b.string("GroupName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.PolicyNames",
                    vec![
                        scenario::where_entry("$", scenario::name("inline")),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_gen_group_get_group_policy(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "GetGroupPolicy",
            params: scenario::map(vec![
                ("GroupName", scenario::context("group.name")),
                ("PolicyName", scenario::name("inline")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_group_policy()
                            .group_name(b.string("GroupName")?)
                            .policy_name(b.string("PolicyName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.GroupName", scenario::context("group.name")),
                scenario::equals("$.PolicyName", scenario::name("inline")),
            ]),
        ],
    }
}

fn test_iam_gen_group_list_group_policies(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "ListGroupPolicies",
            params: scenario::map(vec![
                ("GroupName", scenario::context("group.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_group_policies()
                            .group_name(b.string("GroupName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.PolicyNames",
                vec![
                    scenario::where_entry("$", scenario::name("inline")),
                ],
            ),
        ],
    }
}

fn test_iam_gen_group_delete_group_policy(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "DeleteGroupPolicy",
            params: scenario::map(vec![
                ("GroupName", scenario::context("group.name")),
                ("PolicyName", scenario::name("inline")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_group_policy()
                            .group_name(b.string("GroupName")?)
                            .policy_name(b.string("PolicyName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::absent_from_list(
                    Some(Call {
                        op: "ListGroupPolicies",
                        params: scenario::map(vec![
                            ("GroupName", scenario::context("group.name")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_group_policies()
                                        .group_name(b.string("GroupName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.PolicyNames",
                    vec![
                        scenario::where_entry("$", scenario::name("inline")),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_gen_group_attach_group_policy(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "AttachGroupPolicy",
            params: scenario::map(vec![
                ("GroupName", scenario::context("group.name")),
                ("PolicyArn", scenario::context("policy.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .attach_group_policy()
                            .group_name(b.string("GroupName")?)
                            .policy_arn(b.string("PolicyArn")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::list_contains(
                    Some(Call {
                        op: "ListAttachedGroupPolicies",
                        params: scenario::map(vec![
                            ("GroupName", scenario::context("group.name")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_attached_group_policies()
                                        .group_name(b.string("GroupName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.AttachedPolicies",
                    vec![
                        scenario::where_entry("$.PolicyArn", scenario::context("policy.arn")),
                        scenario::where_entry("$.PolicyName", scenario::context("policy.name")),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_gen_group_list_attached_group_policies(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "ListAttachedGroupPolicies",
            params: scenario::map(vec![
                ("GroupName", scenario::context("group.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_attached_group_policies()
                            .group_name(b.string("GroupName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.AttachedPolicies",
                vec![
                    scenario::where_entry("$.PolicyArn", scenario::context("policy.arn")),
                    scenario::where_entry("$.PolicyName", scenario::context("policy.name")),
                ],
            ),
        ],
    }
}

fn test_iam_gen_group_detach_group_policy(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "DetachGroupPolicy",
            params: scenario::map(vec![
                ("GroupName", scenario::context("group.name")),
                ("PolicyArn", scenario::context("policy.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .detach_group_policy()
                            .group_name(b.string("GroupName")?)
                            .policy_arn(b.string("PolicyArn")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::absent_from_list(
                    Some(Call {
                        op: "ListAttachedGroupPolicies",
                        params: scenario::map(vec![
                            ("GroupName", scenario::context("group.name")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_attached_group_policies()
                                        .group_name(b.string("GroupName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.AttachedPolicies",
                    vec![
                        scenario::where_entry("$.PolicyArn", scenario::context("policy.arn")),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_gen_group_remove_user_from_group(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "RemoveUserFromGroup",
            params: scenario::map(vec![
                ("GroupName", scenario::context("group.name")),
                ("UserName", scenario::context("user.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .remove_user_from_group()
                            .group_name(b.string("GroupName")?)
                            .user_name(b.string("UserName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::absent_from_list(
                    Some(Call {
                        op: "ListGroupsForUser",
                        params: scenario::map(vec![
                            ("UserName", scenario::context("user.name")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_groups_for_user()
                                        .user_name(b.string("UserName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.Groups",
                    vec![
                        scenario::where_entry("$.Arn", scenario::context("group.arn")),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_gen_group_list_groups(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "ListGroups",
            params: scenario::lit(::serde_json::json!({})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_groups()
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.Groups",
                vec![
                    scenario::where_entry("$.Arn", scenario::context("group.arn")),
                ],
            ),
        ],
    }
}

fn test_iam_gen_group_delete_group(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "DeleteGroup",
            params: scenario::map(vec![
                ("GroupName", scenario::context("group.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_group()
                            .group_name(b.string("GroupName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::absent_by_error(
                    Call {
                        op: "GetGroup",
                        params: scenario::map(vec![
                            ("GroupName", scenario::context("group.name")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .get_group()
                                        .group_name(b.string("GroupName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    scenario::error("NoSuchEntityException", "NoSuchEntity"),
                ),
            ),
        ],
    }
}

fn setup_iam_gen_role(client: &aws_sdk_iam::Client) -> Vec<Call> {
    vec![
        Call {
            op: "CreatePolicy",
            params: scenario::map(vec![
                ("Description", scenario::lit(::serde_json::json!("compat scenario managed policy"))),
                ("PolicyDocument", scenario::lit(::serde_json::json!("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Action\":\"s3:GetObject\",\"Resource\":\"arn:aws:s3:::compat-scenario-nonexistent/*\"}]}"))),
                ("PolicyName", scenario::name("p")),
            ]),
            export: vec![
                ("policy.arn", "$.Policy.Arn"),
                ("policy.name", "$.Policy.PolicyName"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_policy()
                            .description("compat scenario managed policy")
                            .policy_document("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Action\":\"s3:GetObject\",\"Resource\":\"arn:aws:s3:::compat-scenario-nonexistent/*\"}]}")
                            .policy_name(b.string("PolicyName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn teardown_iam_gen_role(client: &aws_sdk_iam::Client) -> Vec<Call> {
    vec![
        Call {
            op: "DeleteRole",
            params: scenario::map(vec![
                ("RoleName", scenario::context("role.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_role()
                            .role_name(b.string("RoleName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "DeletePolicy",
            params: scenario::map(vec![
                ("PolicyArn", scenario::context("policy.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_policy()
                            .policy_arn(b.string("PolicyArn")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn test_iam_gen_role_create_role(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "CreateRole",
            params: scenario::map(vec![
                ("AssumeRolePolicyDocument", scenario::lit(::serde_json::json!("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Principal\":{\"Service\":\"ec2.amazonaws.com\"},\"Action\":\"sts:AssumeRole\"}]}"))),
                ("Description", scenario::lit(::serde_json::json!("compat scenario role, initial description"))),
                ("RoleName", scenario::name("r")),
            ]),
            export: vec![
                ("role.arn", "$.Role.Arn"),
                ("role.name", "$.Role.RoleName"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_role()
                            .assume_role_policy_document("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Principal\":{\"Service\":\"ec2.amazonaws.com\"},\"Action\":\"sts:AssumeRole\"}]}")
                            .description("compat scenario role, initial description")
                            .role_name(b.string("RoleName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.Role.Arn"),
                scenario::matches("$.Role.RoleName", "^[\\w+=,.@-]+$"),
            ]),
            scenario::eventually(6, 500,
                scenario::readback(
                    Call {
                        op: "GetRole",
                        params: scenario::map(vec![
                            ("RoleName", scenario::context("role.name")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .get_role()
                                        .role_name(b.string("RoleName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::equals("$.Role.Arn", scenario::context("role.arn")),
                        scenario::equals("$.Role.Description", scenario::lit(::serde_json::json!("compat scenario role, initial description"))),
                    ],
                ),
            ),
            scenario::eventually(6, 500,
                scenario::list_contains(
                    Some(Call {
                        op: "ListRoles",
                        params: scenario::lit(::serde_json::json!({})),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |_b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_roles()
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.Roles",
                    vec![
                        scenario::where_entry("$.Arn", scenario::context("role.arn")),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_gen_role_get_role(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "GetRole",
            params: scenario::map(vec![
                ("RoleName", scenario::context("role.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_role()
                            .role_name(b.string("RoleName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.Role.Arn", scenario::context("role.arn")),
            ]),
        ],
    }
}

fn test_iam_gen_role_update_role(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "UpdateRole",
            params: scenario::map(vec![
                ("Description", scenario::lit(::serde_json::json!("compat scenario role, updated description"))),
                ("RoleName", scenario::context("role.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .update_role()
                            .description("compat scenario role, updated description")
                            .role_name(b.string("RoleName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::readback(
                    Call {
                        op: "GetRole",
                        params: scenario::map(vec![
                            ("RoleName", scenario::context("role.name")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .get_role()
                                        .role_name(b.string("RoleName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::equals("$.Role.Description", scenario::lit(::serde_json::json!("compat scenario role, updated description"))),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_gen_role_tag_role(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "TagRole",
            params: scenario::map(vec![
                ("RoleName", scenario::context("role.name")),
                ("Tags", scenario::lit(::serde_json::json!([{"Key": "compat", "Value": "scenario"}]))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .tag_role()
                            .role_name(b.string("RoleName")?)
                            .tags(
                                aws_sdk_iam::types::Tag::builder()
                                    .key("compat")
                                    .value("scenario")
                                    .build()
                                    .map_err(|err| scenario::build_error("Tags[0]", err))?
                            )
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::list_contains(
                    Some(Call {
                        op: "ListRoleTags",
                        params: scenario::map(vec![
                            ("RoleName", scenario::context("role.name")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_role_tags()
                                        .role_name(b.string("RoleName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.Tags",
                    vec![
                        scenario::where_entry("$.Key", scenario::lit(::serde_json::json!("compat"))),
                        scenario::where_entry("$.Value", scenario::lit(::serde_json::json!("scenario"))),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_gen_role_list_role_tags(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "ListRoleTags",
            params: scenario::map(vec![
                ("RoleName", scenario::context("role.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_role_tags()
                            .role_name(b.string("RoleName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.Tags",
                vec![
                    scenario::where_entry("$.Key", scenario::lit(::serde_json::json!("compat"))),
                    scenario::where_entry("$.Value", scenario::lit(::serde_json::json!("scenario"))),
                ],
            ),
        ],
    }
}

fn test_iam_gen_role_untag_role(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "UntagRole",
            params: scenario::map(vec![
                ("RoleName", scenario::context("role.name")),
                ("TagKeys", scenario::lit(::serde_json::json!(["compat"]))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .untag_role()
                            .role_name(b.string("RoleName")?)
                            .tag_keys("compat")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::absent_from_list(
                    Some(Call {
                        op: "ListRoleTags",
                        params: scenario::map(vec![
                            ("RoleName", scenario::context("role.name")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_role_tags()
                                        .role_name(b.string("RoleName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.Tags",
                    vec![
                        scenario::where_entry("$.Key", scenario::lit(::serde_json::json!("compat"))),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_gen_role_update_assume_role_policy(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "UpdateAssumeRolePolicy",
            params: scenario::map(vec![
                ("PolicyDocument", scenario::lit(::serde_json::json!("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Principal\":{\"Service\":\"lambda.amazonaws.com\"},\"Action\":\"sts:AssumeRole\"}]}"))),
                ("RoleName", scenario::context("role.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .update_assume_role_policy()
                            .policy_document("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Principal\":{\"Service\":\"lambda.amazonaws.com\"},\"Action\":\"sts:AssumeRole\"}]}")
                            .role_name(b.string("RoleName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::readback(
                    Call {
                        op: "GetRole",
                        params: scenario::map(vec![
                            ("RoleName", scenario::context("role.name")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .get_role()
                                        .role_name(b.string("RoleName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::equals("$.Role.Arn", scenario::context("role.arn")),
                        scenario::non_empty("$.Role.AssumeRolePolicyDocument"),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_gen_role_put_role_policy(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "PutRolePolicy",
            params: scenario::map(vec![
                ("PolicyDocument", scenario::lit(::serde_json::json!("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Action\":\"s3:GetObject\",\"Resource\":\"arn:aws:s3:::compat-scenario-nonexistent/*\"}]}"))),
                ("PolicyName", scenario::name("inline")),
                ("RoleName", scenario::context("role.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .put_role_policy()
                            .policy_document("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Action\":\"s3:GetObject\",\"Resource\":\"arn:aws:s3:::compat-scenario-nonexistent/*\"}]}")
                            .policy_name(b.string("PolicyName")?)
                            .role_name(b.string("RoleName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::list_contains(
                    Some(Call {
                        op: "ListRolePolicies",
                        params: scenario::map(vec![
                            ("RoleName", scenario::context("role.name")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_role_policies()
                                        .role_name(b.string("RoleName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.PolicyNames",
                    vec![
                        scenario::where_entry("$", scenario::name("inline")),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_gen_role_get_role_policy(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "GetRolePolicy",
            params: scenario::map(vec![
                ("PolicyName", scenario::name("inline")),
                ("RoleName", scenario::context("role.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_role_policy()
                            .policy_name(b.string("PolicyName")?)
                            .role_name(b.string("RoleName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.PolicyName", scenario::name("inline")),
                scenario::equals("$.RoleName", scenario::context("role.name")),
            ]),
        ],
    }
}

fn test_iam_gen_role_list_role_policies(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "ListRolePolicies",
            params: scenario::map(vec![
                ("RoleName", scenario::context("role.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_role_policies()
                            .role_name(b.string("RoleName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.PolicyNames",
                vec![
                    scenario::where_entry("$", scenario::name("inline")),
                ],
            ),
        ],
    }
}

fn test_iam_gen_role_simulate_principal_policy(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "SimulatePrincipalPolicy",
            params: scenario::map(vec![
                ("ActionNames", scenario::lit(::serde_json::json!(["s3:GetObject"]))),
                ("PolicySourceArn", scenario::context("role.arn")),
                ("ResourceArns", scenario::lit(::serde_json::json!(["arn:aws:s3:::compat-scenario-nonexistent/report.csv"]))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .simulate_principal_policy()
                            .action_names("s3:GetObject")
                            .policy_source_arn(b.string("PolicySourceArn")?)
                            .resource_arns("arn:aws:s3:::compat-scenario-nonexistent/report.csv")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.EvaluationResults[0].EvalActionName", scenario::lit(::serde_json::json!("s3:GetObject"))),
                scenario::equals("$.EvaluationResults[0].EvalDecision", scenario::lit(::serde_json::json!("allowed"))),
            ]),
        ],
    }
}

fn test_iam_gen_role_delete_role_policy(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "DeleteRolePolicy",
            params: scenario::map(vec![
                ("PolicyName", scenario::name("inline")),
                ("RoleName", scenario::context("role.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_role_policy()
                            .policy_name(b.string("PolicyName")?)
                            .role_name(b.string("RoleName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::absent_from_list(
                    Some(Call {
                        op: "ListRolePolicies",
                        params: scenario::map(vec![
                            ("RoleName", scenario::context("role.name")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_role_policies()
                                        .role_name(b.string("RoleName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.PolicyNames",
                    vec![
                        scenario::where_entry("$", scenario::name("inline")),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_gen_role_attach_role_policy(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "AttachRolePolicy",
            params: scenario::map(vec![
                ("PolicyArn", scenario::context("policy.arn")),
                ("RoleName", scenario::context("role.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .attach_role_policy()
                            .policy_arn(b.string("PolicyArn")?)
                            .role_name(b.string("RoleName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::list_contains(
                    Some(Call {
                        op: "ListAttachedRolePolicies",
                        params: scenario::map(vec![
                            ("RoleName", scenario::context("role.name")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_attached_role_policies()
                                        .role_name(b.string("RoleName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.AttachedPolicies",
                    vec![
                        scenario::where_entry("$.PolicyArn", scenario::context("policy.arn")),
                        scenario::where_entry("$.PolicyName", scenario::context("policy.name")),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_gen_role_list_attached_role_policies(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "ListAttachedRolePolicies",
            params: scenario::map(vec![
                ("RoleName", scenario::context("role.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_attached_role_policies()
                            .role_name(b.string("RoleName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.AttachedPolicies",
                vec![
                    scenario::where_entry("$.PolicyArn", scenario::context("policy.arn")),
                    scenario::where_entry("$.PolicyName", scenario::context("policy.name")),
                ],
            ),
        ],
    }
}

fn test_iam_gen_role_detach_role_policy(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "DetachRolePolicy",
            params: scenario::map(vec![
                ("PolicyArn", scenario::context("policy.arn")),
                ("RoleName", scenario::context("role.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .detach_role_policy()
                            .policy_arn(b.string("PolicyArn")?)
                            .role_name(b.string("RoleName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::absent_from_list(
                    Some(Call {
                        op: "ListAttachedRolePolicies",
                        params: scenario::map(vec![
                            ("RoleName", scenario::context("role.name")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_attached_role_policies()
                                        .role_name(b.string("RoleName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.AttachedPolicies",
                    vec![
                        scenario::where_entry("$.PolicyArn", scenario::context("policy.arn")),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_gen_role_put_role_permissions_boundary(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "PutRolePermissionsBoundary",
            params: scenario::map(vec![
                ("PermissionsBoundary", scenario::context("policy.arn")),
                ("RoleName", scenario::context("role.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .put_role_permissions_boundary()
                            .permissions_boundary(b.string("PermissionsBoundary")?)
                            .role_name(b.string("RoleName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::readback(
                    Call {
                        op: "GetRole",
                        params: scenario::map(vec![
                            ("RoleName", scenario::context("role.name")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .get_role()
                                        .role_name(b.string("RoleName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::equals("$.Role.PermissionsBoundary.PermissionsBoundaryArn", scenario::context("policy.arn")),
                        scenario::equals("$.Role.PermissionsBoundary.PermissionsBoundaryType", scenario::lit(::serde_json::json!("PermissionsBoundaryPolicy"))),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_gen_role_delete_role_permissions_boundary(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "DeleteRolePermissionsBoundary",
            params: scenario::map(vec![
                ("RoleName", scenario::context("role.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_role_permissions_boundary()
                            .role_name(b.string("RoleName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::readback(
                    Call {
                        op: "GetRole",
                        params: scenario::map(vec![
                            ("RoleName", scenario::context("role.name")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .get_role()
                                        .role_name(b.string("RoleName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::missing("$.Role.PermissionsBoundary"),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_gen_role_get_account_authorization_details(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "GetAccountAuthorizationDetails",
            params: scenario::lit(::serde_json::json!({})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_account_authorization_details()
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.RoleDetailList",
                vec![
                    scenario::where_entry("$.Arn", scenario::context("role.arn")),
                ],
            ),
        ],
    }
}

fn test_iam_gen_role_list_roles(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "ListRoles",
            params: scenario::lit(::serde_json::json!({})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_roles()
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.Roles",
                vec![
                    scenario::where_entry("$.Arn", scenario::context("role.arn")),
                ],
            ),
        ],
    }
}

fn test_iam_gen_role_delete_role(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "DeleteRole",
            params: scenario::map(vec![
                ("RoleName", scenario::context("role.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_role()
                            .role_name(b.string("RoleName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::absent_by_error(
                    Call {
                        op: "GetRole",
                        params: scenario::map(vec![
                            ("RoleName", scenario::context("role.name")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .get_role()
                                        .role_name(b.string("RoleName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    scenario::error("NoSuchEntityException", "NoSuchEntity"),
                ),
            ),
        ],
    }
}

fn setup_iam_gen_instanceprofile(client: &aws_sdk_iam::Client) -> Vec<Call> {
    vec![
        Call {
            op: "CreatePolicy",
            params: scenario::map(vec![
                ("Description", scenario::lit(::serde_json::json!("compat scenario managed policy"))),
                ("PolicyDocument", scenario::lit(::serde_json::json!("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Action\":\"s3:GetObject\",\"Resource\":\"arn:aws:s3:::compat-scenario-nonexistent/*\"}]}"))),
                ("PolicyName", scenario::name("p")),
            ]),
            export: vec![
                ("policy.arn", "$.Policy.Arn"),
                ("policy.name", "$.Policy.PolicyName"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_policy()
                            .description("compat scenario managed policy")
                            .policy_document("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Action\":\"s3:GetObject\",\"Resource\":\"arn:aws:s3:::compat-scenario-nonexistent/*\"}]}")
                            .policy_name(b.string("PolicyName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "CreateRole",
            params: scenario::map(vec![
                ("AssumeRolePolicyDocument", scenario::lit(::serde_json::json!("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Principal\":{\"Service\":\"ec2.amazonaws.com\"},\"Action\":\"sts:AssumeRole\"}]}"))),
                ("Description", scenario::lit(::serde_json::json!("compat scenario role, initial description"))),
                ("RoleName", scenario::name("r")),
            ]),
            export: vec![
                ("role.arn", "$.Role.Arn"),
                ("role.name", "$.Role.RoleName"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_role()
                            .assume_role_policy_document("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Principal\":{\"Service\":\"ec2.amazonaws.com\"},\"Action\":\"sts:AssumeRole\"}]}")
                            .description("compat scenario role, initial description")
                            .role_name(b.string("RoleName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn teardown_iam_gen_instanceprofile(client: &aws_sdk_iam::Client) -> Vec<Call> {
    vec![
        Call {
            op: "DeleteInstanceProfile",
            params: scenario::map(vec![
                ("InstanceProfileName", scenario::context("instanceprofile.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_instance_profile()
                            .instance_profile_name(b.string("InstanceProfileName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "DeleteRole",
            params: scenario::map(vec![
                ("RoleName", scenario::context("role.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_role()
                            .role_name(b.string("RoleName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        Call {
            op: "DeletePolicy",
            params: scenario::map(vec![
                ("PolicyArn", scenario::context("policy.arn")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_policy()
                            .policy_arn(b.string("PolicyArn")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
    ]
}

fn test_iam_gen_instanceprofile_create_instance_profile(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "CreateInstanceProfile",
            params: scenario::map(vec![
                ("InstanceProfileName", scenario::name("ip")),
            ]),
            export: vec![
                ("instanceprofile.arn", "$.InstanceProfile.Arn"),
                ("instanceprofile.name", "$.InstanceProfile.InstanceProfileName"),
            ],
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .create_instance_profile()
                            .instance_profile_name(b.string("InstanceProfileName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.InstanceProfile.Arn"),
                scenario::matches("$.InstanceProfile.InstanceProfileName", "^[\\w+=,.@-]+$"),
            ]),
            scenario::eventually(6, 500,
                scenario::readback(
                    Call {
                        op: "GetInstanceProfile",
                        params: scenario::map(vec![
                            ("InstanceProfileName", scenario::context("instanceprofile.name")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .get_instance_profile()
                                        .instance_profile_name(b.string("InstanceProfileName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::equals("$.InstanceProfile.Arn", scenario::context("instanceprofile.arn")),
                    ],
                ),
            ),
            scenario::eventually(6, 500,
                scenario::list_contains(
                    Some(Call {
                        op: "ListInstanceProfiles",
                        params: scenario::lit(::serde_json::json!({})),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |_b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_instance_profiles()
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.InstanceProfiles",
                    vec![
                        scenario::where_entry("$.Arn", scenario::context("instanceprofile.arn")),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_gen_instanceprofile_get_instance_profile(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "GetInstanceProfile",
            params: scenario::map(vec![
                ("InstanceProfileName", scenario::context("instanceprofile.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_instance_profile()
                            .instance_profile_name(b.string("InstanceProfileName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::equals("$.InstanceProfile.Arn", scenario::context("instanceprofile.arn")),
            ]),
        ],
    }
}

fn test_iam_gen_instanceprofile_tag_instance_profile(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "TagInstanceProfile",
            params: scenario::map(vec![
                ("InstanceProfileName", scenario::context("instanceprofile.name")),
                ("Tags", scenario::lit(::serde_json::json!([{"Key": "compat", "Value": "scenario"}]))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .tag_instance_profile()
                            .instance_profile_name(b.string("InstanceProfileName")?)
                            .tags(
                                aws_sdk_iam::types::Tag::builder()
                                    .key("compat")
                                    .value("scenario")
                                    .build()
                                    .map_err(|err| scenario::build_error("Tags[0]", err))?
                            )
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::list_contains(
                    Some(Call {
                        op: "ListInstanceProfileTags",
                        params: scenario::map(vec![
                            ("InstanceProfileName", scenario::context("instanceprofile.name")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_instance_profile_tags()
                                        .instance_profile_name(b.string("InstanceProfileName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.Tags",
                    vec![
                        scenario::where_entry("$.Key", scenario::lit(::serde_json::json!("compat"))),
                        scenario::where_entry("$.Value", scenario::lit(::serde_json::json!("scenario"))),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_gen_instanceprofile_list_instance_profile_tags(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "ListInstanceProfileTags",
            params: scenario::map(vec![
                ("InstanceProfileName", scenario::context("instanceprofile.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_instance_profile_tags()
                            .instance_profile_name(b.string("InstanceProfileName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.Tags",
                vec![
                    scenario::where_entry("$.Key", scenario::lit(::serde_json::json!("compat"))),
                    scenario::where_entry("$.Value", scenario::lit(::serde_json::json!("scenario"))),
                ],
            ),
        ],
    }
}

fn test_iam_gen_instanceprofile_untag_instance_profile(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "UntagInstanceProfile",
            params: scenario::map(vec![
                ("InstanceProfileName", scenario::context("instanceprofile.name")),
                ("TagKeys", scenario::lit(::serde_json::json!(["compat"]))),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .untag_instance_profile()
                            .instance_profile_name(b.string("InstanceProfileName")?)
                            .tag_keys("compat")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::absent_from_list(
                    Some(Call {
                        op: "ListInstanceProfileTags",
                        params: scenario::map(vec![
                            ("InstanceProfileName", scenario::context("instanceprofile.name")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .list_instance_profile_tags()
                                        .instance_profile_name(b.string("InstanceProfileName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.Tags",
                    vec![
                        scenario::where_entry("$.Key", scenario::lit(::serde_json::json!("compat"))),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_gen_instanceprofile_add_role_to_instance_profile(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "AddRoleToInstanceProfile",
            params: scenario::map(vec![
                ("InstanceProfileName", scenario::context("instanceprofile.name")),
                ("RoleName", scenario::context("role.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .add_role_to_instance_profile()
                            .instance_profile_name(b.string("InstanceProfileName")?)
                            .role_name(b.string("RoleName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::readback(
                    Call {
                        op: "GetInstanceProfile",
                        params: scenario::map(vec![
                            ("InstanceProfileName", scenario::context("instanceprofile.name")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .get_instance_profile()
                                        .instance_profile_name(b.string("InstanceProfileName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    vec![
                        scenario::equals("$.InstanceProfile.Roles[0].Arn", scenario::context("role.arn")),
                        scenario::equals("$.InstanceProfile.Roles[0].RoleName", scenario::context("role.name")),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_gen_instanceprofile_list_instance_profiles_for_role(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "ListInstanceProfilesForRole",
            params: scenario::map(vec![
                ("RoleName", scenario::context("role.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_instance_profiles_for_role()
                            .role_name(b.string("RoleName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.InstanceProfiles",
                vec![
                    scenario::where_entry("$.Arn", scenario::context("instanceprofile.arn")),
                ],
            ),
        ],
    }
}

fn test_iam_gen_instanceprofile_remove_role_from_instance_profile(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "RemoveRoleFromInstanceProfile",
            params: scenario::map(vec![
                ("InstanceProfileName", scenario::context("instanceprofile.name")),
                ("RoleName", scenario::context("role.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .remove_role_from_instance_profile()
                            .instance_profile_name(b.string("InstanceProfileName")?)
                            .role_name(b.string("RoleName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::absent_from_list(
                    Some(Call {
                        op: "GetInstanceProfile",
                        params: scenario::map(vec![
                            ("InstanceProfileName", scenario::context("instanceprofile.name")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .get_instance_profile()
                                        .instance_profile_name(b.string("InstanceProfileName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    }),
                    "$.InstanceProfile.Roles",
                    vec![
                        scenario::where_entry("$.RoleName", scenario::context("role.name")),
                    ],
                ),
            ),
        ],
    }
}

fn test_iam_gen_instanceprofile_list_instance_profiles(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "ListInstanceProfiles",
            params: scenario::lit(::serde_json::json!({})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_instance_profiles()
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::list_contains(
                None,
                "$.InstanceProfiles",
                vec![
                    scenario::where_entry("$.Arn", scenario::context("instanceprofile.arn")),
                ],
            ),
        ],
    }
}

fn test_iam_gen_instanceprofile_delete_instance_profile(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "DeleteInstanceProfile",
            params: scenario::map(vec![
                ("InstanceProfileName", scenario::context("instanceprofile.name")),
            ]),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .delete_instance_profile()
                            .instance_profile_name(b.string("InstanceProfileName")?)
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::eventually(6, 500,
                scenario::absent_by_error(
                    Call {
                        op: "GetInstanceProfile",
                        params: scenario::map(vec![
                            ("InstanceProfileName", scenario::context("instanceprofile.name")),
                        ]),
                        export: Vec::new(),
                        invoke: {
                            let client = client.clone();
                            scenario::invoker(move |b| {
                                let client = client.clone();
                                Box::pin(async move {
                                    let capture = scenario::Capture::new();
                                    let request = client
                                        .get_instance_profile()
                                        .instance_profile_name(b.string("InstanceProfileName")?)
                                        .customize()
                                        .interceptor(capture.clone());
                                    Ok(scenario::observe(request.send().await, &capture))
                                })
                            })
                        },
                    },
                    scenario::error("NoSuchEntityException", "NoSuchEntity"),
                ),
            ),
        ],
    }
}

fn setup_iam_gen_probe(_client: &aws_sdk_iam::Client) -> Vec<Call> {
    // An empty phase is a no-op, not a missing one.
    Vec::new()
}

fn teardown_iam_gen_probe(_client: &aws_sdk_iam::Client) -> Vec<Call> {
    // An empty phase is a no-op, not a missing one.
    Vec::new()
}

fn test_iam_gen_probe_get_access_key_last_used(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "GetAccessKeyLastUsed",
            params: scenario::lit(::serde_json::json!({"AccessKeyId": "AKIAIOSFODNN7EXAMPLE"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_access_key_last_used()
                            .access_key_id("AKIAIOSFODNN7EXAMPLE")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.UserName"),
            ]),
        ],
    }
}

fn test_iam_gen_probe_get_account_password_policy(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "GetAccountPasswordPolicy",
            params: scenario::lit(::serde_json::json!({})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_account_password_policy()
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.PasswordPolicy"),
            ]),
        ],
    }
}

fn test_iam_gen_probe_get_account_summary(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "GetAccountSummary",
            params: scenario::lit(::serde_json::json!({})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_account_summary()
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.SummaryMap"),
            ]),
        ],
    }
}

fn test_iam_gen_probe_get_context_keys_for_custom_policy(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "GetContextKeysForCustomPolicy",
            params: scenario::lit(::serde_json::json!({"PolicyInputList": ["{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Action\":\"s3:GetObject\",\"Resource\":\"arn:aws:s3:::compat-scenario-nonexistent/*\"}]}"]})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_context_keys_for_custom_policy()
                            .policy_input_list("{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Action\":\"s3:GetObject\",\"Resource\":\"arn:aws:s3:::compat-scenario-nonexistent/*\"}]}")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::is_list("$.ContextKeyNames"),
            ]),
        ],
    }
}

fn test_iam_gen_probe_get_context_keys_for_principal_policy(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "GetContextKeysForPrincipalPolicy",
            params: scenario::lit(::serde_json::json!({"PolicySourceArn": "arn:aws:iam::123456789012:user/compat-scenario-nonexistent"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_context_keys_for_principal_policy()
                            .policy_source_arn("arn:aws:iam::123456789012:user/compat-scenario-nonexistent")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::is_list("$.ContextKeyNames"),
            ]),
        ],
    }
}

fn test_iam_gen_probe_get_credential_report(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "GetCredentialReport",
            params: scenario::lit(::serde_json::json!({})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_credential_report()
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.Content"),
            ]),
        ],
    }
}

fn test_iam_gen_probe_get_delegation_request(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "GetDelegationRequest",
            params: scenario::lit(::serde_json::json!({"DelegationRequestId": "compat-scenario-nonexistent-delegation"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_delegation_request()
                            .delegation_request_id("compat-scenario-nonexistent-delegation")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.PermissionCheckStatus"),
            ]),
        ],
    }
}

fn test_iam_gen_probe_get_human_readable_summary(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "GetHumanReadableSummary",
            params: scenario::lit(::serde_json::json!({"EntityArn": "arn:aws:iam::123456789012:role/compat-scenario-nonexistent"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_human_readable_summary()
                            .entity_arn("arn:aws:iam::123456789012:role/compat-scenario-nonexistent")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.SummaryState"),
            ]),
        ],
    }
}

fn test_iam_gen_probe_get_login_profile(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "GetLoginProfile",
            params: scenario::lit(::serde_json::json!({})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_login_profile()
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.LoginProfile"),
            ]),
        ],
    }
}

fn test_iam_gen_probe_get_mfa_device(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "GetMFADevice",
            params: scenario::lit(::serde_json::json!({"SerialNumber": "arn:aws:iam::123456789012:mfa/compat-scenario-nonexistent"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_mfa_device()
                            .serial_number("arn:aws:iam::123456789012:mfa/compat-scenario-nonexistent")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.UserName"),
            ]),
        ],
    }
}

fn test_iam_gen_probe_get_open_id_connect_provider(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "GetOpenIDConnectProvider",
            params: scenario::lit(::serde_json::json!({"OpenIDConnectProviderArn": "arn:aws:iam::123456789012:oidc-provider/compat-scenario-nonexistent.example.com"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_open_id_connect_provider()
                            .open_id_connect_provider_arn("arn:aws:iam::123456789012:oidc-provider/compat-scenario-nonexistent.example.com")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.Url"),
            ]),
        ],
    }
}

fn test_iam_gen_probe_get_organizations_access_report(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "GetOrganizationsAccessReport",
            params: scenario::lit(::serde_json::json!({"JobId": "00000000-0000-0000-0000-000000000000"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_organizations_access_report()
                            .job_id("00000000-0000-0000-0000-000000000000")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.JobStatus"),
            ]),
        ],
    }
}

fn test_iam_gen_probe_get_outbound_web_identity_federation_info(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "GetOutboundWebIdentityFederationInfo",
            params: scenario::lit(::serde_json::json!({})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_outbound_web_identity_federation_info()
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.IssuerIdentifier"),
            ]),
        ],
    }
}

fn test_iam_gen_probe_get_policy_version(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "GetPolicyVersion",
            params: scenario::lit(::serde_json::json!({"PolicyArn": "arn:aws:iam::123456789012:policy/compat-scenario-nonexistent", "VersionId": "v1"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_policy_version()
                            .policy_arn("arn:aws:iam::123456789012:policy/compat-scenario-nonexistent")
                            .version_id("v1")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.PolicyVersion"),
            ]),
        ],
    }
}

fn test_iam_gen_probe_get_saml_provider(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "GetSAMLProvider",
            params: scenario::lit(::serde_json::json!({"SAMLProviderArn": "arn:aws:iam::123456789012:saml-provider/compat-scenario-nonexistent"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_saml_provider()
                            .saml_provider_arn("arn:aws:iam::123456789012:saml-provider/compat-scenario-nonexistent")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.AssertionEncryptionMode"),
            ]),
        ],
    }
}

fn test_iam_gen_probe_get_ssh_public_key(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "GetSSHPublicKey",
            params: scenario::lit(::serde_json::json!({"Encoding": "PEM", "SSHPublicKeyId": "APKAEIBAERJR2EXAMPLE", "UserName": "compat-scenario-nonexistent"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_ssh_public_key()
                            .encoding(aws_sdk_iam::types::EncodingType::from("PEM"))
                            .ssh_public_key_id("APKAEIBAERJR2EXAMPLE")
                            .user_name("compat-scenario-nonexistent")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.SSHPublicKey"),
            ]),
        ],
    }
}

fn test_iam_gen_probe_get_server_certificate(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "GetServerCertificate",
            params: scenario::lit(::serde_json::json!({"ServerCertificateName": "compat-scenario-nonexistent"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_server_certificate()
                            .server_certificate_name("compat-scenario-nonexistent")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.ServerCertificate"),
            ]),
        ],
    }
}

fn test_iam_gen_probe_get_service_last_accessed_details(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "GetServiceLastAccessedDetails",
            params: scenario::lit(::serde_json::json!({"JobId": "00000000-0000-0000-0000-000000000000"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_service_last_accessed_details()
                            .job_id("00000000-0000-0000-0000-000000000000")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.JobStatus"),
            ]),
        ],
    }
}

fn test_iam_gen_probe_get_service_last_accessed_details_with_entities(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "GetServiceLastAccessedDetailsWithEntities",
            params: scenario::lit(::serde_json::json!({"JobId": "00000000-0000-0000-0000-000000000000", "ServiceNamespace": "s3"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_service_last_accessed_details_with_entities()
                            .job_id("00000000-0000-0000-0000-000000000000")
                            .service_namespace("s3")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.JobStatus"),
            ]),
        ],
    }
}

fn test_iam_gen_probe_get_service_linked_role_deletion_status(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "GetServiceLinkedRoleDeletionStatus",
            params: scenario::lit(::serde_json::json!({"DeletionTaskId": "task/aws-service-role/compat-scenario-nonexistent.amazonaws.com/AWSServiceRoleForCompatScenario/00000000-0000-0000-0000-000000000000"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .get_service_linked_role_deletion_status()
                            .deletion_task_id("task/aws-service-role/compat-scenario-nonexistent.amazonaws.com/AWSServiceRoleForCompatScenario/00000000-0000-0000-0000-000000000000")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.Status"),
            ]),
        ],
    }
}

fn test_iam_gen_probe_list_account_aliases(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "ListAccountAliases",
            params: scenario::lit(::serde_json::json!({})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_account_aliases()
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.IsTruncated"),
            ]),
        ],
    }
}

fn test_iam_gen_probe_list_delegation_requests(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "ListDelegationRequests",
            params: scenario::lit(::serde_json::json!({})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_delegation_requests()
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.isTruncated"),
            ]),
        ],
    }
}

fn test_iam_gen_probe_list_entities_for_policy(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "ListEntitiesForPolicy",
            params: scenario::lit(::serde_json::json!({"PolicyArn": "arn:aws:iam::123456789012:policy/compat-scenario-nonexistent"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_entities_for_policy()
                            .policy_arn("arn:aws:iam::123456789012:policy/compat-scenario-nonexistent")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.IsTruncated"),
            ]),
        ],
    }
}

fn test_iam_gen_probe_list_mfa_device_tags(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "ListMFADeviceTags",
            params: scenario::lit(::serde_json::json!({"SerialNumber": "arn:aws:iam::123456789012:mfa/compat-scenario-nonexistent"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_mfa_device_tags()
                            .serial_number("arn:aws:iam::123456789012:mfa/compat-scenario-nonexistent")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.IsTruncated"),
            ]),
        ],
    }
}

fn test_iam_gen_probe_list_mfa_devices(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "ListMFADevices",
            params: scenario::lit(::serde_json::json!({})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_mfa_devices()
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.IsTruncated"),
            ]),
        ],
    }
}

fn test_iam_gen_probe_list_open_id_connect_provider_tags(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "ListOpenIDConnectProviderTags",
            params: scenario::lit(::serde_json::json!({"OpenIDConnectProviderArn": "arn:aws:iam::123456789012:oidc-provider/compat-scenario-nonexistent.example.com"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_open_id_connect_provider_tags()
                            .open_id_connect_provider_arn("arn:aws:iam::123456789012:oidc-provider/compat-scenario-nonexistent.example.com")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.IsTruncated"),
            ]),
        ],
    }
}

fn test_iam_gen_probe_list_open_id_connect_providers(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "ListOpenIDConnectProviders",
            params: scenario::lit(::serde_json::json!({})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_open_id_connect_providers()
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::is_list("$.OpenIDConnectProviderList"),
            ]),
        ],
    }
}

fn test_iam_gen_probe_list_organizations_features(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "ListOrganizationsFeatures",
            params: scenario::lit(::serde_json::json!({})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_organizations_features()
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.OrganizationId"),
            ]),
        ],
    }
}

fn test_iam_gen_probe_list_policies_granting_service_access(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "ListPoliciesGrantingServiceAccess",
            params: scenario::lit(::serde_json::json!({"Arn": "arn:aws:iam::123456789012:user/compat-scenario-nonexistent", "ServiceNamespaces": ["s3"]})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_policies_granting_service_access()
                            .arn("arn:aws:iam::123456789012:user/compat-scenario-nonexistent")
                            .service_namespaces("s3")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.IsTruncated"),
            ]),
        ],
    }
}

fn test_iam_gen_probe_list_policy_versions(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "ListPolicyVersions",
            params: scenario::lit(::serde_json::json!({"PolicyArn": "arn:aws:iam::123456789012:policy/compat-scenario-nonexistent"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_policy_versions()
                            .policy_arn("arn:aws:iam::123456789012:policy/compat-scenario-nonexistent")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.IsTruncated"),
            ]),
        ],
    }
}

fn test_iam_gen_probe_list_saml_provider_tags(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "ListSAMLProviderTags",
            params: scenario::lit(::serde_json::json!({"SAMLProviderArn": "arn:aws:iam::123456789012:saml-provider/compat-scenario-nonexistent"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_saml_provider_tags()
                            .saml_provider_arn("arn:aws:iam::123456789012:saml-provider/compat-scenario-nonexistent")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.IsTruncated"),
            ]),
        ],
    }
}

fn test_iam_gen_probe_list_saml_providers(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "ListSAMLProviders",
            params: scenario::lit(::serde_json::json!({})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_saml_providers()
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::is_list("$.SAMLProviderList"),
            ]),
        ],
    }
}

fn test_iam_gen_probe_list_ssh_public_keys(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "ListSSHPublicKeys",
            params: scenario::lit(::serde_json::json!({})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_ssh_public_keys()
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.IsTruncated"),
            ]),
        ],
    }
}

fn test_iam_gen_probe_list_server_certificate_tags(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "ListServerCertificateTags",
            params: scenario::lit(::serde_json::json!({"ServerCertificateName": "compat-scenario-nonexistent"})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_server_certificate_tags()
                            .server_certificate_name("compat-scenario-nonexistent")
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.IsTruncated"),
            ]),
        ],
    }
}

fn test_iam_gen_probe_list_server_certificates(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "ListServerCertificates",
            params: scenario::lit(::serde_json::json!({})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_server_certificates()
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.IsTruncated"),
            ]),
        ],
    }
}

fn test_iam_gen_probe_list_service_specific_credentials(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "ListServiceSpecificCredentials",
            params: scenario::lit(::serde_json::json!({})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_service_specific_credentials()
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.IsTruncated"),
            ]),
        ],
    }
}

fn test_iam_gen_probe_list_signing_certificates(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "ListSigningCertificates",
            params: scenario::lit(::serde_json::json!({})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_signing_certificates()
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.IsTruncated"),
            ]),
        ],
    }
}

fn test_iam_gen_probe_list_virtual_mfa_devices(client: &aws_sdk_iam::Client) -> Test {
    Test {
        call: Call {
            op: "ListVirtualMFADevices",
            params: scenario::lit(::serde_json::json!({})),
            export: Vec::new(),
            invoke: {
                let client = client.clone();
                scenario::invoker(move |_b| {
                    let client = client.clone();
                    Box::pin(async move {
                        let capture = scenario::Capture::new();
                        let request = client
                            .list_virtual_mfa_devices()
                            .customize()
                            .interceptor(capture.clone());
                        Ok(scenario::observe(request.send().await, &capture))
                    })
                })
            },
        },
        assert: vec![
            scenario::response_field(vec![
                scenario::non_empty("$.IsTruncated"),
            ]),
        ],
    }
}
