version: 1
reference_policy: aws_docs_only_unless_user_approves_real_aws
status_values:
  - not_started
  - docs_reviewed
  - tests_missing
  - tests_added
  - implementation_mismatch
  - fixed
  - reviewed
  - blocked
  - intentionally_partial
  - unsupported
services:
  acm: {status: fixed, priority: p3, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/acm.yaml, last_updated: 2026-09-20, next_action: "The three #1994 divergences are fixed: DomainName/SAN/ValidationMethod are validated against the modeled constraints, DescribeCertificate returns DomainValidationOptions (stable synthetic CNAME for DNS), and ListCertificates filters on CertificateStatuses. Next: ListCertificates pagination (MaxItems/NextToken) and the CloudFormation handler's dropped ValidationMethod property — neither has an issue."}
  apigateway: {status: in_progress, priority: p0, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/apigateway.yaml, last_updated: 2026-05-19, next_action: Continue ExecuteRestAPI Lambda proxy request event edge cases, then review HTTP API route matching, authorizers, and CDK resources.}
  appconfig: {status: reviewed, priority: p3, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/appconfig.yaml, last_updated: 2026-09-20, next_action: "Enforce VersionLabel/Description/ContentType shape validation (length and VersionLabel's non-numeric-character pattern) across create/update operations; none of it is currently checked."}
  appconfigdata: {status: reviewed, priority: p3, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/appconfigdata.yaml, last_updated: 2026-09-20, next_action: "Both operations (StartConfigurationSession, GetLatestConfiguration) are reviewed against AWS docs (#59): token rotation, 24-hour expiry, unchanged-version empty body, and RequiredMinimumPollIntervalInSeconds enforcement all match documented behavior. Remaining: ResourceNotFoundException's ReferencedBy/ResourceType structured members are not populated (message-only)."}
  appregistry: {status: reviewed, priority: p3, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/appregistry.yaml, last_updated: 2026-09-20, next_action: None — all 22 tracked operations reviewed.}
  appsync: {status: fixed, priority: p2, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/appsync.yaml, last_updated: 2026-09-23, next_action: "#61-#65 closed: CreateGraphqlApi queryDepthLimit/resolverCountLimit boundaries and nested additionalAuthenticationProviders/logConfig shape validation are tested (#61, #65); CreateGraphqlApi no longer auto-creates a default API key, matching AWS's documented response shape (#62, breaking); the audit-by-category gap is narrowed to the one category left, OPENID_CONNECT verification, split to #2095 (#63); Merged APIs and API Cache capability notes now say what actually happens at execution time (#64). Next: #2095 (OPENID_CONNECT signature/issuer/TTL/clientId verification), then remaining typed protocol validation and data-source CloudFormation end-to-end coverage."}
  athena: {status: reviewed, priority: p2, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/athena.yaml, last_updated: 2026-09-20, next_action: "Shape review done (#66, #67) and #2009 fixed: every not-found answers 400 (awsJson1_1's default for InvalidRequestException) and StopQueryExecution is implemented, taking the count to 12. Nothing actionable outstanding; the inert query engine stays intentional (#67)."}
  autoscaling: {status: implementation_mismatch, priority: p2, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/autoscaling.yaml, last_updated: 2026-09-23, next_action: "#68 closed: the tracker said not_started/\"19 tracked, all inert\"/\"Metadata-only ... no EC2 instance management\", which has been false since #541, #1723, #1842 and #1870 — reconcile.go launches/terminates real EC2 instances to converge on DesiredCapacity, and policy.go executes SimpleScaling/StepScaling for real (25 operations, all StatusSupported). Next: #2074, DescribeLaunchConfigurations omitting UserData that CreateLaunchConfiguration stored (fix in progress in a parallel change)."}
  backup: {status: reviewed, priority: p3, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/backup.yaml, last_updated: 2026-09-20, next_action: "Zero of 18 operations are Inert (#70) — #904 wired up real restJson1 routes and #1195/#1467/#1744 added real tag and access-point storage, giving 7 Supported and 11 Partial; a Protocols row is added to the service doc (#69). Next: #479 (real jobs/recovery points/scheduling) is a separate Tier 2 promotion."}
  bedrock: {status: fixed, priority: p2, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/bedrock.yaml, last_updated: 2026-09-23, next_action: "#71 and #72 closed: InvokeModel and Converse have been served at their AWS-modeled REST bindings (POST /model/{modelId}/invoke, POST /model/{modelId}/converse, no X-Amz-Target) since #890 (2026-08-11, closing #857), replacing an emulator-invented /_bedrock prefix that made both operations unreachable from any AWS SDK; the tracker had not been updated to reflect it. No outstanding gap — InvokeModel's per-model-family payload and Converse's canned assistant text are StatusInert by design, already documented as such."}
  cloudformation: {status: implementation_mismatch, priority: p0, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/cloudformation.yaml, last_updated: 2026-09-23, next_action: "#73 closed: resourceHandlers (internal/services/cloudformation/provisioner.go) has 141 entries matching docs/cdk/resource-types.md exactly (regenerated 2026-09-23 by #2080); #540 and #2060 both closed the same day. #74 closed as intentionally_partial: unsupported-resource stubbing is already documented in resource-types.md's Stubs/Fails sections; full in-place update-rollback restoration across every resourceUpdater (not just Lambda's) is #639, open and out of scope here. Add Ref compatibility tests for IAM and EventBridge; dynamic references skip unchanged resources; Secrets Manager GenerateSecretString updates remain partial."}
  cloudfront: {status: reviewed, priority: p1, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/cloudfront.yaml, last_updated: 2026-09-20, next_action: "ProxyRequest is flagged EmulatorOnly and out of the AWS counts (#75); ETag semantics recorded as intentional and the XML shape reviewed with tests (#76). #2010 fixed all three shape divergences — the 2020-05-31 xmlns on every response root, the restXml ErrorResponse envelope, and Distribution/DistributionConfig/DistributionSummary member order. Remaining: nested Origin/DefaultCacheBehavior/CacheBehavior member order still diverges from the model, recorded with nothing pinning it."}
  cloudtrail: {status: reviewed, priority: p2, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/cloudtrail.yaml, last_updated: 2026-09-20, next_action: "docs/services/cloudtrail.md already documents the protocol (#77) and the Inert-by-design decision for all 12 operations (#78, count corrected from 9 after #814 added AddTags/RemoveTags/ListTags). Next: #478/#479/#484 track a separate Tier 1 -> Tier 2 promotion (real event-call logging) outside this tracker's scope."}
  cloudwatch: {status: reviewed, priority: p1, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/cloudwatch.yaml, last_updated: 2026-09-20, next_action: "Protocol header corrected to query/aws_json_1_0/rpcv2_cbor and 12 core operations reviewed with test evidence (#81, #82). Remaining: PutMetricData Value/Dimensions validation, GetMetricStatistics Period/Statistics validation, and DeleteAlarms unknown-alarm handling are tracked in #1720; DescribeAlarmsForMetric/EnableAlarmActions/DisableAlarmActions still need a correctness review."}
  cloudwatch-logs: {status: in_progress, priority: p0, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/cloudwatch-logs.yaml, last_updated: 2026-09-20, next_action: "PutLogEvents sequence_token behavior reviewed (#79) — AWS ignores the token entirely since 2023 and this implementation already matches, now pinned by a new test. Continue with FilterLogEvents syntax/pagination fidelity."}
  cognito: {status: in_progress, priority: p1, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/cognito.yaml, last_updated: 2026-09-20, next_action: "Tier gating (SignInPolicy/ALLOW_USER_AUTH/WebAuthn via UserPoolTier) and the SRP/WebAuthn cryptographic-verification boundary are now documented with test/code evidence (#115, #117, #118, #120, #122, #123, #124, #125, #127, #128, #129, #130, #133). Stale self-references, duplicate-issue bullets, and device-admin review items are reconciled (#83, #85, #88, #89, #91, #92, #94, #95, #99, #100, #101, #102, #104, #105, #111, #112). Remaining: AutoVerifiedAttributes/Schema persistence (#84), RespondToAuthChallenge/AdminRespondToAuthChallenge USERNAME and MFA edge cases (#86, #87, #93, #96), SignUp sub-equality and duplicate-attribute validation (#97, #98), device opt-in confirmation edge cases (#90, #103), exact device error/pagination/not-found parity across ConfirmDevice/GetDevice/ListDevices/UpdateDeviceStatus/ForgetDevice/AdminUpdateDeviceStatus/AdminForgetDevice (#106-#110, #113, #114, tracked together as #126), ListUsers filter parity (#131), SECRET_HASH aliasing (#132), admin Username validation (#134, in progress on claude/compat-cognito-tests-tier-devices-filters-secrethash), MFA config shapes (#119, #121, in progress on claude/issue-119-cognito-mfa-config-shapes), and a dedicated UpdateUserPool tier test (#116)."}
  dynamodb: {status: not_started, priority: p0, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/dynamodb.yaml, last_updated: 2026-07-16, next_action: Review expressions, pagination, GSI/LSI, and CBOR compatibility.}
  dynamodbstreams: {status: reviewed, priority: p1, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/dynamodbstreams.yaml, last_updated: 2026-09-20, next_action: "Integration coverage and JSON/CBOR target dispatch are reviewed (#137, #138): tests/integration/dynamodb/streams_test.go (18 tests) and ddb_stream_esm_test.go cover all 4 operations, and service_dispatch_test.go pins UnknownOperationException and full typed-handler coverage against the pinned models. No remaining gap."}
  ec2: {status: in_progress, priority: p1, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/ec2.yaml, last_updated: 2026-09-20, next_action: "A Describe* given an explicit id list now resolves to Invalid<Resource>ID.NotFound/.Malformed instead of filtering with it (#1708). Continue VPC lookup compatibility review with security group, ENI (#139), and Query XML fidelity gaps (#140); private subnet group metadata is now covered."}
  ecr: {status: fixed, priority: p1, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/ecr.yaml, last_updated: 2026-09-20, next_action: "Fix BatchDeleteImage to reach the registry so a Docker-backed deletion is durable (currently intentionally_partial); registry crash/reconnect recovery, DescribeImages' not-found contract, and Docker-dependent/scanner-unavailable capability annotations (#141) are covered."}
  ecs: {status: fixed, priority: p1, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/ecs.yaml, last_updated: 2026-09-20, next_action: Review ListServiceDeployments, DescribeServiceDeployments, and service revision history — a separate endpoint-sized feature; RunTask, RegisterTaskDefinition, Fargate awsvpc, and CDK asset paths are now covered.}
  eks: {status: reviewed, priority: p2, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/eks.yaml, last_updated: 2026-09-20, next_action: "Full 50-operation inventory complete (#143 + #1979): all 50 operations reviewed with test evidence for every claimed behavior. Live control-plane startup/reconnect recovery is covered."}
  elasticache: {status: fixed, priority: p2, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/elasticache.yaml, last_updated: 2026-09-20, next_action: "Classic CreateCacheCluster/CreateReplicationGroup validation, lifecycle and XML shapes are reviewed (#144): identifier grammar and lowercase storage, per-engine node counts, Memcached-only placement rules, replication-group membership, and the modelled CacheNodes/NodeGroups shapes with AWS ConfigurationEndpoint/PrimaryEndpoint split. The cluster, replication-group and parameter-group not-found faults now answer the 404 their awsQueryError traits bind (#1991). Remaining: multi-shard replication groups are not modelled, and serverless snapshot data stays metadata-only."}
  elbv2: {status: reviewed, priority: p1, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/elbv2.yaml, last_updated: 2026-09-20, next_action: "Real Host-header round-robin forwarding and ECS target registration shipped in #579/#556, so 'metadata-only, no traffic proxying' is no longer true; #1857 fixed the ten #1718 divergences (Describe*'s empty-200-instead-of-NotFound, unchecked CreateTargetGroup/CreateLoadBalancer inputs, non-AWS-shaped ARNs). Operation count corrected to 22 tracked/20 supported/2 unsupported. elb.md's naming and CreateRule/DescribeRules' 501 are confirmed intentional (#145, #146). No open next action."}
  eventbridge: {status: fixed, priority: p1, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/eventbridge.yaml, last_updated: 2026-09-23, next_action: "PutEvents fans out to Lambda, SQS, SNS, Step Functions, Kinesis, Firehose, ECS RunTask and event buses, with InputPath/InputTransformer precedence and RetryPolicy/DLQ, and PutTargets validates targets synchronously (#147, #467). CreateEventBus/DescribeEventBus carry Description, DeadLetterConfig and KmsKeyIdentifier, and PutPermission/RemovePermission store a bus's resource policy (#2076). Next: advanced event pattern operators, and target/rule/tag protocol fidelity (#148, in progress elsewhere)."}
  firehose: {status: fixed, priority: p2, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/firehose.yaml, last_updated: 2026-09-20, next_action: "All 9 operations reviewed and request validation brought in line with the model (#149): DeliveryStreamName length/pattern, Record.Data's 1,000 KiB cap, PutRecordBatch's 1-500 records and 4 MiB per call, ResourceInUseException on a duplicate name, and 400 on every exception. CloudFormation's AWS::KinesisFirehose::DeliveryStream Ref is the stream name rather than the ARN, so a deleted stack no longer leaves its stream behind. Inert delivery is recorded as intentionally partial (#150). Next: ListDeliveryStreams filtering and pagination, then name validation on the three tag operations."}
  glue: {status: in_progress, priority: p2, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/glue.yaml, last_updated: 2026-09-20, next_action: "Data Catalog shapes and unsupported families are reviewed (#152): all 11 operations are Supported and docs/services/glue.md lists every missing family. Remaining: CreateTable/GetTable discard StorageDescriptor/PartitionKeys/Parameters (#151), and GetTables/DeleteTable/TagResource/UntagResource/GetTags have no behavioral integration test."}
  iam: {status: reviewed, priority: p0, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/iam.yaml, last_updated: 2026-09-20, next_action: "Enforcement docs are reconciled and the CDK role/policy/instance-profile surface is reviewed (#154): Ref/GetAtt for all four AWS::IAM::* resource types, the DeleteRole and DeleteInstanceProfile ordering rules, and AWSs one-role-per-instance-profile quota. Remaining: Detach*/Delete*Policy and RemoveRoleFromInstanceProfile answer success where AWS answers NoSuchEntity, which needs a review of CloudFormations mutation compensation rather than a resource-type pass."}
  kinesis: {status: fixed, priority: p1, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/kinesis.yaml, last_updated: 2026-09-20, next_action: "Pagination and error shapes are reviewed (#155): ListStreams, ListShards, DescribeStream and ListTagsForStream honour their cursors, limits and truncation flags, tokens expire after 300s, every exception is a 400, and MissingParameter is gone. Remaining: reconcile the JSON 1.0/1.1 protocol docs (#156), implement ListShards ShardFilter, accept StreamARN on the operations other than ListShards, and review the record/shard-iterator path itself."}
  kms: {status: fixed, priority: p1, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/kms.yaml, last_updated: 2026-09-20, next_action: "Key material length, Decrypt/ReEncrypt key-binding (IncorrectKeyException), Encrypt's size cap, ScheduleKeyDeletion/CancelKeyDeletion ARN shape, CreateAlias validation, and GenerateRandom are reviewed and fixed (#157, #1710, #1844); JSON 1.0/1.1/RPCv2 CBOR acceptance is the framework-wide rule, documented on the service page (#158). Next: grant/policy evaluation, principal existence validation, and CloudFormation Tags/EnableKeyRotation; policy lockout safety is covered."}
  lambda: {status: fixed, priority: p0, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/lambda.yaml, last_updated: 2026-09-20, next_action: "Docker-unavailable fallback is reviewed (#162): Invoke answers 200 with X-Amz-Function-Error: Unhandled and a Runtime.DockerUnavailable body, pinned by docker_verdict_test.go. Corrected operation_count_estimate from 51 to the actual 62 tracked. Continue invocation fidelity, errors, layers, and event source mappings (#161); Parameters and Secrets extension cache ownership and TTL behavior are documented."}
  msk: {status: in_progress, priority: p3, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/msk.yaml, last_updated: 2026-09-20, next_action: "REST path/status/header fidelity and unsupported-operation tracking are reviewed (#163, #164): routing_test.go (PR #1000, 7 tests) covers percent-encoded ARNs, sub-resource absorption guards and the 501 shape; every unsupported operation carries a Notes field. Corrected operation_count_estimate from 16/13 to 17/13 (30 total). Remaining: routing_test.go does not specifically exercise UpdateBroker*/VPC-connection create-delete-describe paths."}
  opensearch: {status: reviewed, priority: p2, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/opensearch.yaml, last_updated: 2026-09-20, next_action: "All 8 operations are reviewed at their modeled restJson1 bindings, with DomainName/EngineVersion constraints and DomainStatus required members now enforced; the data plane is unsupported by design (the endpoint is a hostname, never a path). Next is the tag-ARN existence question across AddTags/ListTags/RemoveTags, which needs an approved real-AWS check, then UpdateDomainConfig."}
  organizations: {status: fixed, priority: p3, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/organizations.yaml, last_updated: 2026-09-06, next_action: "All 15 operations (DescribeOrganization plus the Tier 1 policy/OU/root/tag surface) are reviewed with MaxResults range checks and InvalidInputException.Reason wired through; next is EnablePolicyType or CreateAccount if real Organizations workflows become a target."}
  pipes: {status: not_started, priority: p2, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/pipes.yaml, last_updated: 2026-05-17, next_action: Review source/target delivery partiality and UpdatePipe docs.}
  rds: {status: in_progress, priority: p1, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/rds.yaml, last_updated: 2026-09-23, next_action: Implement cluster parameter groups so DBClusterParameterGroupName can be validated; model instance-level BackupRetentionPeriod; review the remaining optional parameters (AllocatedStorage/StorageType/EngineVersion/windows). Identifier shape and case; port; cluster retention; subnet-group existence; lifecycle and XML parity are covered. CreateDBInstance now rejects AdditionalStorageVolumes (Oracle/SQL Server only, neither emulated) and DBInstance always omits StorageOperationStatus/StorageOperationPercentProgress (#2041).}
  route53: {status: in_progress, priority: p2, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/route53.yaml, last_updated: 2026-09-20, next_action: "Protocol and pagination are reviewed (#170, #171): every Route 53 operation is REST XML (query removed from protocols), and Marker/NextMarker/IsTruncated and NextRecordName/NextRecordType/NextRecordIdentifier pagination are implemented and tested. Corrected operation_count_estimate from 10 (7/3) to the actual 25 (19/6). Per-type record value validation is implemented (#2004): A, AAAA, CNAME, NS, PTR, MX, SRV, CAA, TXT and SPF values are checked and rejected with InvalidChangeBatch, while the six unmodeled types keep the shape-only check. No remaining gap."}
  s3: {status: in_progress, priority: p0, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/s3.yaml, last_updated: 2026-09-23, next_action: "ListObjectsV2 is reviewed and the CDK bootstrap bucket flow is covered; next are ListObjects v1 per-object Owner, per-object checksums on both listings, bucket policy document validation and evaluation, PublicAccessBlock, and max-parts/max-uploads validation; follow up EventBridge notifications (#635), lifecycle transition minimum size (#636), and website routing (#637)."}
  scheduler: {status: fixed, priority: p2, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/scheduler.yaml, last_updated: 2026-09-20, next_action: "Target dispatch now fires all eight target kinds through the same dispatcher EventBridge/Pipes use, with a worker-pool delivery engine and correct UpdateSchedule replace semantics (#176, #734, PR #787, PR #816). Next: FlexibleTimeWindow jitter and duplicate-name handling (#177, in progress elsewhere)."}
  secretsmanager: {status: in_progress, priority: p1, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/secretsmanager.yaml, last_updated: 2026-09-20, next_action: "Docs drift is reviewed (#179): no duplicated hand-maintained summary exists. Deletion recovery is implemented (#178): DeleteSecret validates ForceDeleteWithoutRecovery/RecoveryWindowInDays, schedules the delete behind a 7-30 day window enforced lazily on read, and RestoreSecret cancels it. Replication stays unsupported (intentionally_partial). Remaining: CloudFormation GenerateSecretString changes do not create a new secret version (#678)."}
  ses: {status: reviewed, priority: p2, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/ses.yaml, last_updated: 2026-09-20, next_action: "#864 fixed v2 REST-JSON doc-only tracking (#180); the unsupported v1 stubs are verified to answer the awsQuery ErrorResponse 501 shape, pinned by tests (#181). Operation count 45 tracked/27 supported/18 unsupported. No open next action."}
  shield: {status: fixed, priority: p3, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/shield.yaml, last_updated: 2026-09-20, next_action: "#1983 is closed: CreateProtection refuses a duplicate ResourceArn with ResourceAlreadyExistsException, Protection carries ProtectionArn on describe and list, ListProtections filters by InclusionFilters and paginates through MaxResults/NextToken with InvalidPaginationTokenException, and DescribeProtection refuses both identifiers at once. No remaining gap; two forks are recorded in the service review."}
  sns: {status: fixed, priority: p0, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/sns.yaml, last_updated: 2026-09-20, next_action: "Protocol header corrected to [query] (#184, SNS has no JSON-typed wire protocol on AWS); async delivery and filter-policy fidelity reviewed across SQS/Lambda/email/email-json/SMS fan-out (#185). Remaining: MessageAttributes are not forwarded in the SQS/HTTP/email notification envelope; FIFO ordering/dedup/SequenceNumber remain intentionally partial."}
  sqs: {status: in_progress, priority: p0, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/sqs.yaml, last_updated: 2026-09-05, next_action: ReceiveMessage is reviewed end to end; review remaining SQS operations (SendMessage, DeleteMessage, GetQueueAttributes) for fidelity gaps.}
  ssm: {status: docs_reviewed, priority: p1, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/ssm.yaml, last_updated: 2026-07-13, next_action: Review Parameter Store semantics (advanced tiers, policies, intelligent tiering) beyond the tracked core operations.}
  stepfunctions: {status: fixed, priority: p1, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/stepfunctions.yaml, last_updated: 2026-09-20, next_action: "#2003 is closed: CreateStateMachine refuses a name outside AWS's charset with InvalidName and a roleArn that is not an IAM role ARN with InvalidArn, and every operation taking a caller-supplied stateMachineArn answers InvalidArn for a malformed one while keeping StateMachineDoesNotExist for a well-formed ARN naming nothing. Remaining: missing or empty required-member handling, and UpdateStateMachine given a qualified ARN."}
  sts: {status: docs_reviewed, priority: p0, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/sts.yaml, last_updated: 2026-07-13, next_action: Review credential response shapes (Expiration format, PackedPolicySize) against AWS docs for the supported operations.}
  transfer: {status: reviewed, priority: p3, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/transfer.yaml, last_updated: 2026-09-20, next_action: "Inert CRUD classification and protocol docs are reviewed (#195, #196): docs/services/transfer.md already documents the no-data-plane gap explicitly with a full Differences table, and StatusInert (not Supported) is the correct classification since no bytes ever move. Corrected operation_count_estimate from 10 to the actual 13 (adds the tag surface). No remaining gap."}
  waf: {status: fixed, priority: p3, claimed_by: "", claim_expires: "", service_review: docs/dev/compatibility/services/waf.yaml, last_updated: 2026-09-20, next_action: "#197 is closed: the AWSWAF_20190729. target prefix matches the model, and Scope is now validated as the CLOUDFRONT | REGIONAL enum (WAFInvalidParameterException, 400) on all four WebACL operations and both wire paths, with CLOUDFRONT round-trip and namespace-separation coverage. Remaining: a missing required Scope on ListWebACLs/DeleteWebACL, and the us-east-1 requirement for CLOUDFRONT scope."}
