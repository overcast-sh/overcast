service: athena
status: in_progress
priority: p2
reference_policy: aws_docs_only_unless_user_approves_real_aws
last_updated: 2026-09-20
protocols: [aws_json_1_1, aws_json_1_0, rpcv2_cbor]
implementation: [internal/services/athena]
integration_tests: [tests/integration/athena]
capability_docs: docs/services/athena.md
operation_count_estimate: >-
  11 tracked in internal/services/athena/capabilities_dev.go, all StatusSupported: 4 Queries
  (StartQueryExecution, GetQueryExecution, GetQueryResults, ListQueryExecutions), 4 WorkGroups
  (CreateWorkGroup, GetWorkGroup, ListWorkGroups, DeleteWorkGroup) and 3 Tags (TagResource,
  UntagResource, ListTagsForResource). docs/services/athena.md confirms "All 11 listed
  operations are implemented." (previously recorded as "8 supported", which predates
  ListQueryExecutions and the three tag operations.)
current_support_level: Basic queries and workgroups; queries immediately succeed with empty results.
operations: {}
findings:
  fixed:
    - "#66: reviewed. tests/integration/athena/athena_test.go now pins the not-found error code and
      message for GetQueryExecution, GetQueryResults, GetWorkGroup and DeleteWorkGroup, the empty
      ResultSet shape (ResultSetMetadata.ColumnInfo and Rows present, UpdateCount absent per the
      model), and StopQueryExecution's current 501. The two divergences the review found are
      tracked as #2009."
  remaining_gaps:
    - "#2009: GetQueryExecution, GetQueryResults, GetWorkGroup and DeleteWorkGroup answer
      InvalidRequestException with HTTP 404 for an unknown id or name; the pinned model gives
      InvalidRequestException no @httpError, so AWS JSON 1.1 answers 400. StopQueryExecution is
      not registered at all (501). Found by the #66 shape tests."
  intentionally_partial:
    - "#67: the inert query engine is already documented. docs/services/athena.md's subtitle
      ('no SQL is executed, so every query succeeds immediately with an empty result set'), its
      'Differences from AWS' table (Query execution: 'stored, never parsed or run'; Results:
      'Nothing is written'; Execution states: 'None of them is ever observed'; Statistics:
      'Absent'; Workgroup configuration: 'Echoed, not enforced'), and its Gotchas note ('Any
      assertion about the rows that come back needs real Athena') together describe the design
      boundary this issue asked to have recorded."
handoff:
  start_with: []
  do_not_redo: [StartQueryExecution, GetQueryExecution, GetQueryResults, ListQueryExecutions, CreateWorkGroup, GetWorkGroup, ListWorkGroups, DeleteWorkGroup, TagResource, UntagResource, ListTagsForResource]
  commands: [go test -count=1 ./internal/services/athena/... ./tests/integration/athena/...]
