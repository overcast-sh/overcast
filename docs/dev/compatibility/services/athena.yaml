service: athena
status: reviewed
priority: p2
reference_policy: aws_docs_only_unless_user_approves_real_aws
last_updated: 2026-09-25
protocols: [aws_json_1_1, aws_json_1_0, rpcv2_cbor]
implementation: [internal/services/athena]
integration_tests: [tests/integration/athena]
capability_docs: docs/services/athena.md
operation_count_estimate: >-
  36 tracked in internal/services/athena/capabilities_dev.go since #2065: queries (6, two Inert
  because no SQL runs), workgroups and engine versions (6), named queries (6), prepared
  statements (6), data catalogs and Glue metadata (9) and tags (3).
current_support_level: >-
  Control plane for SQL workgroups, saved queries and catalogs (Spark sessions, notebooks,
  calculations, capacity reservations and GetQueryRuntimeStatistics are not implemented);
  queries go through a queryExecutor seam whose inert implementation
  succeeds at once with empty results (#2066 replaces it with Trino).
operations: {}
findings:
  fixed:
    - "#66: reviewed. tests/integration/athena/athena_test.go now pins the not-found error code and
      message for GetQueryExecution, GetQueryResults, GetWorkGroup and DeleteWorkGroup, the empty
      ResultSet shape (ResultSetMetadata.ColumnInfo and Rows present, UpdateCount absent per the
      model), and StopQueryExecution (which answered 501 at the time). The two divergences the
      review found were tracked as #2009 and are fixed below."
    - "#2009: fixed. Every Athena not-found now answers HTTP 400, the awsJson1_1 default for
      InvalidRequestException (no @httpError in the pinned model), through the shared
      errQueryNotFound/errWorkGroupNotFound constructors in internal/services/athena/typed_logic.go.
      StopQueryExecution is registered on both the legacy and typed paths and declared Supported:
      an unknown id is InvalidRequestException/400, a known one an empty StopQueryExecutionOutput,
      and state is unchanged because every Overcast query is already SUCCEEDED (the model marks the
      operation @idempotent and declares no wrong-state error). Tests:
      TestGetQueryExecution_unknownId, TestGetQueryResults_unknownId, TestGetWorkGroup_unknownName,
      TestDeleteWorkGroup_unknownName, TestAthenaCreateWorkGroup_invalidTagRejected,
      TestStopQueryExecution_alreadyFinished, TestStopQueryExecution_unknownId."
    - "#2065: control plane completed. primary workgroup seeded on first use; CreateWorkGroup
      refuses duplicates; UpdateWorkGroup applies ConfigurationUpdates with Remove* flags;
      QueryExecution carries context, StatementType, EngineVersion, Statistics and the result
      configuration resolved against the workgroup (EnforceWorkGroupConfiguration); a missing
      output location is InvalidRequestException; ClientRequestToken idempotency; per-workgroup,
      paginated ListQueryExecutions/ListNamedQueries; named queries, prepared statements and data
      catalogs with their BatchGet operations; metadata operations read the Glue catalog. Tests:
      tests/integration/athena/*_sdk_test.go (AWS SDK for Go v2), internal/services/athena unit
      tests, tests/integration/cloudformation/athena_resources_test.go, compat group
      athena-control."
  remaining_gaps: []
  intentionally_partial:
    - "#2065: FEDERATED data catalogs answer 501, and LAMBDA/HIVE catalogs are stored but their
      metadata is not readable (no connector runs). Spark workgroups (PySpark engine) answer 501.
      Unverified choices recorded in the PR: BatchGet ErrorCode INVALID_INPUT for named queries and
      query executions; a missing named query on Delete is InvalidRequestException; only named
      queries block a non-recursive DeleteWorkGroup (the one kind of content both the API and
      CloudFormation references name); OutputLocation reports <location>/<id>.csv or .txt."
    - "#67: the inert query engine is already documented. docs/services/athena.md's subtitle
      ('no SQL is executed, so every query succeeds immediately with an empty result set'), its
      'Differences from AWS' table (Query execution: 'stored, never parsed or run'; Results:
      'Nothing is written'; Execution states: 'None of them is ever observed'; Statistics:
      'Absent'; Workgroup configuration: 'Echoed, not enforced'), and its Gotchas note ('Any
      assertion about the rows that come back needs real Athena') together describe the design
      boundary this issue asked to have recorded."
handoff:
  start_with: []
  do_not_redo: [UpdateWorkGroup, BatchGetQueryExecution, ListEngineVersions, CreateNamedQuery, GetNamedQuery, BatchGetNamedQuery, ListNamedQueries, UpdateNamedQuery, DeleteNamedQuery, CreatePreparedStatement, GetPreparedStatement, BatchGetPreparedStatement, ListPreparedStatements, UpdatePreparedStatement, DeletePreparedStatement, CreateDataCatalog, GetDataCatalog, ListDataCatalogs, UpdateDataCatalog, DeleteDataCatalog, GetDatabase, ListDatabases, GetTableMetadata, ListTableMetadata, StartQueryExecution, GetQueryExecution, GetQueryResults, StopQueryExecution, ListQueryExecutions, CreateWorkGroup, GetWorkGroup, ListWorkGroups, DeleteWorkGroup, TagResource, UntagResource, ListTagsForResource]
  commands: [go test -count=1 ./internal/services/athena/... ./tests/integration/athena/...]
