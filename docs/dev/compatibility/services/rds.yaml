service: rds
status: in_progress
priority: p1
reference_policy: aws_docs_only_unless_user_approves_real_aws
last_updated: 2026-09-23
protocols: [query]
implementation: [internal/services/rds]
integration_tests: [tests/integration/rds]
capability_docs: docs/services/rds.md
operation_count_estimate: 33 supported
current_support_level: DB instances, Aurora clusters, subnet/parameter groups, snapshots, logs, and tagging supported; Docker-backed where possible.
operations:
  CreateDBParameterGroup:
    status: reviewed
    scenarios:
      - name: Aurora MySQL 3 instance parameter group family
        status: fixed
        tests: [TestCreateDBParameterGroup_auroraMySQL8Family, rds-parameter-groups/CreateDBParameterGroup]
        evidence: >-
          CreateDBParameterGroup accepts aurora-mysql8.0 and echoes that family through the AWS Query response.
          AWS documents aurora-mysql8.0 for Aurora MySQL version 3 at
          https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/aurora-serverless-v2.setting-capacity.html
          and directs callers to discover valid families through DescribeDBEngineVersions at
          https://docs.aws.amazon.com/AmazonRDS/latest/APIReference/API_CreateDBParameterGroup.html.
      - name: Unknown parameter group family
        status: reviewed
        tests: [TestCreateDBParameterGroup_unknownFamily]
        evidence: Families absent from DescribeDBEngineVersions remain rejected with InvalidParameterValue.
  CreateDBInstance:
    status: in_progress
    scenarios:
      - name: AdditionalStorageVolumes and StorageOperationStatus (#2041)
        status: intentionally_partial
        tests: [TestCreateDBInstance_additionalStorageVolumesRejected, TestDescribeDBInstances_storageOperationStatusOmittedAtRest]
        evidence: >-
          rds-2014-10-31.json documents CreateDBInstanceMessage.AdditionalStorageVolumes as "supported for RDS
          for Oracle and RDS for SQL Server DB instances only" (added 2025-11-14), and DBInstance.StorageOperationStatus
          / StorageOperationPercentProgress as present "only while a storage operation is in progress" — covering
          PITR/snapshot restore, read-replica creation, blue/green deployment, Single-AZ->Multi-AZ conversion, and
          storage scaling — and absent otherwise (added 2026-07-20). Overcast emulates neither Oracle nor SQL Server
          (see docs/services/rds/limitations.md), so every engine CreateDBInstance can actually create is one AWS
          itself would refuse AdditionalStorageVolumes for; it is now rejected with InvalidParameterCombination
          instead of being silently accepted and never honoured. Overcast also runs no asynchronous storage
          operation, so StorageOperationStatus/StorageOperationPercentProgress are modelled on xmlDBInstance but
          always omitted — AWS's own documented resting value, not an invented "always complete" state.
        notes: >-
          Revisit alongside SQL Server support (docs/services/rds/limitations.md "Engines that are not
          emulated"): AdditionalStorageVolumes would then need real acceptance, storage, and per-volume
          AdditionalStorageVolumeOutput rendering (StorageVolumeStatus, StorageOperationStatus,
          StorageOperationPercentProgress) rather than outright rejection.
      - name: Lambda container DNS for DB instance endpoint
        status: fixed
        tests: [TestEndpointAliases_dbInstance, TestEndpointAliases_dbInstancePreservesCanonicalNameWhenEndpointIsIP]
        evidence: Docker-backed DB instance containers now register the advertised Endpoint.Address hostname as a Docker DNS alias on the Lambda network so Lambda execution containers can resolve endpoint hostnames.
      - name: Runtime and startup reconciliation after engine-container drift
        status: fixed
        tests: [TestHandleContainerEvent_recoversDockerLevelStop, TestHandleContainerEvent_coalescesDuplicateRecoveryEvents, TestHandleContainerEvent_stopsRestartingAfterRecoveryBudget, TestDockerRecoveryBackoff, TestDockerRecoveryBudget_resetsOnlyAfterStableAvailability, TestDockerRecoveryBudget_recognizesPersistedStabilityAfterRestart, TestReconcileContainers_reconcilesMissedStop, TestReconcileContainers_retriesRecoveryInterruptedByDaemonShutdown, TestHandleContainerStarted_preservesExplicitStop, TestHandleContainerEvent_ignoresOvercastShutdown, TestReconcileContainers_rebuildsMissingAvailableInstance, TestReconcileContainers_rebuildsMissingExternallyStoppedInstance, TestReconcileContainers_restartsExitedContainerForAvailableInstance, TestReconcileContainers_leavesExplicitlyStoppedInstanceStopped, TestReconcileContainers_stopsDockerStartedExplicitlyStoppedInstance]
        evidence: Docker-level stops and crashes are recovered with bounded exponential backoff, duplicate die events are coalesced, and repeated crash loops settle to failed after five attempts unless the engine remains available for a two-minute stability period. Missed changes are reconciled when the Docker event stream reconnects, and startup rebuilds missing or exited containers. Only StopDBInstance establishes a durable stop; reconciliation stops a container started out of band for such an instance.
      - name: Master-account initialization and initial database semantics
        status: fixed
        tests: [TestMasterUser_realAuroraMySQLCanBootstrapDatabaseAndUser, TestMasterUser_realPostgresHasRDSSuperuserSemantics, TestCreateAuroraMySQL3Container_bootstrapsAWSMasterCredentialsBeforeStart, TestCredentialInitializersGiveEveryRequestedMasterAWSBootstrapPrivileges, TestMySQLMasterAccountSpec_engineVersionSemantics, TestCreateMySQLContainer_withoutDBNameDoesNotCreateTestDatabase, TestCreateDBInstance_auroraMemberUsesClusterOwnedSettings, TestValidateMasterUserPassword]
        evidence: >-
          Docker-backed Aurora MySQL and PostgreSQL tests connect as the requested master and create databases and users. MySQL-family privileges and roles vary by the selected AWS engine version; PostgreSQL exposes a non-superuser master with the documented rds_superuser memberships. Readiness waits for completed initialization and the final engine server without depending on mutable master credentials, and a SQL-side password change survives stop/start. Aurora members always use cluster-owned credentials, engine version, port, database, and subnet placement. Password and identifier shape constraints follow the supported AWS request forms.
  CreateDBCluster:
    status: in_progress
    scenarios:
      - name: Container DNS and dialable port for Aurora cluster endpoints
        status: fixed
        tests: [TestClusterEndpointAliases_coverBothRolesOnEveryMintableHostname, TestClusterAliasesForInstance_onlyTheWriterCarriesThem, TestClusterAliasesForInstance_anticipatesTheFirstMembersWriterRole, TestContainerAliases_carryBothTheInstanceAndClusterEndpoints, TestContainerAliases_standaloneInstanceCarriesOnlyItsOwn, TestClusterEndpoints_containerCallerGetsTheNamesAndEnginePort, TestClusterEndpoints_hostCallerGetsThePublishedPort, TestClusterEndpoints_withoutAWriterKeepTheNameAndClusterPort]
        evidence: >-
          The writer member's engine container now registers both cluster endpoint hostnames as Docker network
          aliases alongside its own, so cluster.clusterEndpoint.hostname — the property CDK's rds.DatabaseCluster
          exposes and the idiomatic way to reference a cluster — resolves from sibling containers instead of
          falling through to public DNS and answering loopback. DescribeDBClusters renders Endpoint,
          ReaderEndpoint and Port from the writer instance on the same per-caller rules as
          DescribeDBInstances, so a host caller is given the published port rather than the engine port
          nothing binds on the host. AWS documents the two cluster endpoints at
          https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/Aurora.Overview.Endpoints.html.
      - name: Reader endpoint instance selection
        status: intentionally_partial
        evidence: >-
          AWS load-balances the reader endpoint across the Aurora Replicas and serves the writer only when the
          cluster has none. Overcast gives every member its own engine container with its own storage — there is
          no shared Aurora volume — so the reader endpoint resolves to the writer. Distributing it across
          replicas would return an empty result set for data the writer holds. Consequence for users: replica
          lag is not reproducible locally, so a read-after-write race against the reader endpoint passes here
          and can still fail on AWS.
      - name: Cluster endpoint hostname shape
        status: fixed
        tests: [TestClusterEndpointAliases_coverBothRolesOnEveryMintableHostname, TestClusterEndpoints_containerCallerGetsTheNamesAndEnginePort]
        evidence: >-
          The writer endpoint label was cluster-rw, which AWS has never used, and is now cluster — what AWS's
          cluster-{hash} reduces to once the account-specific hash every Overcast endpoint name drops is
          removed. Changed in 0.0.1-alpha.37 while the project is pre-1.0. AWS documents the endpoint shapes at
          https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/Aurora.Overview.Endpoints.html.
      - name: Aurora cluster lifecycle over the AWS SDKs and the CLI
        status: fixed
        tests: [rds-clusters/CreateDBCluster, rds-clusters/DescribeDBClusters, rds-clusters/ModifyDBCluster, rds-clusters/StopDBCluster, rds-clusters/StartDBCluster, rds-clusters/DeleteDBCluster]
        evidence: >-
          The rds-clusters compat group exercises CreateDBCluster, DescribeDBClusters, ModifyDBCluster,
          StopDBCluster, StartDBCluster and DeleteDBCluster through five unmodified AWS clients — the
          Java, Go, Python and Node SDKs and the AWS CLI — so the Query XML the Aurora cluster
          operations emit is now parsed by real SDK deserializers rather than only by Overcast's own Go
          tests. The group asserts the created identifier and ARN, that DescribeDBClusters echoes the
          engine and reports distinct non-empty writer and reader endpoints, that ModifyDBCluster's
          BackupRetentionPeriod survives a read-back, that the available -> stopped -> available
          transitions are observable, and that the cluster is absent from DescribeDBClusters after
          delete. Endpoint assertions check shape rather than literal hostnames, since the hostname
          suffix depends on the host the caller reached Overcast on.
        notes: >-
          Coverage only. This does not constitute an AWS-docs review of CreateDBCluster's optional
          parameters, defaults, or validation error codes — see findings.remaining_gaps.
      - name: Documented parameter constraints and defaults
        status: fixed
        tests: [TestCreateDB_identifierShape, TestCreateDB_identifierIsStoredLowercase, TestCreateDBCluster_backupRetentionPeriod, TestCreateDB_portRange, TestCreateDB_subnetGroupMustExist, TestModifyDBCluster_zeroValuesAreRequestsNotAbsences]
        evidence: >-
          Four constraints AWS documents were unenforced, every one of them permissive — the direction that
          provisions here and fails in the account. DBClusterIdentifier and DBInstanceIdentifier now follow
          "Must contain from 1 to 63 letters, numbers, or hyphens. First character must be a letter. Can't end
          with a hyphen or contain two consecutive hyphens", and are stored lowercase as
          "This parameter is stored as a lowercase string" requires; store keys are normalised too, so a
          describe for the name the caller sent finds the record the create made. BackupRetentionPeriod on a
          cluster defaults to 1 and is held to 1-35 on CreateDBCluster and ModifyDBCluster alike, both of which
          document that range. Port is held to the documented 1150-65535 on both operations. DBSubnetGroupName
          must "match the name of an existing DB subnet group", and DBSubnetGroupNotFoundFault is now the 404
          AWS documents rather than a 400.
        notes: >-
          Instance-level BackupRetentionPeriod is not modelled at all — CreateDBInstance documents 0-35 there,
          0 meaning backups disabled, which has no counterpart in the cluster range. DBClusterParameterGroupName
          existence is unverifiable while no cluster parameter group operations exist; see
          findings.remaining_gaps.
  DeleteDBInstance:
    status: in_progress
    scenarios:
      - name: Cluster membership and writer re-election
        status: fixed
        tests: [TestDeleteDBInstance_dropsTheMemberFromItsCluster, TestDeleteDBInstance_promotesASurvivorWhenTheWriterIsDeleted, TestDeleteDBInstance_deletingAReplicaLeavesTheWriterInPlace, TestDeleteDBInstance_lastMemberLeavesTheClusterEmpty, TestDeleteDBInstance_promotesTheHighestPriorityTier, TestRemoveInstanceFromCluster_unknownClusterIsANoOp, TestRemoveInstanceFromCluster_unknownMemberLeavesTheWriterAlone]
        evidence: >-
          addInstanceToCluster had no counterpart, so DeleteDBInstance dropped the instance record and left the
          membership entry — DescribeDBClusters kept reporting a member that no longer existed, and deleting the
          writer left IsClusterWriter false on every member. Members are now pruned, and losing the writer promotes
          a survivor: AWS documents deleting or losing the primary as a failover that promotes an Aurora Replica
          chosen by promotion tier, 0 being the highest priority and 15 the lowest, at
          https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/Concepts.AuroraHighAvailability.html.
          Ties within a tier are settled on the oldest surviving member, which sits inside AWS's own allowance to
          promote "an arbitrary replica in the same promotion tier" while staying reproducible across runs.
      - name: Cluster endpoints follow a promoted writer
        status: fixed
        tests: [TestAdoptClusterEndpoints_movesTheClusterNamesOntoTheNewWriter, TestDeleteDBInstance_clusterEndpointFollowsThePromotedWriter, TestDeleteDBInstance_lastMemberMovesNoAliases, TestReattach_leavesThePlaneBeforeRejoiningIt, TestReattach_coversEveryPlaneThePlacementPutsItOn, TestReattach_toleratesAContainerThatIsNotOnThePlane]
        evidence: >-
          Both cluster endpoint names are Docker aliases on the writer's engine container, and Docker fixes a
          container's aliases when it joins a network — so a promotion that only rewrote the record left both names
          resolving to the container that had just been deleted. The promoted member's container now leaves its
          data planes and rejoins them advertising the cluster names, which is the only way an alias set changes;
          dataplane.Attach treats a repeat connect as success and would silently keep the old set. Existing
          connections on those planes drop across the rejoin, matching the interruption an AWS failover causes.
          Nothing detaches the outgoing writer: DeleteDBInstance has already stopped its container and queued it
          for removal.
      - name: Cluster membership over the AWS SDKs and the CLI
        status: fixed
        tests: [rds-cluster-members/CreateClusterForMembers, rds-cluster-members/AddClusterMember, rds-cluster-members/DescribeClusterMembers, rds-cluster-members/DescribeMemberInheritedSettings, rds-cluster-members/RemoveClusterMember, rds-cluster-members/DeleteClusterAfterMembers]
        evidence: >-
          The rds-cluster-members compat group adds an instance to an Aurora cluster through five
          unmodified AWS clients and asserts what only Go tests had checked: that DBClusterMembers lists
          the member and records it as the writer, that the member inherits the cluster's MasterUsername
          and EngineVersion despite sending neither, and that DeleteDBInstance prunes the membership
          entry rather than leaving a member that no longer exists. Port is deliberately not asserted,
          since Overcast answers with a dialable port that differs between a host and a sibling
          container.
        notes: >-
          Kept apart from rds-clusters rather than added to it. A writer member changes what the cluster
          endpoints mean — they can collapse onto one loopback address for a host caller, which is
          deliberate and would break that group's "the two endpoints differ" assertion. A second group
          declaring CreateDBCluster would also make the bare impl key ambiguous in every suite and abort
          the run, hence the distinct test names carrying the real operation in the registry's op field.
  StopDBCluster:
    status: fixed
    scenarios:
      - name: Stop and Start answer in their own response envelope
        status: fixed
        tests: [TestClusterResponses_useTheirOwnEnvelope, rds-clusters/StopDBCluster, rds-clusters/StartDBCluster]
        evidence: >-
          StopDBCluster and StartDBCluster returned an xmlCreateDBClusterResponse, so the wire carried
          CreateDBClusterResponse/CreateDBClusterResult for both. Every AWS SDK refused the answer
          outright — "deserialization failed, failed to decode response body, StopDBClusterResult node
          not found" — while the state transition itself was correct, so no assertion on the stored
          record could see it. AWS documents StopDBClusterResult and StartDBClusterResult at
          https://docs.aws.amazon.com/AmazonRDS/latest/APIReference/API_StopDBCluster.html and
          https://docs.aws.amazon.com/AmazonRDS/latest/APIReference/API_StartDBCluster.html.
          Both operations now have their own envelope types, the duplicate raw Query handlers were
          collapsed onto the typed implementations as Create/Modify/Delete already were, and
          TestClusterResponses_useTheirOwnEnvelope pins the root and result element names for all six
          cluster operations. Found by the rds-clusters compat group on its first run.
findings:
  fixed:
    - CreateDBParameterGroup accepts the advertised aurora-mysql8.0 family used by Aurora MySQL 3 instance parameter groups while still rejecting unknown families.
    - Docker-backed RDS DB instance endpoint hostnames are registered as Docker DNS aliases on the Lambda network.
    - Missing, crashed, or externally stopped DB instance containers are recovered with bounded backoff, after Docker reconnects, and at startup without overriding an explicit StopDBInstance; repeated crash loops settle to failed.
    - Master accounts have version-specific cross-database bootstrap privileges without exposing a remote root credential; Aurora MySQL 3 uses mysql_native_password, newer role models are represented, and PostgreSQL masters are NOSUPERUSER members of rds_superuser.
    - Initial database creation and password validation follow the supported engine's AWS-specific rules, including Aurora DatabaseName inheritance.
    - Aurora cluster endpoints resolve to the writer member's container from sibling containers, and carry a port the caller can actually dial.
    - DeleteDBInstance removes the instance from its cluster's DBClusterMembers, and deleting the writer promotes a surviving member and moves the cluster endpoints onto its container.
    - Aurora DB cluster create/describe/modify/stop/start/delete are exercised by five unmodified AWS clients through the rds-clusters compat group, so the cluster Query XML is parsed by real SDK deserializers instead of only by Overcast's own Go tests.
    - StopDBCluster and StartDBCluster return their own response envelopes rather than CreateDBClusterResponse, which every AWS SDK had been refusing to deserialize.
    - CreateDBCluster and CreateDBInstance enforce the documented identifier shape, store identifiers lowercase, and reject a DBSubnetGroupName that does not exist; DBSubnetGroupNotFoundFault is a 404 as AWS documents.
    - Cluster BackupRetentionPeriod defaults to 1 and is held to the documented 1-35 on create and modify; Port is held to 1150-65535 on both.
    - CloudFormation no longer mints a physical ID containing two consecutive hyphens when truncation lands on a separator, which several services — RDS among them — refuse.
    - Aurora cluster membership — DBClusterMembers, writer election, cluster-owned settings on a member, and pruning on DeleteDBInstance — is exercised by five unmodified AWS clients through the rds-cluster-members compat group.
  remaining_gaps:
    - DBClusterParameterGroupName and DBInstanceParameterGroupName are recorded without an existence check. AWS answers DBClusterParameterGroupNotFound (404) for a name that does not exist, which Overcast cannot do while it implements no cluster parameter group operations at all — CreateDBClusterParameterGroup would have to come first.
    - Instance-level BackupRetentionPeriod is not modelled, so CreateDBInstance's documented 0-35 range has nothing to validate and automated backups cannot be disabled per instance.
    - The remaining CreateDBInstance/CreateDBCluster optional parameters beyond identifier, port, retention and subnet group — AllocatedStorage, StorageType, EngineVersion, PreferredBackupWindow and PreferredMaintenanceWindow among them — are still accepted without range or format checks.
    - FailoverDBCluster is not implemented, so a promotion can only be triggered by deleting the writer, not asked for directly.
  intentionally_partial:
    - The Aurora reader endpoint resolves to the writer rather than load-balancing across replicas, because members do not share storage.
    - Cluster endpoint hostnames drop AWS's account-specific hash, as every Overcast endpoint name does: AWS's cluster-{hash} and cluster-ro-{hash} reduce to cluster and cluster-ro. The hash is service-assigned and not derivable, so a name carrying one could not be registered as a Docker alias ahead of the first lookup.
    - A member is dropped from DBClusterMembers when DeleteDBInstance is accepted rather than when the instance record finally goes; AWS keeps it listed while the instance is "deleting". The window is one scheduler tick, and closing it early keeps the cluster from advertising a container that has already been stopped.
    - Writer promotion honours PromotionTier and breaks ties on the oldest surviving member. AWS breaks the same tie on instance size and then arbitrarily; Overcast runs every member as the same container whatever its DBInstanceClass, so there is no size to compare.
    - Protected internal-account enforcement and the complete catalog of AWS-managed rds_* procedures are not emulated by stock engine images.
    - PostgreSQL extension availability follows the backing image rather than AWS's RDS extension allowlist.
    - Reserved-word validation covers common SQL keywords and engine system schemas, not every version-specific engine reserved word.
    - CreateDBInstance's AdditionalStorageVolumes is rejected for every engine Overcast can create (Oracle/SQL Server only on AWS, neither emulated), and DBInstance's StorageOperationStatus/StorageOperationPercentProgress are always omitted since Overcast runs no asynchronous storage operation (#2041).
handoff:
  start_with: [CreateDBInstance validation errors, CreateDBCluster optional parameters and defaults, DBClusterMembers]
  do_not_redo: [Aurora MySQL 3 instance parameter group family, Lambda container DNS for Docker-backed RDS DB instance endpoint hostnames, runtime and startup recovery of DB instance containers, master-account initialization and initial database semantics, Aurora cluster happy-path lifecycle and cluster XML response parity across the SDK/CLI compat suites]
  commands: [go test -count=1 ./internal/services/rds/... ./tests/integration/rds/...]
